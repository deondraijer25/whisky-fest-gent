---
// src/pages/standhouders.astro
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getStandhouders } from '../lib/ghl';

const exhibitors = await getStandhouders('gent');
---

<Layout 
  title="Interactieve Plattegrond & Standhouders" 
  description="Navigeer door de De Oude Vismijn Gent met onze interactieve plattegrond. Zoek direct waar uw favoriete whiskymerken en standhouders zich bevinden op het International Whisky Festival."
>
  <!-- Navigation Header -->
  <Header />

  <main class="standhouders-page-wrapper">
    <!-- Page Header -->
    <div class="page-title-section animate-fade-in">
      <span class="page-tag">Festival Gids 2026</span>
      <h1 class="page-title">Plattegrond & Stands</h1>
      <div class="vintage-line"></div>
      <p class="page-subtitle">Verken de Grote Kerk, zoek uw favoriete merken en zie direct welke standhouder zich waar bevindt.</p>
    </div>

    <!-- Horizontal Search & Filters Bar -->
    <div class="search-filter-horizontal-bar vintage-frame animate-fade-in" style="animation-delay: 0.1s;">
      <div class="vintage-frame-inner horizontal-bar-inner">
        
        <div class="search-input-wrapper">
          <input type="text" id="exhibitor-search" class="form-control" placeholder="Zoek op standhouder of merk..." />
          <span class="search-icon-inside">
            <svg class="search-icon-svg" viewBox="0 0 24 24" width="16" height="16" stroke="var(--text-charcoal)" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="11" cy="11" r="8"></circle>
              <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
            </svg>
          </span>
        </div>
        
        <div class="filter-buttons-row">
          <button type="button" class="filter-tab active" data-filter="all">Alle</button>
          <button type="button" class="filter-tab" data-filter="scotch">Schots</button>
          <button type="button" class="filter-tab" data-filter="dutch">Nederlands</button>
          <button type="button" class="filter-tab" data-filter="independent">Onafhankelijk</button>
          <button type="button" class="filter-tab" data-filter="world">Wereld</button>
          <button type="button" class="filter-tab" data-filter="catering">Food & Catering</button>
        </div>

      </div>
    </div>

    <!-- Interactive Map & Detail Layout -->
    <div class="map-detail-layout animate-fade-in" style="animation-delay: 0.15s;">
      
      <!-- LEFT COLUMN: The Interactive Floor Plan Map -->
      <div class="map-visual-container vintage-frame">
        <div class="vintage-frame-inner map-scroll-area">
          <div class="map-toolbar">
            <span class="toolbar-title">Plattegrond De Oude Vismijn Gent</span>
            <div class="zoom-controls">
              <button type="button" class="zoom-btn" id="zoom-in" title="Inzoomen">＋</button>
              <button type="button" class="zoom-btn" id="zoom-out" title="Uitzoomen">－</button>
              <button type="button" class="zoom-btn" id="zoom-reset" title="Herstellen">Herstel</button>
            </div>
          </div>
          
          <div class="svg-zoom-wrapper" id="svg-zoom-target">
            <!-- Dynamic Vector SVG Map of Grote Kerk -->
            <svg viewBox="0 0 980 500" class="church-svg-map" id="church-map">
              <defs>
                <pattern id="pillar-pattern" width="10" height="10" patternUnits="userSpaceOnUse">
                  <circle cx="5" cy="5" r="3" fill="var(--border-color)" />
                </pattern>
              </defs>
              
              <!-- Subtle blueprint lines / grid background -->
              <rect width="980" height="500" fill="transparent" />
              
              <!-- Church Outer Walls Silhouette (gothic outline) -->
              <path d="M 140 150 
                       L 140 80 Q 140 70 150 70 L 640 70 L 640 20 Q 640 10 650 10 L 760 10 Q 770 10 770 20 L 770 70 L 820 70 
                       L 860 90 L 900 130 L 920 180 L 930 250 L 920 320 L 900 370 L 860 410 L 820 430 L 770 430 L 770 480 Q 770 490 760 490 L 650 490 Q 640 490 640 480 L 640 430 L 250 430 L 250 480 Q 250 490 240 490 L 150 490 Q 140 490 140 480 L 140 430 L 140 350
                       L 90 350 L 50 310 L 30 250 L 50 190 L 90 150 Z" 
                    fill="var(--bg-sand)" stroke="var(--border-color-dark)" stroke-width="2" stroke-linejoin="round" class="church-outline" />

              <!-- Interior Architecture Zones -->
              <!-- Hoogkoor (High Choir) Area -->
              <rect x="670" y="150" width="180" height="200" fill="#EBE4D5" stroke="var(--border-color)" stroke-width="1.5" stroke-dasharray="4 4" />
              <text x="760" y="255" class="map-zone-label">Hoogkoor</text>
              
              <!-- Kooromgang (Ambulatory) Arch Guide -->
              <path d="M 670 80 A 170 170 0 0 1 890 250 A 170 170 0 0 1 670 420" fill="none" stroke="var(--border-color)" stroke-width="1" stroke-dasharray="6 6" />

              <!-- Structural Pillars (Gothic columns) -->
              <g class="pillars">
                <!-- Row Top -->
                <circle cx="200" cy="140" r="10" class="pillar" />
                <circle cx="280" cy="140" r="10" class="pillar" />
                <circle cx="360" cy="140" r="10" class="pillar" />
                <circle cx="440" cy="140" r="10" class="pillar" />
                <circle cx="520" cy="140" r="10" class="pillar" />
                <circle cx="600" cy="140" r="10" class="pillar" />
                
                <!-- Row Bottom -->
                <circle cx="200" cy="360" r="10" class="pillar" />
                <circle cx="280" cy="360" r="10" class="pillar" />
                <circle cx="360" cy="360" r="10" class="pillar" />
                <circle cx="440" cy="360" r="10" class="pillar" />
                <circle cx="520" cy="360" r="10" class="pillar" />
                <circle cx="600" cy="360" r="10" class="pillar" />

                <!-- Choir pillars -->
                <circle cx="670" cy="150" r="8" class="pillar" />
                <circle cx="720" cy="115" r="8" class="pillar" />
                <circle cx="780" cy="100" r="8" class="pillar" />
                <circle cx="840" cy="115" r="8" class="pillar" />
                <circle cx="890" cy="150" r="8" class="pillar" />
                
                <circle cx="670" cy="350" r="8" class="pillar" />
                <circle cx="720" cy="385" r="8" class="pillar" />
                <circle cx="780" cy="400" r="8" class="pillar" />
                <circle cx="840" cy="385" r="8" class="pillar" />
                <circle cx="890" cy="350" r="8" class="pillar" />
              </g>

              <!-- EXTERNAL ANNOTATIONS & ZONES -->
              <!-- Top-Right: Buitenruimte / Food Trucks / Vaten-Maker -->
              <g class="map-annotation-zone">
                <path d="M 770 40 Q 810 25 840 20 M 840 20 L 825 18 M 840 20 L 836 32" stroke="#1E3A8A" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" fill="none" />
                <text x="850" y="22" font-family="var(--font-sans)" font-size="11" font-weight="800" fill="#1E3A8A">Workshop ruimte "Vaten-Maker"</text>
                <text x="850" y="36" font-family="var(--font-sans)" font-size="11" font-weight="800" fill="#1E3A8A">Food trucks</text>
                <text x="850" y="50" font-family="var(--font-sans)" font-size="12" font-weight="900" fill="#b57402">Buitenterrein ↗</text>
              </g>

              <!-- Top-Left: Garderobe en uitgang (Clean text label) -->
              <g class="map-annotation-zone">
                <text x="15" y="30" font-family="var(--font-sans)" font-size="13" font-weight="800" fill="var(--text-charcoal)">Garderobe en uitgang</text>
              </g>

              <!-- Bottom-Left: Nutshuis (Clean text label block) -->
              <g class="map-annotation-zone">
                <text x="15" y="425" font-family="var(--font-sans)" font-size="13" font-weight="900" fill="var(--text-charcoal)">Nutshuis</text>
                <text x="15" y="440" font-family="var(--font-sans)" font-size="11" font-weight="800" fill="var(--text-charcoal)">ALLE masterclasses</text>
                <text x="15" y="453" font-family="var(--font-sans)" font-size="9.5" font-weight="600" fill="var(--text-muted)">m.u.v. de Toren-tastings</text>
              </g>

              <!-- ============================================== -->
              <!-- STANDS GRAPHICAL BLOCKS (SVG Interactive elements) -->
              <!-- ============================================== -->
              
              <!-- LEFT AREA (Main Entrance & Stand U) -->
              <g class="svg-stand" data-stand="U">
                <rect x="50" y="210" width="60" height="80" rx="4" class="stand-box" />
                <text x="80" y="255" class="stand-num">U</text>
              </g>

              <!-- Entrance Text Labels (Above and Below Corridor) -->
              <text x="80" y="195" font-family="var(--font-sans)" font-size="11" font-weight="900" fill="var(--text-charcoal)" text-anchor="middle">Uitgifte glazen +</text>
              <text x="80" y="308" font-family="var(--font-sans)" font-size="11" font-weight="900" fill="var(--text-charcoal)" text-anchor="middle">Festival botteling</text>

              <!-- Entrance Corridor Arrow pointing into the Schip -->
              <g class="entrance-arrow">
                <path d="M 120 250 L 138 250 M 132 245 L 138 250 L 132 255" stroke="var(--text-charcoal)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none" />
              </g>

              <!-- Entrance Corridor Arrow pointing into the Schip -->
              <g class="entrance-arrow">
                <path d="M 125 250 L 140 250 M 135 245 L 140 250 L 135 255" stroke="var(--text-charcoal)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none" />
              </g>

              <!-- Stands 1 & 2 (Left wall above entrance passage) -->
              <g class="svg-stand" data-stand="2">
                <rect x="155" y="155" width="22" height="28" rx="2" class="stand-box" />
                <text x="166" y="173" class="stand-num text-small">2</text>
              </g>
              <g class="svg-stand" data-stand="1">
                <rect x="155" y="188" width="22" height="28" rx="2" class="stand-box" />
                <text x="166" y="206" class="stand-num text-small">1</text>
              </g>

              <!-- Stand 13 (Left wall below entrance passage) -->
              <g class="svg-stand" data-stand="13">
                <rect x="155" y="260" width="22" height="28" rx="2" class="stand-box" />
                <text x="166" y="278" class="stand-num text-small">13</text>
              </g>

              <g class="svg-stand" data-stand="C">
                <rect x="145" y="305" width="45" height="45" rx="4" class="stand-box catering" />
                <text x="167" y="332" class="stand-num text-small">C</text>
              </g>

              <!-- Main Nave: Top Row (Stands 3, 4, 5, 6) -->
              <g class="svg-stand" data-stand="3">
                <rect x="155" y="85" width="85" height="45" rx="3" class="stand-box" />
                <text x="197" y="112" class="stand-num">3</text>
              </g>
              <g class="svg-stand" data-stand="4">
                <rect x="250" y="85" width="130" height="35" rx="3" class="stand-box" />
                <text x="315" y="107" class="stand-num">4</text>
              </g>

              <g class="svg-stand" data-stand="5">
                <rect x="410" y="85" width="50" height="35" rx="3" class="stand-box" />
                <text x="435" y="107" class="stand-num">5</text>
              </g>

              <!-- Stand 6 is split into three table modules -->
              <g class="svg-stand" data-stand="6">
                <rect x="525" y="85" width="22" height="35" rx="2" class="stand-box" />
                <text x="536" y="107" class="stand-num text-small">6</text>
              </g>
              <g class="svg-stand" data-stand="6">
                <rect x="552" y="85" width="22" height="35" rx="2" class="stand-box" />
                <text x="563" y="107" class="stand-num text-small">6</text>
              </g>
              <g class="svg-stand" data-stand="6">
                <rect x="579" y="85" width="22" height="35" rx="2" class="stand-box" />
                <text x="590" y="107" class="stand-num text-small">6</text>
              </g>

              <!-- Main Nave: Middle-Left (Stands 12, 14, 14A, 19, 18, 17) -->
              <g class="svg-stand" data-stand="12">
                <rect x="250" y="170" width="130" height="60" rx="3" class="stand-box" />
                <text x="315" y="205" class="stand-num">12</text>
              </g>

              <g class="svg-stand" data-stand="14">
                <rect x="250" y="285" width="65" height="35" rx="3" class="stand-box" />
                <text x="282" y="307" class="stand-num">14</text>
              </g>
              <g class="svg-stand" data-stand="15">
                <rect x="315" y="285" width="65" height="35" rx="3" class="stand-box" />
                <text x="347" y="307" class="stand-num">15</text>
              </g>

              <g class="svg-stand" data-stand="22">
                <rect x="250" y="325" width="40" height="30" rx="3" class="stand-box" />
                <text x="270" y="345" class="stand-num text-small">22</text>
              </g>
              <g class="svg-stand" data-stand="21">
                <rect x="290" y="325" width="40" height="30" rx="3" class="stand-box" />
                <text x="310" y="345" class="stand-num text-small">21</text>
              </g>
              <g class="svg-stand" data-stand="20">
                <rect x="330" y="325" width="50" height="30" rx="3" class="stand-box" />
                <text x="355" y="345" class="stand-num text-small">20</text>
              </g>

              <!-- Main Nave: Middle-Center (Stands 11, 8, 9, 10, O, 16, 15) -->
              <g class="svg-stand" data-stand="11">
                <rect x="440" y="170" width="80" height="70" rx="3" class="stand-box" />
                <text x="480" y="210" class="stand-num">11</text>
              </g>
              <g class="svg-stand" data-stand="8">
                <rect x="525" y="170" width="35" height="33" rx="3" class="stand-box" />
                <text x="542" y="191" class="stand-num text-small">8</text>
              </g>
              <g class="svg-stand" data-stand="9">
                <rect x="565" y="170" width="35" height="33" rx="3" class="stand-box" />
                <text x="582" y="191" class="stand-num text-small">9</text>
              </g>
              <g class="svg-stand" data-stand="10">
                <rect x="525" y="208" width="75" height="32" rx="3" class="stand-box" />
                <text x="562" y="229" class="stand-num text-small">10</text>
              </g>

              <!-- Organisatie block: O (top-left), 16 (bottom-left), 17 (top-right), 18 (bottom-right) -->
              <g class="svg-stand" data-stand="O">
                <rect x="395" y="285" width="45" height="34" rx="3" class="stand-box organisation" />
                <text x="417" y="307" class="stand-num text-small">O</text>
              </g>
              <g class="svg-stand" data-stand="16">
                <rect x="395" y="322" width="45" height="34" rx="3" class="stand-box" />
                <text x="417" y="343" class="stand-num text-small">16</text>
              </g>
              <g class="svg-stand" data-stand="17">
                <rect x="443" y="285" width="45" height="34" rx="3" class="stand-box" />
                <text x="465" y="307" class="stand-num text-small">17</text>
              </g>
              <g class="svg-stand" data-stand="18">
                <rect x="443" y="322" width="45" height="34" rx="3" class="stand-box" />
                <text x="465" y="343" class="stand-num text-small">18</text>
              </g>
              
              <!-- Two central tasting island blocks: Stand 15 (Moët Hennessy) and Stand 19 (Helvetia Spirits) -->
              <g class="svg-stand" data-stand="15">
                <rect x="500" y="285" width="60" height="71" rx="3" class="stand-box" />
                <text x="530" y="325" class="stand-num">15</text>
              </g>
              <g class="svg-stand" data-stand="19">
                <rect x="570" y="285" width="60" height="71" rx="3" class="stand-box" />
                <text x="600" y="325" class="stand-num">19</text>
              </g>

              <!-- Main Nave: Bottom Row (Stands 23, 24, S, 25, 26, 27, 28) -->
              <g class="svg-stand" data-stand="23">
                <rect x="250" y="375" width="75" height="35" rx="3" class="stand-box" />
                <text x="287" y="397" class="stand-num text-small">23</text>
              </g>
              <g class="svg-stand" data-stand="24">
                <rect x="330" y="375" width="50" height="35" rx="3" class="stand-box" />
                <text x="355" y="397" class="stand-num text-small">24</text>
              </g>

              <g class="svg-stand" data-stand="S">
                <rect x="260" y="420" width="110" height="60" rx="4" class="stand-box shop" />
                <text x="315" y="455" class="stand-num">S</text>
              </g>

              <g class="svg-stand" data-stand="25">
                <rect x="400" y="375" width="45" height="35" rx="3" class="stand-box" />
                <text x="422" y="397" class="stand-num text-small">25</text>
              </g>
              <g class="svg-stand" data-stand="26">
                <rect x="450" y="375" width="45" height="35" rx="3" class="stand-box" />
                <text x="472" y="397" class="stand-num text-small">26</text>
              </g>
              <g class="svg-stand" data-stand="27">
                <rect x="500" y="375" width="45" height="35" rx="3" class="stand-box" />
                <text x="522" y="397" class="stand-num text-small">27</text>
              </g>
              <g class="svg-stand" data-stand="28">
                <rect x="550" y="375" width="45" height="35" rx="3" class="stand-box" />
                <text x="572" y="397" class="stand-num text-small">28</text>
              </g>

              <!-- EAST AREA: Chapels & Ambulatory (Choir area) -->
              <!-- Kapel Noord (Stand 42) -->
              <g class="svg-stand" data-stand="42">
                <rect x="645" y="20" width="90" height="60" rx="4" class="stand-box chapel" />
                <text x="690" y="55" class="stand-num">42</text>
              </g>
              <!-- Kapel Zuid (Stand 29) -->
              <g class="svg-stand" data-stand="29">
                <rect x="645" y="420" width="90" height="60" rx="4" class="stand-box chapel" />
                <text x="690" y="455" class="stand-num">29</text>
              </g>

              <!-- Hoogkoor center (Stand 44) -->
              <g class="svg-stand" data-stand="44">
                <rect x="700" y="190" width="120" height="120" rx="6" class="stand-box choir" />
                <text x="760" y="255" class="stand-num">44</text>
              </g>

              <!-- Inner ambulatory stands (43, 45, 46, 47, 48) -->
              <g class="svg-stand" data-stand="43">
                <rect x="795" y="150" width="30" height="30" rx="3" class="stand-box" />
                <text x="810" y="170" class="stand-num text-small">43</text>
              </g>
              <g class="svg-stand" data-stand="45">
                <rect x="830" y="170" width="30" height="30" rx="3" class="stand-box" />
                <text x="845" y="190" class="stand-num text-small">45</text>
              </g>
              <g class="svg-stand" data-stand="46">
                <rect x="845" y="210" width="30" height="80" rx="3" class="stand-box" />
                <text x="860" y="255" class="stand-num text-small">46</text>
              </g>
              <g class="svg-stand" data-stand="47">
                <rect x="830" y="300" width="30" height="30" rx="3" class="stand-box" />
                <text x="845" y="320" class="stand-num text-small">47</text>
              </g>
              <g class="svg-stand" data-stand="48">
                <rect x="795" y="320" width="30" height="30" rx="3" class="stand-box" />
                <text x="810" y="340" class="stand-num text-small">48</text>
              </g>

              <!-- Outer Curved Wall Ambulatory: Top-Right (41, 40, 39, 38) -->
              <g class="svg-stand" data-stand="41">
                <rect x="680" y="85" width="35" height="28" rx="2" class="stand-box" />
                <text x="697" y="103" class="stand-num text-small">41</text>
              </g>
              <g class="svg-stand" data-stand="40">
                <rect x="720" y="85" width="35" height="28" rx="2" class="stand-box" />
                <text x="737" y="103" class="stand-num text-small">40</text>
              </g>
              <g class="svg-stand" data-stand="39">
                <rect x="760" y="85" width="35" height="28" rx="2" class="stand-box catering" />
                <text x="777" y="103" class="stand-num text-small">39</text>
              </g>
              <g class="svg-stand" data-stand="38">
                <rect x="800" y="85" width="35" height="28" rx="2" class="stand-box" />
                <text x="817" y="103" class="stand-num text-small">38</text>
              </g>

              <!-- Outer Curved Wall: Right curving down (37, 36, 35, 34, 33, 32, 31, 30) -->
              <!-- 100% Straight, unrotated SVG stand boxes placed along the curve contour -->
              <g class="svg-stand" data-stand="37">
                <rect x="838" y="105" width="35" height="28" rx="2" class="stand-box" />
                <text x="855" y="123" class="stand-num text-small">37</text>
              </g>

              <g class="svg-stand" data-stand="36">
                <rect x="865" y="145" width="35" height="28" rx="2" class="stand-box" />
                <text x="882" y="163" class="stand-num text-small">36</text>
              </g>

              <g class="svg-stand" data-stand="35">
                <rect x="880" y="190" width="28" height="38" rx="2" class="stand-box" />
                <text x="894" y="214" class="stand-num text-small">35</text>
              </g>

              <g class="svg-stand" data-stand="34">
                <rect x="880" y="265" width="28" height="38" rx="2" class="stand-box" />
                <text x="894" y="289" class="stand-num text-small">34</text>
              </g>

              <g class="svg-stand" data-stand="33">
                <rect x="865" y="325" width="35" height="28" rx="2" class="stand-box" />
                <text x="882" y="343" class="stand-num text-small">33</text>
              </g>

              <g class="svg-stand" data-stand="32">
                <rect x="838" y="365" width="35" height="28" rx="2" class="stand-box" />
                <text x="855" y="383" class="stand-num text-small">32</text>
              </g>

              <g class="svg-stand" data-stand="31">
                <rect x="800" y="385" width="40" height="28" rx="2" class="stand-box" />
                <text x="820" y="403" class="stand-num text-small">31</text>
              </g>
              <g class="svg-stand" data-stand="30">
                <rect x="755" y="385" width="40" height="28" rx="2" class="stand-box" />
                <text x="775" y="403" class="stand-num text-small">30</text>
              </g>
            </svg>
          </div>
        </div>
      </div>
      
      <!-- RIGHT COLUMN: Stand Info Card -->
      <div class="sidebar-exhibitors-panel">
        
        <!-- Interactive Detail Card -->
        <div class="sidebar-card info-card-detail" id="stand-info-card">
          <div class="card-padded" id="default-info-state">
            <h3 class="card-inner-title text-center">Stand Detail</h3>
            <div class="vintage-line"></div>
            <p class="sidebar-card-text text-center italic-text">
              Klik op een standnummer op de plattegrond of kies een standhouder uit de lijst om de details, merken en standlocatie te bekijken.
            </p>
          </div>
          
          <div class="card-padded" id="active-info-state" style="display: none;">
            <div class="detail-stand-badge-row">
              <span class="detail-stand-number" id="detail-number">11</span>
              <span class="status-badge" id="detail-category-badge">Schots</span>
            </div>
            
            <div class="detail-content-block">
              <h3 class="card-inner-title" id="detail-name">Standhouder Naam</h3>
              <div class="vintage-line" style="margin: 0.5rem 0 1rem 0;"></div>
              
              <p class="sidebar-card-text" id="detail-description">
                Beschrijving van de standhouder.
              </p>
              
              <div class="detail-brands-box">
                <span class="brands-label">Aanwezige Merken:</span>
                <div class="brands-list-tags" id="detail-brands">
                  <!-- Dynamic brands go here -->
                </div>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>

    <!-- FULL DIRECTORY LIST BELOW -->
    <section class="directory-list-section">
      <div class="directory-section-header">
        <h2 class="directory-title">Deelnemerslijst (A-Z)</h2>
        <span class="results-counter" id="results-count">48 standhouders gevonden</span>
      </div>
      
      <div class="directory-grid" id="directory-grid">
        {exhibitors.map((exh) => (
          <div class="directory-item-card" data-id={exh.id} data-cat={exh.category} data-name={exh.name.toLowerCase()} data-brands={exh.brands.join(' ').toLowerCase()}>
            <div class="card-top-header">
              <span class="card-stand-number">{exh.isNotOnMap ? 'Niet op plattegrond' : `Stand ${exh.id}`}</span>
              <span class={`category-tag tag-${exh.category}`}>
                {exh.category === 'scotch' ? 'Schots' : 
                 exh.category === 'dutch' ? 'Nederlands' : 
                 exh.category === 'independent' ? 'Onafhankelijke Bottelaar' : 
                 exh.category === 'world' ? 'Wereld' : 
                 exh.category === 'catering' ? 'Catering' : 'Organisatie'}
              </span>
            </div>
            <div class="card-body-content">
              <h4>{exh.name}</h4>
              <p>{exh.description}</p>
              <div class="card-brands-row">
                <strong>Merken:</strong> {exh.brands.join(', ')}
              </div>
            </div>
            <div class="card-action-footer">
              {!exh.isNotOnMap && (
                <button type="button" class="btn-locate-stand" data-target={exh.id}>
                  Toon op plattegrond &rarr;
                </button>
              )}
            </div>
          </div>
        ))}
      </div>
    </section>

  </main>

  <!-- Global Footer -->
  <Footer />
</Layout>

<style>
  /* --- Page layout and structure --- */
  .standhouders-page-wrapper {
    background-color: var(--bg-parchment);
    min-height: 100vh;
    padding: 9rem 3rem 6rem;
    max-width: 1600px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 3.5rem;
  }

  /* --- Page Title Section --- */
  .page-title-section {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    max-width: 700px;
    margin: 0 auto;
  }

  .page-tag {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--whisky-gold-dark);
  }

  .page-title {
    font-family: var(--font-display);
    font-size: 3.75rem;
    color: var(--text-charcoal);
    margin: 0;
    line-height: 1;
  }

  .page-subtitle {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    color: var(--text-muted);
    margin: 0;
    line-height: 1.5;
  }

  /* --- Map and Detail Columns --- */
  .map-detail-layout {
    display: grid;
    grid-template-columns: 1.7fr 1fr;
    gap: 2.5rem;
    align-items: stretch;
  }

  /* Horizontal Search & Filter Bar */
  .search-filter-horizontal-bar {
    background-color: var(--bg-paper-card);
    border: 2px solid var(--text-charcoal);
    border-radius: 4px;
    box-shadow: 6px 6px 0px var(--text-charcoal);
  }

  .horizontal-bar-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2.5rem;
    padding: 1.25rem 2rem;
  }

  .search-filter-horizontal-bar .search-input-wrapper {
    flex: 1.2;
    min-width: 280px;
  }

  .filter-buttons-row {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    flex: 2;
    justify-content: flex-end;
  }

  .search-icon-svg {
    display: block;
  }

  /* --- Map Container & SVG Scroll Area --- */
  .map-visual-container {
    background-color: var(--bg-paper-card);
    border-radius: 4px;
    box-shadow: 6px 6px 0px var(--text-charcoal);
    overflow: hidden;
  }

  .map-scroll-area {
    padding: 1.5rem !important;
    position: relative;
    overflow: auto;
    max-height: 600px;
  }

  .map-toolbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid var(--text-charcoal);
    padding-bottom: 0.75rem;
    margin-bottom: 1rem;
    font-family: var(--font-sans);
  }

  .toolbar-title {
    font-weight: 800;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-charcoal);
  }

  .zoom-controls {
    display: flex;
    gap: 4px;
  }

  .zoom-btn {
    background-color: var(--bg-sand);
    border: 1.5px solid var(--text-charcoal);
    font-family: var(--font-sans);
    font-weight: 700;
    padding: 0.25rem 0.65rem;
    border-radius: 3px;
    cursor: pointer;
    font-size: 0.85rem;
    transition: all 0.2s;
  }

  .zoom-btn:hover {
    background-color: var(--whisky-gold);
    color: var(--bg-parchment);
  }

  .svg-zoom-wrapper {
    width: 100%;
    transform-origin: 0 0;
    transition: transform 0.2s cubic-bezier(0.16, 1, 0.3, 1);
  }

  /* --- SVG Map Styling --- */
  .church-svg-map {
    width: 100%;
    height: auto;
    display: block;
    user-select: none;
  }

  .church-outline {
    fill: var(--bg-sand);
    stroke: var(--text-charcoal);
    stroke-width: 2.5;
  }

  .pillar {
    fill: var(--border-color-dark);
    stroke: var(--text-charcoal);
    stroke-width: 1;
    opacity: 0.9;
  }

  .map-zone-label {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 1.1rem;
    fill: var(--text-muted);
    opacity: 0.25;
    text-anchor: middle;
    pointer-events: none;
  }

  /* --- SVG Stand Boxes --- */
  .stand-box {
    fill: var(--bg-paper-card);
    stroke: var(--text-charcoal);
    stroke-width: 1.8;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .stand-num {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.85rem;
    fill: var(--text-charcoal);
    text-anchor: middle;
    pointer-events: none;
    user-select: none;
  }

  .stand-num.text-small {
    font-size: 0.75rem;
  }

  /* Hover states for SVG stands */
  .svg-stand {
    cursor: pointer;
  }

  .svg-stand:hover .stand-box {
    fill: var(--whisky-gold-light);
    stroke: var(--whisky-gold-dark);
    filter: drop-shadow(0 2px 5px rgba(197, 139, 57, 0.2));
  }

  /* Functional Areas Styling */
  .stand-box.shop {
    fill: rgba(44, 94, 67, 0.05);
    stroke: var(--success-green);
  }
  .svg-stand[data-stand="S"]:hover .stand-box {
    fill: var(--success-green-light);
    stroke: var(--success-green);
  }
  .svg-stand[data-stand="S"] .stand-num {
    fill: var(--success-green);
  }

  .stand-box.organisation {
    fill: rgba(143, 91, 25, 0.05);
    stroke: var(--whisky-gold-dark);
  }
  .svg-stand[data-stand="O"] .stand-num {
    fill: var(--whisky-gold-dark);
  }

  .stand-box.catering {
    fill: #FFF3CD;
    stroke: #856404;
  }
  .svg-stand[data-stand="C"]:hover .stand-box {
    fill: #FFEBAA;
  }
  .svg-stand[data-stand="C"] .stand-num {
    fill: #856404;
  }

  /* Interactive active selection state in SVG */
  .svg-stand.selected-stand .stand-box {
    fill: var(--whisky-gold-dark);
    stroke: var(--text-charcoal);
    stroke-width: 2.5px;
    animation: pulseGlow 1.5s infinite;
  }

  .svg-stand.selected-stand .stand-num {
    fill: var(--bg-parchment) !important;
    font-weight: 900;
  }

  /* Dimmed state during search/filter */
  .svg-stand.dimmed-stand {
    opacity: 0.15;
  }

  .svg-stand.highlighted-search-stand .stand-box {
    fill: var(--whisky-gold-light);
    stroke: var(--whisky-gold-dark);
    stroke-width: 2px;
  }

  @keyframes pulseGlow {
    0% { filter: drop-shadow(0 0 0px var(--whisky-gold)); }
    50% { filter: drop-shadow(0 0 8px var(--whisky-gold)); }
    100% { filter: drop-shadow(0 0 0px var(--whisky-gold)); }
  }

  /* --- Sidebar Panel --- */
  .sidebar-exhibitors-panel {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    height: 100%;
  }

  .sidebar-card {
    background-color: var(--bg-paper-card);
    border: 2px solid var(--text-charcoal);
    border-radius: 4px;
    box-shadow: 6px 6px 0px var(--text-charcoal);
    display: flex;
    flex-direction: column;
  }

  .card-padded {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .card-inner-title {
    font-family: var(--font-display);
    font-size: 1.65rem;
    text-transform: uppercase;
    color: var(--text-charcoal);
    margin: 0;
    line-height: 1.1;
    letter-spacing: 0.03em;
  }

  .sidebar-card-text {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--text-charcoal);
    margin: 0;
  }

  .sidebar-card-text.italic-text {
    font-family: var(--font-serif);
    font-style: italic;
    color: var(--text-muted);
  }

  /* --- Stand Detail Card (Dynamic) --- */
  .info-card-detail {
    height: 100%;
    transition: all 0.3s;
  }

  .info-card-detail .card-padded {
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  #default-info-state {
    justify-content: center;
  }

  .info-card-header-art {
    font-size: 3rem;
    text-align: center;
    line-height: 1;
    margin-bottom: 0.5rem;
  }

  .detail-stand-badge-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid var(--text-charcoal);
    padding-bottom: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .detail-stand-number {
    font-family: var(--font-display);
    font-size: 2.25rem;
    color: var(--whisky-gold-dark);
    line-height: 1;
  }

  .detail-content-block {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .detail-brands-box {
    border-top: 1px dotted var(--border-color-dark);
    padding-top: 0.85rem;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .brands-label {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
  }

  .brands-list-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .brand-tag {
    background-color: var(--bg-sand);
    border: 1px solid var(--border-color-dark);
    color: var(--text-charcoal);
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.75rem;
    padding: 0.25rem 0.5rem;
    border-radius: 3px;
  }

  /* --- Search & Filters Card --- */
  .search-input-wrapper {
    position: relative;
    width: 100%;
  }

  .form-control {
    width: 100%;
    background-color: var(--bg-sand);
    border: 2px solid var(--text-charcoal);
    border-radius: 4px;
    padding: 0.75rem 1rem;
    padding-right: 2.5rem;
    font-family: var(--font-sans);
    font-size: 0.95rem;
    color: var(--text-charcoal);
    outline: none;
    transition: all 0.2s;
  }

  .form-control:focus {
    background-color: var(--bg-paper-card);
    border-color: var(--whisky-gold-dark);
    box-shadow: 0 0 0 3px var(--whisky-gold-trans);
  }

  .search-icon-inside {
    position: absolute;
    right: 12px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 1rem;
    pointer-events: none;
  }

  .filter-buttons-row {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    flex: 2;
    justify-content: flex-end;
  }

  .filter-tab {
    background-color: var(--bg-sand);
    border: 1.5px solid var(--text-charcoal);
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.65rem 0.5rem;
    border-radius: 4px;
    cursor: pointer;
    text-align: center;
    transition: all 0.2s;
    color: var(--text-muted);
  }

  .filter-tab:hover {
    background-color: var(--bg-paper-card);
    color: var(--text-charcoal);
  }

  .filter-tab.active {
    background-color: var(--text-charcoal);
    color: var(--whisky-gold-light);
    border-color: var(--text-charcoal);
  }

  /* --- Directory Section Below --- */
  .directory-list-section {
    border-top: 2px solid var(--text-charcoal);
    padding-top: 3.5rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .directory-section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .directory-title {
    font-family: var(--font-display);
    font-size: 2.25rem;
    margin: 0;
  }

  .results-counter {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    background-color: var(--bg-sand);
    padding: 0.35rem 0.85rem;
    border-radius: 20px;
    border: 1px solid var(--border-color-dark);
  }

  .directory-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.75rem;
  }

  .directory-item-card {
    background-color: var(--bg-paper-card);
    border: 2px solid var(--text-charcoal);
    border-radius: 4px;
    box-shadow: 4px 4px 0px var(--text-charcoal);
    display: flex;
    flex-direction: column;
    min-height: 240px;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .directory-item-card:hover {
    transform: translateY(-4px);
    box-shadow: 6px 6px 0px var(--text-charcoal);
    border-color: var(--whisky-gold-dark);
  }

  .directory-item-card.selected-card {
    border-color: var(--whisky-gold-dark);
    background-color: rgba(197, 139, 57, 0.03);
    box-shadow: 6px 6px 0px var(--whisky-gold-dark);
  }

  .card-top-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1.5px solid var(--text-charcoal);
    padding: 0.85rem 1.25rem;
    background-color: var(--bg-sand);
  }

  .card-stand-number {
    font-family: var(--font-display);
    font-size: 1.25rem;
    color: var(--text-charcoal);
    line-height: 1;
  }

  .category-tag {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.2rem 0.5rem;
    border-radius: 20px;
  }

  .category-tag.tag-scotch { background-color: var(--whisky-gold-trans); color: var(--whisky-gold-dark); border: 1px solid var(--whisky-gold); }
  .category-tag.tag-dutch { background-color: rgba(44, 94, 67, 0.05); color: var(--success-green); border: 1px solid var(--success-green); }
  .category-tag.tag-independent { background-color: #E2ECF7; color: #2C5A94; border: 1px solid #A2C4EC; }
  .category-tag.tag-world { background-color: #F8E6E2; color: #9E3E2B; border: 1px solid #EFA79A; }
  .category-tag.tag-catering { background-color: #FFF3CD; color: #856404; border: 1px solid #FFEBAA; }
  .category-tag.tag-organisation { background-color: #E2E3E5; color: #383D41; border: 1px solid #D6D8DB; }

  .card-body-content {
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
    flex-grow: 1;
  }

  .card-body-content h4 {
    font-family: var(--font-serif);
    font-weight: 700;
    font-size: 1.15rem;
    color: var(--text-charcoal);
    text-transform: none;
    letter-spacing: 0;
  }

  .card-body-content p {
    font-size: 0.9rem;
    line-height: 1.5;
    color: var(--text-muted);
    margin: 0;
  }

  .card-brands-row {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    color: var(--text-charcoal);
    border-top: 1px dotted var(--border-color-dark);
    padding-top: 0.65rem;
    margin-top: auto;
  }

  .card-brands-row strong {
    font-weight: 800;
    font-size: 0.75rem;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .card-action-footer {
    border-top: 1px solid var(--border-color);
    padding: 0.85rem 1.25rem;
    display: flex;
    justify-content: flex-end;
  }

  .btn-locate-stand {
    background: transparent;
    border: none;
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--whisky-gold-dark);
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 4px;
    transition: color 0.2s;
  }

  .btn-locate-stand:hover {
    color: var(--text-charcoal);
  }

  /* --- Responsive Breakpoints --- */
  @media (max-width: 1200px) {
    .directory-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 1050px) {
    .map-detail-layout {
      grid-template-columns: 1fr;
    }
    
    .info-card-detail {
      height: auto;
      min-height: auto;
    }

    .horizontal-bar-inner {
      flex-direction: column;
      align-items: stretch;
      gap: 1.25rem;
      padding: 1.5rem;
    }
    
    .search-filter-horizontal-bar .search-input-wrapper {
      width: 100%;
    }
    
    .filter-buttons-row {
      justify-content: center;
    }
  }

  @media (max-width: 600px) {
    .standhouders-page-wrapper {
      padding: 7rem 1rem 4rem;
      gap: 2.5rem;
    }

    .page-title {
      font-size: 2.75rem;
    }

    .directory-grid {
      grid-template-columns: 1fr;
    }

    .filter-buttons-row {
      justify-content: center;
      gap: 6px;
    }
  }
</style>

<!-- CLIENT-SIDE LOGIC FOR SEARCH, FILTER, AND SVG HOVER & SELECTION -->
<script define:vars={{ exhibitors }}>
  // Initialize elements
  const searchInput = document.getElementById('exhibitor-search');
  const filterTabs = document.querySelectorAll('.filter-tab');
  const directoryGrid = document.getElementById('directory-grid');
  const directoryCards = document.querySelectorAll('.directory-item-card');
  const resultsCount = document.getElementById('results-count');
  
  // Detail card states
  const defaultInfoState = document.getElementById('default-info-state');
  const activeInfoState = document.getElementById('active-info-state');
  const detailNumber = document.getElementById('detail-number');
  const detailCategory = document.getElementById('detail-category-badge');
  const detailName = document.getElementById('detail-name');
  const detailDesc = document.getElementById('detail-description');
  const detailBrands = document.getElementById('detail-brands');

  // SVG Elements
  const svgMap = document.getElementById('church-map');
  const svgStands = document.querySelectorAll('.svg-stand');
  const zoomTarget = document.getElementById('svg-zoom-target');

  let activeFilter = 'all';
  let activeSearch = '';
  let selectedStandId = null;

  // Zoom variables
  let scale = 1;
  const zoomStep = 0.15;
  const maxScale = 2.5;
  const minScale = 0.7;

  // Map Category Mapping Helper
  const categoryLabels = {
    scotch: 'Schots',
    dutch: 'Nederlands',
    independent: 'Onafhankelijk',
    world: 'Wereld',
    catering: 'Catering',
    organisation: 'Organisatie'
  };

  // --- Zoom Controls ---
  document.getElementById('zoom-in').addEventListener('click', () => {
    if (scale < maxScale) {
      scale += zoomStep;
      applyZoom();
    }
  });

  document.getElementById('zoom-out').addEventListener('click', () => {
    if (scale > minScale) {
      scale -= zoomStep;
      applyZoom();
    }
  });

  document.getElementById('zoom-reset').addEventListener('click', () => {
    scale = 1;
    applyZoom();
  });

  function applyZoom() {
    zoomTarget.style.transform = `scale(${scale})`;
  }

  // --- Update Details Card ---
  function showStandDetails(exh) {
    if (!exh) {
      activeInfoState.style.display = 'none';
      defaultInfoState.style.display = 'flex';
      return;
    }

    // Set stand numbers & titles
    detailNumber.textContent = exh.isNotOnMap ? "Niet op plattegrond" : "Stand " + exh.id;
    detailName.textContent = exh.name;
    detailDesc.textContent = exh.description;
    
    // Category mapping
    detailCategory.textContent = categoryLabels[exh.category] || 'Overig';
    detailCategory.className = `status-badge category-tag tag-${exh.category}`;

    // Clear and fill brands
    detailBrands.innerHTML = '';
    exh.brands.forEach(brand => {
      const tag = document.createElement('span');
      tag.className = 'brand-tag';
      tag.textContent = brand;
      detailBrands.appendChild(tag);
    });

    defaultInfoState.style.display = 'none';
    activeInfoState.style.display = 'flex';
  }

  // --- Search & Filter Logic ---
  function filterAndSearch() {
    let count = 0;
    
    directoryCards.forEach(card => {
      const name = card.getAttribute('data-name');
      const brands = card.getAttribute('data-brands');
      const cat = card.getAttribute('data-cat');
      const matchesSearch = name.includes(activeSearch) || brands.includes(activeSearch);
      const matchesFilter = activeFilter === 'all' || cat === activeFilter;

      if (matchesSearch && matchesFilter) {
        card.style.display = 'flex';
        count++;
      } else {
        card.style.display = 'none';
      }
    });

    resultsCount.textContent = `${count} standhouders gevonden`;

    // Also update SVG map visuals (dim non-matching stands)
    svgStands.forEach(group => {
      const standId = group.getAttribute('data-stand');
      const standExhibitors = exhibitors.filter(e => e.id === standId);

      // Check if any exhibitor on this stand matches
      let matches = false;
      if (standExhibitors.length > 0) {
        matches = standExhibitors.some(e => {
          const nameMatches = e.name.toLowerCase().includes(activeSearch);
          const brandsMatches = e.brands.join(' ').toLowerCase().includes(activeSearch);
          const matchesSearch = nameMatches || brandsMatches;
          const matchesFilter = activeFilter === 'all' || e.category === activeFilter;
          return matchesSearch && matchesFilter;
        });
      }

      if (activeSearch !== '' || activeFilter !== 'all') {
        if (matches) {
          group.classList.remove('dimmed-stand');
          group.classList.add('highlighted-search-stand');
        } else {
          group.classList.add('dimmed-stand');
          group.classList.remove('highlighted-search-stand');
        }
      } else {
        group.classList.remove('dimmed-stand');
        group.classList.remove('highlighted-search-stand');
      }
    });
  }

  // Search Input Listener
  searchInput.addEventListener('input', (e) => {
    activeSearch = e.target.value.toLowerCase().trim();
    filterAndSearch();
  });

  // Category Tabs Listeners
  filterTabs.forEach(tab => {
    tab.addEventListener('click', () => {
      filterTabs.forEach(t => t.classList.remove('active'));
      tab.classList.add('active');
      activeFilter = tab.getAttribute('data-filter');
      filterAndSearch();
    });
  });

  // --- Selection Syncing ---
  function selectStand(standId) {
    selectedStandId = standId;

    // 1. Highlight in SVG
    svgStands.forEach(group => {
      if (group.getAttribute('data-stand') === standId) {
        group.classList.add('selected-stand');
      } else {
        group.classList.remove('selected-stand');
      }
    });

    // 2. Update Details Panel
    const selectedExhibitor = exhibitors.find(e => e.id === standId);
    showStandDetails(selectedExhibitor);

    // 3. Highlight in Directory List
    directoryCards.forEach(card => {
      if (card.getAttribute('data-id') === standId) {
        card.classList.add('selected-card');
      } else {
        card.classList.remove('selected-card');
      }
    });
  }

  // --- SVG click listeners ---
  svgStands.forEach(group => {
    group.addEventListener('click', () => {
      const standId = group.getAttribute('data-stand');
      selectStand(standId);
    });
  });

  // --- List card click & locate listeners ---
  directoryCards.forEach(card => {
    card.addEventListener('click', (e) => {
      // Don't trigger if clicking the button (handled separately)
      if (e.target.classList.contains('btn-locate-stand')) return;
      
      const id = card.getAttribute('data-id');
      selectStand(id);
    });
  });

  // Locate buttons scroll to SVG map and highlight
  const locateButtons = document.querySelectorAll('.btn-locate-stand');
  locateButtons.forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.stopPropagation();
      const targetId = btn.getAttribute('data-target');
      selectStand(targetId);
      
      // Scroll to SVG map
      document.querySelector('.map-visual-container').scrollIntoView({ behavior: 'smooth', block: 'center' });
    });
  });
</script>
