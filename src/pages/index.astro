---
// src/pages/index.astro
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import TicketStub from '../components/TicketStub.astro';
import { getTickets } from '../lib/ghl';
import LogoSvg from '../assets/Logo/Logo.svg?raw';
import GentStampWit from '../assets/LogosAll/logo-wit-gent.svg?raw';

const tickets = await getTickets('gent');
---

<Layout 
  title="Home - Premium Vintage-Modern Ontwerp" 
  description="Bezoek het meest sfeervolle en bekende whiskyfestival van Nederland in de De Oude Vismijn Gent. 2, 3 en 4 oktober 2026. Bestel hier direct uw entreekaarten via onze mock ticketshop."
>
  <!-- 0. HOMEPAGE 2.5-SECOND COIN-FLIP PRELOADER -->
  <div id="hp-preloader" class="hp-preloader-overlay">
    <div class="preloader-content">
      <!-- 3D Flipping Gold Coin Container -->
      <div class="coin-3d-stage">
        <div class="coin-flipper-core">
          <Fragment set:html={LogoSvg} />
        </div>
        <div class="coin-glow-shadow"></div>
      </div>

      <!-- Loading Text & Progress Bar -->
      <div class="preloader-text-group">
        <p class="preloader-title">GENTS WHISKY FESTIVAL</p>
        <div class="preloader-progress-track">
          <div class="preloader-progress-bar"></div>
        </div>
        <p class="preloader-subtitle">EST. 2004 &bull; GENT</p>
      </div>
    </div>
  </div>

  <!-- Preloader Safety Fallback (Guarantees complete 2.4s coin flip experience) -->
  <script is:inline>
    (function() {
      function dismissPreloader() {
        var pl = document.getElementById('hp-preloader');
        if (pl && !pl.classList.contains('fade-out')) {
          pl.classList.add('fade-out');
          setTimeout(function() {
            if (pl) pl.style.display = 'none';
          }, 400);
        }
      }
      // Safety fallback timer to guarantee dismiss after full coin toss animation finishes
      setTimeout(dismissPreloader, 2500);
    })();
  </script>

  <!-- 1. Navigation Header -->
  <Header />

  <main>
    <!-- 2. TT HERO SECTION (Left Text, Right Full-Height Video Banner with Gradient Fade) -->
    <section class="tt-hero-section" style="background: linear-gradient(135deg, #091326 0%, #1E3A8A 35%, #172554 65%, #0B1528 100%) !important; color: #FFFFFF !important;">
      <div class="hero-vintage-overlay"></div>

      <!-- Full-Height Right Side Video Banner -->
      <div class="hero-video-full-right">
        <video 
          id="hero-bg-video"
          class="hero-video-element"
          autoplay 
          muted 
          loop 
          playsinline
          preload="auto"
          poster="/aftermovie_poster.jpg"
        >
          <source src="/Video/hero_loop.mp4" type="video/mp4" />
          Uw browser ondersteunt geen HTML5 video.
        </video>

        <!-- Seamless Blend Overlays: Left Fade & Warm Vintage Vignette -->
        <div class="hero-video-blend-gradient"></div>
        <div class="hero-video-tint-overlay"></div>
      </div>

      <div class="section-container tt-hero-grid">
        
        <div class="tt-hero-text">
          <!-- Decorative barrel line-art -->
          <div class="hero-barrel-art" aria-hidden="true">
            <img src="/barrels line.svg" alt="" width="320" height="380" loading="eager" />
          </div>

          <h1 class="tt-hero-title">
            Hét weekend<br>
            waar elke whisky-<br>
            liefhebber naar uitkijkt.
          </h1>

          <div class="hero-date-line">
            <span class="date-text">2, 3 &amp; 4 Oktober 2026</span>
            <span class="date-sep">◆</span>
            <span class="loc-text">De Oude Vismijn Gent</span>
          </div>

          <p class="tt-hero-subtitle">
            Van unieke Belgische craft distillaten tot zeldzame Islay single malts in De Oude Vismijn. Drie dagen lang verandert Gent in de whiskyhoofdstad van België voor de 22e editie.
          </p>
          <div class="hero-actions" style="align-items: center;">
            <a href="#sessies" class="btn btn-beige">Bestel Tickets</a>
            <a href="/programma" class="tt-text-link" style="margin-left: 1rem;">Bekijk Programma</a>
          </div>

          <div class="tt-hero-reviews">
            <p class="reviews-title">Geprezen door duizenden liefhebbers!</p>
            <div class="reviews-grid">
              <div class="review-item">
                <span class="stars">★★★★★</span>
                <span class="review-source">4.9/5 op Trustpilot</span>
              </div>
            </div>

          </div>
        </div>

        <!-- Right Column: Glassware SVG Artwork (Middel 15.svg 1-op-1 overgenomen) -->
        <div class="tt-hero-visual-right" aria-hidden="true">
          <div class="hero-glass-art-card">
            <Fragment set:html={GentStampWit} />
          </div>
        </div>

      </div>
    </section>

                    <!-- 3. CONTINUOUS MOVING PARTNER & DISTILLERY MARQUEE SLIDER -->
    <section class="trust-marquee-section" aria-label="Aanwezige Partner Distilleerderijen">
      <div class="trust-marquee-wrapper">
        <div class="trust-marquee-track">
          
          <span class="trust-text-logo">The Macallan</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Highland Park</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Glenfiddich</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">The Balvenie</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Laphroaig</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Ardbeg</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Springbank</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Bowmore</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Teeling Whiskey</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">SMWS</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">House of Suntory</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Glengoyne</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Talisker</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Lagavulin</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Dalmore</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Arran Single Malt</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Oban</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Nc'nean</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          
          <span class="trust-text-logo">The Macallan</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Highland Park</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Glenfiddich</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">The Balvenie</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Laphroaig</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Ardbeg</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Springbank</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Bowmore</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Teeling Whiskey</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">SMWS</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">House of Suntory</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Glengoyne</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Talisker</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Lagavulin</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Dalmore</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Arran Single Malt</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Oban</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          <span class="trust-text-logo">Nc'nean</span>
          <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
        </div>
      </div>
    </section>

    <!-- 4. TORN PAPER TICKET SECTION -->
    <section id="sessies" class="tear-section">
      
      <!-- Section header on light paper -->
      <div class="tear-header-area">
        <div class="section-container">
          <div class="tt-section-header text-center">
            <h2 class="tt-section-title">Kies uw Sessie & Extras</h2>
            <p class="tt-section-subtitle">Selecteer uw favoriete dag. De capaciteit is gelimiteerd om een exclusieve, rustige proefervaring te garanderen.</p>
          </div>
        </div>
      </div>

      <!-- Top torn edge (light paper over dark) -->
      <div class="torn-edge-wrap">
        <img src="/torn-top.svg?v=2" alt="" class="torn-edge-img" />
      </div>

      <!-- Dark ticket area -->
      <div class="dark-gap" id="ticket-gap">
        <div class="section-container">
          
          <!-- Shop Filters -->
          <div class="shop-filters-container">
            <div class="filter-row">
              <span class="filter-label">Dag:</span>
              <div class="filter-buttons" id="day-filter-buttons">
                <button type="button" class="filter-tab-btn active" data-filter-day="all">Alle Dagen</button>
                <button type="button" class="filter-tab-btn" data-filter-day="vrijdag">Vrijdag 2 Okt</button>
                <button type="button" class="filter-tab-btn" data-filter-day="zaterdag">Zaterdag 3 Okt</button>
                <button type="button" class="filter-tab-btn" data-filter-day="zondag">Zondag 4 Okt</button>
              </div>
            </div>
            <div class="filter-row" style="margin-top: 0.5rem;">
              <span class="filter-label">Type:</span>
              <div class="filter-buttons" id="cat-filter-buttons">
                <button type="button" class="filter-tab-btn active" data-filter-cat="all">Alles</button>
                <button type="button" class="filter-tab-btn" data-filter-cat="entree">Sessie Entree</button>
                <button type="button" class="filter-tab-btn" data-filter-cat="warehouse">Dada Rondleiding</button>
                <button type="button" class="filter-tab-btn" data-filter-cat="masterclass">Masterclasses</button>
                <button type="button" class="filter-tab-btn" data-filter-cat="tram">Whisky Bootjes</button>
                <button type="button" class="filter-tab-btn" data-filter-cat="botteling">Botteling</button>
              </div>
            </div>
            <div class="filter-row" id="time-filter-row" style="margin-top: 0.5rem; display: flex;">
              <span class="filter-label">Tijd:</span>
              <div class="filter-buttons" id="time-filter-buttons">
                <button type="button" class="filter-tab-btn active" data-filter-time="all">Alles</button>
                <button type="button" class="filter-tab-btn" data-filter-time="ochtend">Ochtend (tot 13u)</button>
                <button type="button" class="filter-tab-btn" data-filter-time="middag">Middag (13u - 18u)</button>
                <button type="button" class="filter-tab-btn" data-filter-time="avond">Avond (vanaf 18u)</button>
              </div>
            </div>
          </div>

          <!-- Mobile 3-Stacked Custom Styled Dropdowns (< 990px) -->
          <div class="mobile-filter-dropdowns" id="mobile-ticket-filters">
            <!-- 1. Dag Filter -->
            <div class="custom-dropdown" id="dd-filter-day">
              <button type="button" class="custom-dd-btn" aria-haspopup="listbox" aria-expanded="false">
                <span class="custom-dd-label-group">
                  <span class="custom-dd-prefix">1. Dag:</span>
                  <span class="custom-dd-selected-text" id="dd-day-text">Alle Dagen</span>
                </span>
                <svg class="custom-dd-chevron" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"></polyline></svg>
              </button>
              <div class="custom-dd-menu" role="listbox">
                <div class="custom-dd-item active" data-value="all" role="option">
                  <span>Alle Festivaldagen (Vr, Za, Zo)</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="vrijdag" role="option">
                  <span>Vrijdag 2 Okt 2026</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="zaterdag" role="option">
                  <span>Zaterdag 3 Okt 2026</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="zondag" role="option">
                  <span>Zondag 4 Okt 2026</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
              </div>
            </div>

            <!-- 2. Type Filter -->
            <div class="custom-dropdown" id="dd-filter-cat">
              <button type="button" class="custom-dd-btn" aria-haspopup="listbox" aria-expanded="false">
                <span class="custom-dd-label-group">
                  <span class="custom-dd-prefix">2. Type:</span>
                  <span class="custom-dd-selected-text" id="dd-cat-text">Alle Tickets & Masterclasses</span>
                </span>
                <svg class="custom-dd-chevron" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"></polyline></svg>
              </button>
              <div class="custom-dd-menu" role="listbox">
                <div class="custom-dd-item active" data-value="all" role="option">
                  <span>Alle Types (Entree, Masterclasses, etc.)</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="entree" role="option">
                  <span>Sessie Entree</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="warehouse" role="option">
                  <span>Dada Rondleiding</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="masterclass" role="option">
                  <span>Masterclasses</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="tram" role="option">
                  <span>Whisky Bootjes</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="botteling" role="option">
                  <span>Botteling</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
              </div>
            </div>

            <!-- 3. Tijd Filter -->
            <div class="custom-dropdown" id="dd-filter-time">
              <button type="button" class="custom-dd-btn" aria-haspopup="listbox" aria-expanded="false">
                <span class="custom-dd-label-group">
                  <span class="custom-dd-prefix">3. Tijd:</span>
                  <span class="custom-dd-selected-text" id="dd-time-text">Alle Tijdsloten</span>
                </span>
                <svg class="custom-dd-chevron" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"></polyline></svg>
              </button>
              <div class="custom-dd-menu" role="listbox">
                <div class="custom-dd-item active" data-value="all" role="option">
                  <span>Alle Tijdsloten (Ochtend, Middag, Avond)</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="ochtend" role="option">
                  <span>Ochtend (tot 13:00 uur)</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="middag" role="option">
                  <span>Middag (13:00 – 18:00 uur)</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
                <div class="custom-dd-item" data-value="avond" role="option">
                  <span>Avond (vanaf 18:00 uur)</span>
                  <svg class="check-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#1E3A8A" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg>
                </div>
              </div>
            </div>
          </div>

          <div class="shop-grid">
            <!-- Left side: Organized Ticket List -->
            <div class="tickets-list fade-in-group" id="shop-tickets-list">
              {tickets.map((ticket) => (
                <TicketStub
                  id={ticket.id}
                  title={ticket.title}
                  price={ticket.price}
                  date={ticket.date}
                  time={ticket.time}
                  category={ticket.category}
                  daypart={ticket.daypart}
                  location={ticket.location}
                  bookingType={ticket.bookingType}
                  status={ticket.status}
                  statusText={ticket.statusText}
                  isSoldOut={ticket.isSoldOut}
                  extraMeta={ticket.extra}
                  included={ticket.category === 'entree' ? (
                    ticket.title.toLowerCase().includes('vip') 
                      ? ["12 gratis drams (€12)", "Glencairn tastingglas", "Hapjes-vouchers & Boekje", "VIP masterclasses"]
                      : ["Glencairn tastingglas", "Talloze gratis drams", "Festivalboekje", "Toegang 4 uur"]
                  ) : undefined}
                />
              ))}

              <!-- Show More Tickets Button -->
              <div id="show-more-container" style="display: none; text-align: center; margin-top: 3rem; margin-bottom: 2rem; width: 100%;">
                <button type="button" id="show-more-btn" style="padding: 1rem 3rem; font-family: var(--font-sans); font-weight: 800; font-size: 0.95rem; text-transform: uppercase; letter-spacing: 0.05em; border-radius: 8px; border: 2px solid var(--whisky-gold); background-color: var(--whisky-gold); color: #1c1917; cursor: pointer; transition: all 0.2s ease; box-shadow: 0 4px 15px rgba(0, 0, 0, 0.25); display: inline-flex; align-items: center; gap: 0.5rem; justify-content: center;">
                  Toon alle tickets (0 extra) ↓
                </button>
              </div>

              <style>
                #show-more-btn:hover {
                  background-color: var(--whisky-gold-light) !important;
                  border-color: var(--whisky-gold-light) !important;
                  transform: translateY(-2px);
                  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3) !important;
                }
                #show-more-btn:active {
                  transform: translateY(1px);
                  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2) !important;
                }
              
  /* ==========================================
     UNIVERSAL MODAL OVERLAY & POPUP STYLING
     ========================================== */
  .modal-overlay {
    position: fixed !important;
    inset: 0 !important;
    top: 0 !important;
    left: 0 !important;
    width: 100% !important;
    height: 100vh !important;
    background-color: rgba(15, 14, 12, 0.78) !important;
    backdrop-filter: blur(8px) !important;
    -webkit-backdrop-filter: blur(8px) !important;
    z-index: 999999 !important;
    display: none;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    overflow-y: auto;
    box-sizing: border-box;
  }

  .vintage-modal-card {
    background-color: #FAF7F2 !important;
    border: 1.5px solid #D8CFC0 !important;
    border-radius: 12px !important;
    width: 100% !important;
    max-width: 680px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5) !important;
    position: relative;
    overflow: hidden;
    margin: auto;
    color: #171614 !important;
    animation: modalFadeIn 0.25s cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes modalFadeIn {
    from {
      opacity: 0;
      transform: scale(0.95) translateY(10px);
    }
    to {
      opacity: 1;
      transform: scale(1) translateY(0);
    }
  }

  .modal-header {
    padding: 1.5rem 2rem 1.25rem;
    background-color: #F4EFE6;
    border-bottom: 1px solid #E6DEC8;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    position: relative;
  }

  .modal-step-tag {
    font-size: 0.68rem;
    font-weight: 800;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--whisky-gold-dark, #8b5a1b);
    display: block;
    margin-bottom: 0.25rem;
  }

  .modal-title {
    font-family: var(--font-display, serif);
    font-size: 1.6rem;
    color: #171614;
    margin: 0;
    line-height: 1.2;
  }

  .modal-subtitle {
    font-size: 0.88rem;
    color: #63584E;
    margin: 0.25rem 0 0;
  }

  .modal-close-btn {
    background: none;
    border: none;
    font-size: 1.75rem;
    line-height: 1;
    color: #8C827A;
    cursor: pointer;
    padding: 0.25rem;
    border-radius: 4px;
    transition: color 0.2s;
  }

  .modal-close-btn:hover {
    color: #171614;
  }


  /* Timetable Type Filter Buttons */
  .timetable-type-filter {
    background: rgba(197, 139, 57, 0.08);
    padding: 0.35rem 0.75rem;
    border-radius: 30px;
    border: 1px solid rgba(140, 110, 70, 0.25);
  }
  .tt-type-btn {
    border: none;
    background: transparent;
    padding: 0.4rem 0.85rem;
    font-family: var(--font-sans);
    font-size: 0.78rem;
    font-weight: 700;
    color: var(--text-charcoal);
    border-radius: 20px;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .tt-type-btn.active, .tt-type-btn:hover {
    background-color: var(--whisky-gold-dark, #1E3A8A);
    color: #FAF7F2;
  }
  .tt-card-info-btn:hover {
    color: var(--text-charcoal, #171614) !important;
    text-decoration: underline !important;
  }

  /* Compact, Elegant Timetable Boeken Button */
  .tt-card-action-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 1rem;
    width: 100%;
    gap: 0.5rem;
  }
  .tt-book-btn {
    height: 32px !important;
    min-height: 32px !important;
    max-height: 32px !important;
    padding: 0 0.85rem !important;
    font-family: var(--font-sans, sans-serif) !important;
    font-size: 0.76rem !important;
    font-weight: 700 !important;
    letter-spacing: 0.04em !important;
    text-transform: uppercase !important;
    border-radius: 4px !important;
    border: 1.5px solid var(--text-charcoal, #171614) !important;
    box-shadow: 2px 2px 0px rgba(23, 22, 20, 0.75) !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    white-space: nowrap !important;
    cursor: pointer !important;
    transition: all 0.15s ease !important;
  }
  .tt-book-btn:hover {
    transform: translateY(-1px) !important;
    box-shadow: 2px 3px 0px rgba(23, 22, 20, 0.9) !important;
  }
  .tt-book-btn:active {
    transform: translate(1px, 1px) !important;
    box-shadow: 1px 1px 0px rgba(23, 22, 20, 0.9) !important;
  }


  /* Strict Global SVG Bounds */
  :global(.hero-glass-art-card svg) {
    width: 100% !important;
    max-width: 320px !important;
    height: auto !important;
    max-height: 420px !important;
    display: block !important;
    margin: 0 auto !important;
  }
  :global(.coin-flipper-core svg) {
    width: 100% !important;
    max-width: 120px !important;
    height: auto !important;
    display: block !important;
    margin: 0 auto !important;
  }
  :global(.freestanding-svg-wrapper svg) {
    width: 100% !important;
    max-width: 140px !important;
    height: 100% !important;
    max-height: 190px !important;
    display: block !important;
    margin: 0 auto !important;
  }
  .hero-glass-art-card {
    width: 100% !important;
    max-width: 320px !important;
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
  }

  /* ====================================================
     PREMIUM INFINITE PARTNER & DISTILLERY MARQUEE SLIDER
     ==================================================== */
  .trust-marquee-section {
    background-color: var(--bg-sand, #FAF7F2) !important;
    border-top: 1.5px solid var(--border-color, #E6DDD0) !important;
    border-bottom: 1.5px solid var(--border-color, #E6DDD0) !important;
    padding: 1.6rem 0 !important;
    overflow: hidden !important;
    position: relative !important;
    width: 100% !important;
    box-sizing: border-box !important;
    mask-image: linear-gradient(to right, transparent 0%, black 6%, black 94%, transparent 100%);
    -webkit-mask-image: linear-gradient(to right, transparent 0%, black 6%, black 94%, transparent 100%);
    z-index: 10;
  }

  .trust-marquee-wrapper {
    width: 100% !important;
    overflow: hidden !important;
    display: flex !important;
  }

  .trust-marquee-track {
    display: flex !important;
    align-items: center !important;
    gap: 3.5rem !important;
    width: max-content !important;
    white-space: nowrap !important;
    animation: partnerMarqueeScroll 35s linear infinite !important;
    -webkit-animation: partnerMarqueeScroll 35s linear infinite !important;
    will-change: transform !important;
  }

  .trust-marquee-section:hover .trust-marquee-track {
    animation-play-state: paused !important;
    -webkit-animation-play-state: paused !important;
  }

  @keyframes partnerMarqueeScroll {
    0% {
      transform: translate3d(0, 0, 0);
    }
    100% {
      transform: translate3d(-50%, 0, 0);
    }
  }

  @-webkit-keyframes partnerMarqueeScroll {
    0% {
      -webkit-transform: translate3d(0, 0, 0);
    }
    100% {
      -webkit-transform: translate3d(-50%, 0, 0);
    }
  }

  .trust-text-logo {
    font-family: var(--font-display, serif) !important;
    font-size: 1.45rem !important;
    color: var(--text-charcoal, #171614) !important;
    opacity: 0.65 !important;
    letter-spacing: 0.12em !important;
    text-transform: uppercase !important;
    transition: all 0.25s ease !important;
    cursor: default !important;
    user-select: none !important;
    display: inline-flex !important;
    align-items: center !important;
    flex-shrink: 0 !important;
  }

  .trust-text-logo:hover {
    opacity: 1 !important;
    color: #1E3A8A !important;
    transform: scale(1.08) !important;
  }

  .partner-sep {
    color: #caac8e !important;
    font-size: 0.85rem !important;
    opacity: 0.8 !important;
    user-select: none !important;
    flex-shrink: 0 !important;
  }

  /* ----------------------------------------------------
     FAQ ACCORDION SECTION
     ---------------------------------------------------- */
  .faq-accordion-section {
    padding: 6.5rem 1.5rem;
    background-color: var(--bg-parchment);
    position: relative;
    border-top: 2px solid var(--border-color);
  }

  .faq-header-block {
    max-width: 720px;
    margin: 0 auto 3.5rem;
  }

  .faq-main-title {
    font-family: var(--font-display);
    font-size: clamp(1.85rem, 3.2vw, 2.45rem);
    line-height: 1.15;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--text-charcoal);
    margin: 0.5rem 0 1rem;
  }

  .faq-subtitle {
    font-family: var(--font-sans);
    font-size: 1.02rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .faq-accordion-list {
    max-width: 880px;
    width: 100%;
    margin: 0 auto 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .faq-col-title {
    font-family: var(--font-sans);
    font-size: 1.15rem;
    font-weight: 800;
    color: var(--text-charcoal);
    margin-bottom: 1.25rem;
    padding-bottom: 0.65rem;
    border-bottom: 2px solid var(--border-color);
    letter-spacing: -0.01em;
  }

  .faq-column {
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }

  .faq-item {
    background: #FFFFFF;
    border: 1.5px solid var(--border-color);
    border-radius: 6px;
    overflow: hidden;
    transition: all 0.2s ease;
    box-shadow: 2px 2px 0px rgba(29, 28, 26, 0.05);
  }

  .faq-item[open] {
    border-color: var(--text-charcoal);
    box-shadow: 4px 4px 0px var(--text-charcoal);
  }

  .faq-summary {
    padding: 1.15rem 1.25rem;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    list-style: none;
    user-select: none;
  }

  .faq-summary::-webkit-details-marker {
    display: none;
  }

  .faq-q-text {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 800;
    color: var(--text-charcoal);
    line-height: 1.35;
  }

  .faq-icon-cross {
    font-family: var(--font-sans);
    font-size: 1.3rem;
    font-weight: 800;
    color: var(--whisky-gold-dark, #8b5a1b);
    transition: transform 0.25s ease;
    line-height: 1;
  }

  .faq-item[open] .faq-icon-cross {
    transform: rotate(45deg);
  }

  .faq-answer {
    padding: 0 1.25rem 1.25rem;
    border-top: 1px dashed var(--border-color);
    margin-top: 0.25rem;
    padding-top: 1rem;
  }

  .faq-answer p {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin: 0;
  }

  .faq-contact-card {
    max-width: 880px;
    width: 100%;
    margin: 0 auto;
    background: #1E3A8A;
    color: #FFFFFF;
    padding: 2.25rem 2.5rem;
    border-radius: 8px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
    border: 2px solid var(--text-charcoal);
    box-shadow: 4px 4px 0px var(--text-charcoal);
  }

  .fcc-title {
    font-family: var(--font-sans);
    font-size: 1.25rem;
    font-weight: 800;
    color: #FFFFFF;
    margin-bottom: 0.35rem;
  }

  .fcc-desc {
    font-family: var(--font-sans);
    font-size: 0.92rem;
    color: rgba(255, 255, 255, 0.85);
    margin: 0;
  }

  .fcc-btn {
    font-size: 0.85rem;
    white-space: nowrap;
    padding: 0.75rem 1.6rem;
  }

  @media (max-width: 992px) {
    .faq-accordion-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .faq-contact-card {
      flex-direction: column;
      text-align: center;
      padding: 2rem 1.5rem;
    }
  }
</style>

            </div>

            <!-- Right side: Realtime Receipt & Checkout Form -->
            <div class="checkout-sidebar-container">
              
              <div class="ticket-receipt-card">
                <span class="receipt-header">Winkelwagen</span>
                
                <div id="cart-items" class="cart-items-list">
                  <p class="empty-cart-msg">Selecteer hiernaast uw ticket(s) om de bestelling te starten.</p>
                </div>



                <div class="receipt-perforation"></div>

                <div class="receipt-calculation">
                  <div class="calc-row">
                    <span class="calc-label">Subtotaal:</span>
                    <span class="calc-val" id="receipt-subtotal">&euro;0,00</span>
                  </div>
                  <div class="calc-row">
                    <span class="calc-label">Administratiekosten:</span>
                    <span class="calc-val" id="receipt-fee">&euro;0,00</span>
                  </div>
                  <div class="calc-row" id="shipping-row" style="display: none;">
                    <span class="calc-label">Verzendkosten Fles:</span>
                    <span class="calc-val" id="receipt-shipping">&euro;0,00</span>
                  </div>
                  <div class="calc-row total-row">
                    <span class="calc-label">Totaal:</span>
                    <span class="calc-val" id="receipt-total">&euro;0,00</span>
                  </div>
                </div>

                <button type="button" class="btn btn-gold btn-checkout" id="checkout-trigger-btn" disabled>
                  Afrekenen &rarr;
                </button>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Bottom torn edge (light paper over dark) -->
      <div class="torn-edge-wrap torn-edge-bottom">
        <img src="/torn-bottom.svg?v=2" alt="" class="torn-edge-img" />
      </div>

    </section>

    <!-- 5. FESTIVAL BELEVING HEADER BANNER -->
    <section class="banner-drams">
      <div class="section-container text-center" style="max-width: 860px; margin: 0 auto;">
        <span class="tt-tag" style="margin-bottom: 0.75rem; display: inline-block;">HET FESTIVAL &amp; DE BELEVING</span>
        <h2>Een Onvergetelijke Whiskybeleving in het Hart van Gent</h2>
        <div class="vintage-line"></div>
        <p style="font-size: 1.15rem; color: var(--text-charcoal); opacity: 0.8; margin-bottom: 2rem; line-height: 1.6;">
          Van toegang tot honderden whisk(e)y's in De Oude Vismijn en intieme masterclasses tot een varende proeverij in de Gentse Whisky Bootjes. Ontdek wat het Gents Whisky Festival al 22 jaar zo uniek maakt.
        </p>
        <a href="#sessies" class="btn btn-gold" style="font-size: 0.95rem; padding: 0.9rem 2.25rem;">Kies uw Sessie &amp; Tickets</a>
      </div>
    </section>

    <!-- 6. PRAKTISCHE INFORMATIE SECTIES (3 PIJLERS) -->
    <section id="ervaring" class="split-features-section">
      <div class="section-container">
        
        <!-- Pijler 1: Type Tickets & Inbegrepen -->
        <div class="split-layout">
          <div class="split-text">
            <span class="tt-tag">Toegang &amp; Sessies</span>
            <h3 class="tt-split-title">Sessietickets: Wat zit er Inbegrepen?</h3>
            <p class="tt-split-desc">Het festival is opgedeeld in overzichtelijke dagdelen (Vrijdagavond, Zaterdagmiddag, Zaterdagavond en Zondagmiddag) om een ontspannen proefervaring te garanderen. Bij elk sessieticket ontvangt u bij binnenkomst alles wat u nodig heeft voor een complete festivaldag.</p>
            <ul class="tt-feature-list">
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Inclusief officieel Glencairn proefglas</li>
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Inclusief festivalboekje met alle 30+ stands &amp; proefnotities</li>
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Toegang tot honderden whisk(e)y's in de De Oude Vismijn</li>
            </ul>
            <a href="#sessies" class="tt-text-link" style="margin-top: 0.5rem; display: inline-block; color: var(--whisky-gold-dark, #1E3A8A); font-weight: 700;">Bekijk alle sessies &amp; tickets &rarr;</a>
          </div>
          <div class="split-visual tilt-slight-right">
            <div class="visual-placeholder vintage-frame">
              <div class="vintage-frame-inner text-center" style="padding: 0; background: var(--bg-paper-card); overflow: hidden;">
                <img src="/Afbeeldingen/whisky-festival-2025-0032.jpg" alt="Sessietickets De Oude Vismijn Gent" style="width: 100%; height: 320px; object-fit: cover; display: block;" />
              </div>
            </div>
          </div>
        </div>

        <!-- Pijler 2: Het Drams-Systeem & Proeven -->
        <div class="split-layout reverse">
          <div class="split-text">
            <span class="tt-tag">Proeven &amp; Betalen</span>
            <h3 class="tt-split-title">Het Drams-Systeem: Honderden te proeven Whisk(e)y's</h3>
            <p class="tt-split-desc">Bij vrijwel alle standhouders op de beursvloer proeft u standaard één of meerdere whisk(e)y's gratis. Voor oudere, zeldzame of gelimiteerde whisk(e)y's vragen importeurs een kleine bijdrage in 'drams' (ons festivalgeld: 1 dram = 1 euro). Drams koopt u eenvoudig bij de kassa's en zijn na afloop inwisselbaar.</p>
            <ul class="tt-feature-list">
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Honderden kwaliteitswhisky's van bekende merken te ontdekken</li>
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Exclusieve single casks proeven voor enkele drams</li>
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Drams zijn na afloop aan de kassa inwisselbaar</li>
            </ul>
            <a href="#sessies" class="tt-text-link" style="margin-top: 0.5rem; display: inline-block; color: var(--whisky-gold-dark, #1E3A8A); font-weight: 700;">Bestel uw tickets direct online &rarr;</a>
          </div>
          <div class="split-visual tilt-slight-left">
            <div class="visual-placeholder vintage-frame">
              <div class="vintage-frame-inner text-center" style="padding: 0; background: var(--bg-paper-card); overflow: hidden;">
                <img src="/Afbeeldingen/whisky-festival-2025-0038.jpg" alt="Het Drams Systeem Proeverij" style="width: 100%; height: 320px; object-fit: cover; display: block;" />
              </div>
            </div>
          </div>
        </div>

        <!-- Pijler 3: Masterclasses & De Whisky Bootjes -->
        <div class="split-layout">
          <div class="split-text">
            <span class="tt-tag">Verdieping &amp; Beleving</span>
            <h3 class="tt-split-title">Masterclasses, Whisky Bootjes of een rondleiding</h3>
            <p class="tt-split-desc">Wilt u uw kennis verdiepen of genieten van een varende proeverij? Volg een intieme masterclass in een aparte ruimte met alle geheimen van dat specifieke merk of geniet van de speciale festival botteling.</p>
            <ul class="tt-feature-list">
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Masterclasses: 4 tot 6 whisky's</li>
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Eenvoudig bij te boeken bij uw sessie-entreeticket</li>
              <li><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-2px; margin-right:6px;"><polyline points="20 6 9 17 4 12"></polyline></svg>Directe ontmoetingen met importeurs en meesterstokers</li>
            </ul>
            <a href="/programma" class="tt-text-link" style="margin-top: 0.5rem; display: inline-block; color: var(--whisky-gold-dark, #1E3A8A); font-weight: 700;">Bekijk het programma &amp; masterclasses &rarr;</a>
          </div>
          <div class="split-visual tilt-slight-right">
            <div class="visual-placeholder vintage-frame">
              <div class="vintage-frame-inner text-center" style="padding: 0; background: var(--bg-paper-card); overflow: hidden;">
                <img src="/Afbeeldingen/whisky-festival-2025-0042.jpg" alt="Masterclasses en Whisky Bootjes" style="width: 100%; height: 320px; object-fit: cover; display: block;" />
              </div>
            </div>
          </div>
        </div>

      </div>
    </section>

        <!-- 7B. HORIZONTELE FOTO MARQUEE SECTIE (2 Rijen Continu Lopende Carousel) -->
    <section class="horizontal-photo-gallery-section" id="het-festival-in-beeld">
      <div class="section-container">
        
        <!-- Section Header -->
        <div class="gallery-header-row text-center" style="margin-bottom: 2rem; text-align: center;">
          <span class="section-tag">SFEER &amp; HERINNERINGEN</span>
          <h2 class="section-title">Het Festival in Beeld</h2>
          <div class="vintage-line" style="margin: 0.5rem auto 1rem;"></div>
          <p class="section-subtitle" style="margin: 0 auto; max-width: 680px;">
            Een sfeerimpressie van 22 jaar International Whisky Festival Gent in de adembenemende De Oude Vismijn.
          </p>
        </div>

      </div>

      <!-- 2-Row Infinite Marquee Container -->
      <div class="horizontal-marquee-container">
        
        <!-- ROW 1: Scrolls Left continuously -->
        <div class="marquee-row-wrapper">
          <div class="marquee-row marquee-row-left">
            <!-- Set A -->
            <div class="marquee-photo-card rotate-neg-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0060.jpg" alt="De Oude Vismijn Sfeer" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0064.jpg" alt="Exclusieve Drams &amp; Glazen" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0068.jpg" alt="Masterclass in het De Oude Vismijn" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0072.jpg" alt="Inschenken van een Dram" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0081.jpg" alt="Bezoekers Genieten Samen" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0086.jpg" alt="Brand Ambassador Uitleg" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0089.jpg" alt="Gezelligheid bij de Stands" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0102.jpg" alt="Gotische Gewelven" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0114.jpg" alt="Proeven aan de Bar" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0121.jpg" alt="Drams Selectie" loading="lazy" />
              </div>
            </div>

            <!-- Set B (Duplicate for seamless infinite scroll) -->
            <div class="marquee-photo-card rotate-neg-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0136.jpg" alt="De Oude Vismijn Sfeer" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0155.jpg" alt="Exclusieve Drams &amp; Glazen" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0159.jpg" alt="Masterclass in het De Oude Vismijn" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0176.jpg" alt="Inschenken van een Dram" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0177.jpg" alt="Bezoekers Genieten Samen" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0032.jpg" alt="Brand Ambassador Uitleg" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0038.jpg" alt="Gezelligheid bij de Stands" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0042.jpg" alt="Gotische Gewelven" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0053.jpg" alt="Proeven aan de Bar" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0060.jpg" alt="Drams Selectie" loading="lazy" />
              </div>
            </div>
          </div>
        </div>

        <!-- ROW 2: Scrolls Right continuously -->
        <div class="marquee-row-wrapper">
          <div class="marquee-row marquee-row-right">
            <!-- Set A -->
            <div class="marquee-photo-card rotate-pos-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0064.jpg" alt="Schotse Traditie" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0068.jpg" alt="Proeftafels &amp; Stands" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0072.jpg" alt="Intieme Proeverij" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0081.jpg" alt="Proefnotities Maken" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0086.jpg" alt="Beursvloer Beleving" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0089.jpg" alt="Festivalslijterij" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0102.jpg" alt="Glencairn Proefglas" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0114.jpg" alt="Brand Presentatie" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0121.jpg" alt="Vrienden Proeven Samen" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0136.jpg" alt="Vintage Single Malts" loading="lazy" />
              </div>
            </div>

            <!-- Set B (Duplicate for seamless infinite scroll) -->
            <div class="marquee-photo-card rotate-pos-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0155.jpg" alt="Schotse Traditie" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0159.jpg" alt="Proeftafels &amp; Stands" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0176.jpg" alt="Intieme Proeverij" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0177.jpg" alt="Proefnotities Maken" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0032.jpg" alt="Beursvloer Beleving" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0038.jpg" alt="Festivalslijterij" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0042.jpg" alt="Glencairn Proefglas" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0053.jpg" alt="Brand Presentatie" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-pos-1" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0060.jpg" alt="Vrienden Proeven Samen" loading="lazy" />
              </div>
            </div>
            <div class="marquee-photo-card rotate-neg-2" aria-hidden="true">
              <div class="photo-mask-frame">
                <img src="/Afbeeldingen/whisky-festival-2025-0064.jpg" alt="Vintage Single Malts" loading="lazy" />
              </div>
            </div>
          </div>
        </div>

      </div>
    </section>

    <!-- VINTAGE HORIZONTAL TIMELINE -->
    <section class="timeline-scroll-section" id="historie">
      <div class="timeline-sticky-viewport">
        <div class="timeline-container">
          <!-- Left Side Editorial Header -->
          <div class="timeline-left-header">
            <span class="tt-tag">Sinds 2000</span>
            <h2 class="timeline-title">Het Ontstaan van het Festival</h2>
            <div class="vintage-line" style="margin: 1.5rem 0;"></div>
            <p class="timeline-intro-text">
              Ontdek hoe een passie voor Schotse single malts uitgroeide tot het meest sfeervolle en toonaangevende whisky-evenement van Nederland.
            </p>
            <div class="timeline-era-list" aria-hidden="true">
              <div class="era-item active" data-target="0">
                <span class="era-dot"></span>
                <span class="era-text">2000 — Het Eerste Festival</span>
              </div>
              <div class="era-item" data-target="1">
                <span class="era-dot"></span>
                <span class="era-text">2001 — De De Oude Vismijn</span>
              </div>
              <div class="era-item" data-target="7">
                <span class="era-dot"></span>
                <span class="era-text">2011 — De Whisky Tram</span>
              </div>
              <div class="era-item" data-target="12">
                <span class="era-dot"></span>
                <span class="era-text">2022 — Toren-Tasting</span>
              </div>
              <div class="era-item" data-target="15">
                <span class="era-dot"></span>
                <span class="era-text">2026 — 25e Editie</span>
              </div>
            </div>
          </div>

          <!-- Right Side Scrolling Track -->
          <div class="timeline-track-viewport">
            <div class="timeline-track" id="timeline-track">
              <!-- Card 1: 2000 -->
              <div class="timeline-card active" data-index="0">
                <div class="card-year-row">
                  <span class="card-year">2000</span>
                  <span class="card-num">01</span>
                </div>
                <h3 class="card-title">Het Eerste Whisky Festival</h3>
                <p class="card-desc">Het eerste Whisky Festival in Nederland /Europa en naar later bleek 1 van de 3 whisky festivals ter wereld. De andere whisky festivals dat jaar waren San Francisco en Tokyo. De eerst genoemde bestaat niet meer maar Tokyo is er nog steeds onder de naam Whisky Live Japan.</p>
                <div class="card-img-frame">
                  <img src="/timeline_2000_origin.png" alt="Het Eerste Whisky Festival 2000" loading="lazy" />
                </div>
              </div>

              <!-- Card 2: 2001 -->
              <div class="timeline-card" data-index="1">
                <div class="card-year-row">
                  <span class="card-year">2001</span>
                  <span class="card-num">02</span>
                </div>
                <h3 class="card-title">Verhuizing De Oude Vismijn & IMWS</h3>
                <p class="card-desc">Verhuizing naar De Oude Vismijn Gent. Oprichting International Malt Whisky Society. Onder Stichting Celtische Cultuur.</p>
                <div class="card-img-frame">
                  <img src="/timeline_2005_church.png" alt="De Oude Vismijn Gent" loading="lazy" />
                </div>
              </div>

              <!-- Card 3: 2002 -->
              <div class="timeline-card" data-index="2">
                <div class="card-year-row">
                  <span class="card-year">2002</span>
                  <span class="card-num">03</span>
                </div>
                <h3 class="card-title">Introductie Dram</h3>
                <p class="card-desc">Introductie Dram als betaalmiddel !</p>
                <div class="card-img-frame">
                  <img src="/timeline_2012_drams.png" alt="Dram als betaalmiddel" loading="lazy" />
                </div>
              </div>

              <!-- Card 4: 2003 -->
              <div class="timeline-card" data-index="3">
                <div class="card-year-row">
                  <span class="card-year">2003</span>
                  <span class="card-num">04</span>
                </div>
                <h3 class="card-title">Naamwijziging & Leiden</h3>
                <p class="card-desc">De naam Malt verdwijnt omdat er steeds meer andere whisky’s worden gepresenteerd. Verhuizing naar Pieterskerk in Leiden.</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0068.jpg" alt="Pieterskerk Leiden" loading="lazy" />
                </div>
              </div>

              <!-- Card 5: 2004 -->
              <div class="timeline-card" data-index="4">
                <div class="card-year-row">
                  <span class="card-year">2004</span>
                  <span class="card-num">05</span>
                </div>
                <h3 class="card-title">Gent & Masterclasses</h3>
                <p class="card-desc">Oprichting Gents Whisky Festival in Gent. Introductie van Masterclasses in Gent.</p>
                <div class="card-img-frame">
                  <img src="/timeline_2023_masterclass.png" alt="Gent & Masterclasses" loading="lazy" />
                </div>
              </div>

              <!-- Card 6: 2006 -->
              <div class="timeline-card" data-index="5">
                <div class="card-year-row">
                  <span class="card-year">2006</span>
                  <span class="card-num">06</span>
                </div>
                <h3 class="card-title">Vaksessie Slijters & Horeca</h3>
                <p class="card-desc">Introductie van een vaksessie voor slijters en horeca op zondag voorafgaand aan openstelling publiek. (doen we een aantal jaren, maar belangstelling te klein, dus wordt weer opgeheven)</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0072.jpg" alt="Vaksessie" loading="lazy" />
                </div>
              </div>

              <!-- Card 7: 2009 -->
              <div class="timeline-card" data-index="6">
                <div class="card-year-row">
                  <span class="card-year">2009</span>
                  <span class="card-num">07</span>
                </div>
                <h3 class="card-title">Whisky Awards</h3>
                <p class="card-desc">Introductie Whisky Awards</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0081.jpg" alt="Whisky Awards" loading="lazy" />
                </div>
              </div>

              <!-- Card 8: 2011 -->
              <div class="timeline-card" data-index="7">
                <div class="card-year-row">
                  <span class="card-year">2011</span>
                  <span class="card-num">08</span>
                </div>
                <h3 class="card-title">Terug in Gent & Whisky Tram</h3>
                <p class="card-desc">Terug naar de De Oude Vismijn in Gent (hier zitten we nog steeds). Introductie: Whisky Tram (Dram in de Tram).</p>
                <div class="card-img-frame">
                  <img src="/timeline_2018_tram.png" alt="Whisky Tram" loading="lazy" />
                </div>
              </div>

              <!-- Card 9: 2015 -->
              <div class="timeline-card" data-index="8">
                <div class="card-year-row">
                  <span class="card-year">2015</span>
                  <span class="card-num">09</span>
                </div>
                <h3 class="card-title">Introductie Slijterij</h3>
                <p class="card-desc">Introductie Slijterij</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0086.jpg" alt="Introductie Slijterij" loading="lazy" />
                </div>
              </div>

              <!-- Card 10: 2017 -->
              <div class="timeline-card" data-index="9">
                <div class="card-year-row">
                  <span class="card-year">2017</span>
                  <span class="card-num">10</span>
                </div>
                <h3 class="card-title">Tasting For Women Only</h3>
                <p class="card-desc">Eerste Whisky Tasting For Women Only</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0089.jpg" alt="Tasting For Women Only" loading="lazy" />
                </div>
              </div>

              <!-- Card 11: 2020 -->
              <div class="timeline-card" data-index="10">
                <div class="card-year-row">
                  <span class="card-year">2020</span>
                  <span class="card-num">11</span>
                </div>
                <h3 class="card-title">Geannuleerd wegens Covid</h3>
                <p class="card-desc">Geannuleerd wegens Covid</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0102.jpg" alt="Geannuleerd wegens Covid" loading="lazy" />
                </div>
              </div>

              <!-- Card 12: 2021 -->
              <div class="timeline-card" data-index="11">
                <div class="card-year-row">
                  <span class="card-year">2021</span>
                  <span class="card-num">12</span>
                </div>
                <h3 class="card-title">Geannuleerd wegens Covid</h3>
                <p class="card-desc">Geannuleerd wegens Covid</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0114.jpg" alt="Geannuleerd wegens Covid" loading="lazy" />
                </div>
              </div>

              <!-- Card 13: 2022 -->
              <div class="timeline-card" data-index="12">
                <div class="card-year-row">
                  <span class="card-year">2022</span>
                  <span class="card-num">13</span>
                </div>
                <h3 class="card-title">Toren-tasting</h3>
                <p class="card-desc">Introductie van de Toren-tasting: Tasting in de Gentse Toren. Eerst een rondleiding, dram op de top en dan afdalen voor een bijzondere tasting.</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0121.jpg" alt="Toren-tasting" loading="lazy" />
                </div>
              </div>

              <!-- Card 14: 2023 -->
              <div class="timeline-card" data-index="13">
                <div class="card-year-row">
                  <span class="card-year">2023</span>
                  <span class="card-num">14</span>
                </div>
                <h3 class="card-title">Buitenterrein</h3>
                <p class="card-desc">Introductie van het buitenterrein met foodtruck en lucht-ruimte.</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0136.jpg" alt="Buitenterrein" loading="lazy" />
                </div>
              </div>

              <!-- Card 15: 2025 -->
              <div class="timeline-card" data-index="14">
                <div class="card-year-row">
                  <span class="card-year">2025</span>
                  <span class="card-num">15</span>
                </div>
                <h3 class="card-title">Amsterdam & Vatenmaken</h3>
                <p class="card-desc">Oprichting WhiskyFest Amsterdam. Introductie van de Vatenmaken workshop op het buitenterrein. Mini-tasting en mini-workshop over hoe je vaten maakt, maar ook wat de invloed van het hout op de whisky is. Echt informatief over het whisky maakproces.</p>
                <div class="card-img-frame">
                  <img src="/Afbeeldingen/whisky-festival-2025-0155.jpg" alt="Amsterdam & Vatenmaken" loading="lazy" />
                </div>
              </div>

              <!-- Card 16: 2026 -->
              <div class="timeline-card highlighted-jubilee" data-index="15">
                <div class="card-year-row">
                  <span class="card-year text-white">2026</span>
                  <span class="card-num text-white-muted">16</span>
                </div>
                <h3 class="card-title text-white">25e Editie</h3>
                <p class="card-desc text-white-muted">25 ste editie van het International Whisky Festival</p>
                <div class="card-img-frame">
                  <img src="/timeline_2026_jubilee.png" alt="25 ste editie" loading="lazy" />
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Bottom Timeline Axis Selector -->
        <div class="timeline-axis-wrap">
          <div class="timeline-axis-line">
            <div class="timeline-axis-progress" id="axis-progress"></div>
          </div>
          <div class="timeline-axis-ticks">
            <button class="axis-tick active" data-index="0"><span>'00</span></button>
            <button class="axis-tick" data-index="1"><span>'01</span></button>
            <button class="axis-tick" data-index="2"><span>'02</span></button>
            <button class="axis-tick" data-index="3"><span>'03</span></button>
            <button class="axis-tick" data-index="4"><span>'04</span></button>
            <button class="axis-tick" data-index="5"><span>'06</span></button>
            <button class="axis-tick" data-index="6"><span>'09</span></button>
            <button class="axis-tick" data-index="7"><span>'11</span></button>
            <button class="axis-tick" data-index="8"><span>'15</span></button>
            <button class="axis-tick" data-index="9"><span>'17</span></button>
            <button class="axis-tick" data-index="10"><span>'20</span></button>
            <button class="axis-tick" data-index="11"><span>'21</span></button>
            <button class="axis-tick" data-index="12"><span>'22</span></button>
            <button class="axis-tick" data-index="13"><span>'23</span></button>
            <button class="axis-tick" data-index="14"><span>'25</span></button>
            <button class="axis-tick" data-index="15"><span>'26</span></button>
          </div>
        </div>
      </div>
    </section>

    <!-- 8. HERITAGE STATS BAR (Sustainability/Trust) -->
    <section class="heritage-bar">
      <div class="section-container">
        <div class="heritage-grid">
          <div class="heritage-stat">
            <div class="stat-icon-wrapper">
              <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold-dark)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                <polyline points="14 2 14 8 20 8"></polyline>
                <line x1="16" y1="13" x2="8" y2="13"></line>
                <line x1="16" y1="17" x2="8" y2="17"></line>
              </svg>
            </div>
            <span class="stat-val">Sinds 2000</span>
            <span class="stat-label">Historische Traditie</span>
          </div>
          <div class="heritage-stat">
            <div class="stat-icon-wrapper">
              <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold-dark)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
              </svg>
            </div>
            <span class="stat-val">Duizenden</span>
            <span class="stat-label">Tevreden Bezoekers</span>
          </div>
          <div class="heritage-stat">
            <div class="stat-icon-wrapper">
              <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="var(--whisky-gold-dark)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
                <path d="M7 3h10l1 7c0 4.4-3.6 8-8 8s-8-3.6-8-8l1-7z"></path>
                <line x1="12" y1="18" x2="12" y2="21"></line>
                <line x1="8" y1="21" x2="16" y2="21"></line>
              </svg>
            </div>
            <span class="stat-val">200+</span>
            <span class="stat-label">Unieke Whisky's</span>
          </div>
        </div>
      </div>
    </section>

    <!-- 9. ASYMMETRICAL TESTIMONIALS -->
    <section class="testimonials-section">
      <div class="section-container split-layout">
        
        <div class="split-visual tilt-slight-right">
          <div class="video-thumbnail-card vintage-frame">
            <div class="vintage-frame-inner" style="background: var(--text-charcoal); padding: 0; position: relative; overflow: hidden; border-radius: 4px; aspect-ratio: 16/9;">
              <video 
                id="aftermovie-video"
                src="/Video/hero_loop.mp4"
                poster="/aftermovie_poster.jpg"
                preload="none"
                playsinline
                style="width: 100%; height: 100%; object-fit: cover; display: block; border-radius: 4px;"
              ></video>
              <div id="video-overlay" class="video-overlay-cover text-center" style="position: absolute; inset: 0; background: linear-gradient(180deg, rgba(28, 25, 23, 0.25) 0%, rgba(28, 25, 23, 0.65) 100%); display: flex; flex-direction: column; align-items: center; justify-content: center; cursor: pointer; transition: all 0.3s ease; z-index: 10;">
                <div class="play-button" id="aftermovie-play-btn">▶</div>
                <p style="color: #FFF; font-family: var(--font-sans); letter-spacing:0.12em; margin-top:1.25rem; text-transform:uppercase; font-size:0.85rem; font-weight:bold; text-shadow: 0 2px 10px rgba(0,0,0,0.8);">Bekijk Official Aftermovie 2025</p>
              </div>
            </div>
          </div>
        </div>

        <div class="split-text">
          <span class="tt-tag">Wat bezoekers zeggen</span>
          <h3 class="testimonial-quote">"Dit festival is echt van een ander niveau. De locatie is adembenemend en het aanbod aan unieke masterclasses is enorm."</h3>
          <p class="testimonial-author">— Jeroen van den Berg, Bezoeker sinds 2012</p>
          <div class="review-stars">★★★★★</div>
        </div>

      </div>
    </section>

        <!-- 10. VEELGESTELDE VRAGEN (FAQ ACCORDION) -->
    <section id="faq" class="faq-accordion-section">
      <div class="section-container">
        
        <div class="faq-header-block text-center">
          <span class="tt-tag">Praktische Informatie</span>
          <h2 class="faq-main-title">Veelgestelde Vragen</h2>
          <p class="faq-subtitle">
            Heeft u vragen over uw bezoek, tickets, bereikbaarheid of het festivalprogramma? Hier vindt u alle antwoorden op een rij.
          </p>
        </div>

        <div class="faq-accordion-list">
          
          <details class="faq-item" name="whisky-faq" open>
            <summary class="faq-summary">
              <span class="faq-q-text">Wat zit er inbegrepen bij een regulier sessieticket?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>Bij ieder sessie-entreeticket is inbegrepen: toegang tot de beursvloer in de De Oude Vismijn, uw officiële Glencairn proefglas, het festivalboekje met plattegrond en standinformatie, onbeperkt gratis water bij de waterpunten en gratis stokbroodjes om smaak te neutraliseren.</p>
            </div>
          </details>

          <details class="faq-item" name="whisky-faq">
            <summary class="faq-summary">
              <span class="faq-q-text">Kan ik een masterclass of whisky bootje boeken zonder entreeticket?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>Voor masterclasses in de De Oude Vismijn heeft u altijd een geldig entreeticket nodig voor de bijbehorende sessie. Het whisky bootje kan ook zonder entree ticket.</p>
            </div>
          </details>

          <details class="faq-item" name="whisky-faq">
            <summary class="faq-summary">
              <span class="faq-q-text">Hoe werkt het proeven op de beursvloer en het dram-systeem?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>Bij vrijwel alle standhouders kunt u standaard één of enkele whisk(e)y's gratis proeven. Voor zeldzame, oudere of gelimiteerde single casks vragen standhouders 1 of enkele drams (verkrijgbaar bij de festival infostands en kassa's). Drams kunnen met contant geld worden aangekocht én met de pin, bij de informatie-stand.</p>
            </div>
          </details>

          <details class="faq-item" name="whisky-faq">
            <summary class="faq-summary">
              <span class="faq-q-text">Hoe bereik ik de De Oude Vismijn met het openbaar vervoer?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>De Oude Vismijn bevindt zich in het historische hart van Gent aan het Sint-Veerleplein 5 / Rekelingestraat 5. Vanaf station Gent-Sint-Pieters of Gent-Dampoort reist u in enkele minuten met tram 1 of bus (Halte Gravensteen / Sint-Veerleplein, 1 min. lopen).</p>
            </div>
          </details>

          <details class="faq-item" name="whisky-faq">
            <summary class="faq-summary">
              <span class="faq-q-text">Hoe ontvang ik mijn festivaltickets na aankoop?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>Direct na afronding van uw bestelling ontvangt u een bevestigingsmail met uw digitale e-tickets en QR-codes. U kunt de tickets ook direct toevoegen aan uw Apple Wallet of bekijken in uw account.</p>
            </div>
          </details>

          <details class="faq-item" name="whisky-faq">
            <summary class="faq-summary">
              <span class="faq-q-text">Geldt er een minimumleeftijd voor het festival?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>Ja, conform de wetgeving hanteren wij een strikt 18+ beleid. Personen onder de 18 jaar (ook baby's en kinderen onder begeleiding) worden niet toegelaten tot de festivallocaties. Er kan bij de entree om een geldig legitimatiebewijs worden gevraagd.</p>
            </div>
          </details>

          <details class="faq-item" name="whisky-faq">
            <summary class="faq-summary">
              <span class="faq-q-text">Is er catering en eten aanwezig in De Oude Vismijn?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>Jazeker, er is een selectie van ambachtelijke foodstands aanwezig met o.a. broodjes, warme snacks, worsten en warme gerechten om een goede bodem te leggen voor het proeven.</p>
            </div>
          </details>

          <details class="faq-item" name="whisky-faq">
            <summary class="faq-summary">
              <span class="faq-q-text">Kan ik mijn tickets annuleren of ruilen naar een ander dagdeel?</span>
              <span class="faq-icon-cross">+</span>
            </summary>
            <div class="faq-answer">
              <p>Tickets zijn niet-restitueerbaar. Wel kunt u uw ticket kosteloos overdragen aan een vriend of kennis, aangezien de tickets niet persoonsgebonden op naam gecontroleerd worden, maar per unieke QR-code gescand worden.</p>
            </div>
          </details>

        </div>

        <!-- FAQ Help CTA Card -->
        <div class="faq-contact-card">
          <div class="fcc-content">
            <h4 class="fcc-title">Staat uw vraag er niet tussen?</h4>
            <p class="fcc-desc">Onze festivalorganisatie helpt u graag verder met vragen over uw boeking of programma.</p>
          </div>
          <a href="mailto:info@whiskyfestival.be" class="btn btn-gold fcc-btn">Contact opnemen</a>
        </div>

      </div>
    </section>

    <!-- 11. MEGA-FOOTER (Includes Brand Strip & Footer component) -->
    <section class="mega-footer-wrapper">
      <div class="brand-strip text-center" style="padding: 4.5rem 0 2rem; background: var(--bg-parchment); overflow: hidden;">
        <h4 style="color: var(--text-muted); font-size:0.85rem; font-family: var(--font-sans); font-weight: 800; text-transform: uppercase; letter-spacing:0.18em; margin-bottom: 2rem;">EEN GREEP UIT DE AANWEZIGE MERKEN</h4>
        
        <div class="trust-marquee-wrapper">
          <div class="trust-marquee-track">
            <span class="trust-text-logo">The Macallan</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Highland Park</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Glenfiddich</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">The Balvenie</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Laphroaig</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Ardbeg</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Springbank</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Bowmore</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Teeling Whiskey</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">SMWS</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">House of Suntory</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Glengoyne</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Talisker</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Lagavulin</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Dalmore</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Arran Single Malt</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Oban</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Nc'nean</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            
            <!-- Duplicated track for infinite smooth loop -->
            <span class="trust-text-logo">The Macallan</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Highland Park</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Glenfiddich</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">The Balvenie</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Laphroaig</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Ardbeg</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Springbank</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Bowmore</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Teeling Whiskey</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">SMWS</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">House of Suntory</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Glengoyne</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Talisker</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Lagavulin</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Dalmore</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Arran Single Malt</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Oban</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
            <span class="trust-text-logo">Nc'nean</span>
            <span class="partner-sep" style="color: #caac8e; font-size: 0.9rem; opacity: 0.85; user-select: none;">◆</span>
          </div>
        </div>
      </div>
      <Footer />
    </section>

  </main>

  <!-- MOCK CHECKOUT MODAL OVERLAY (Kept intact for full functionality) -->
  <div class="checkout-modal-overlay" id="checkout-modal" style="display: none;">
    <div class="checkout-modal-card">
      <button type="button" class="modal-close-btn" id="modal-close-x">&times;</button>
      
      <div id="modal-step-1">
        <h3 class="modal-title">Voltooi Uw Reservering</h3>
        <div class="vintage-line"></div>
        <p class="modal-subtitle">Dit is een demonstratie van uw nieuwe, premium bestelflow.</p>

        <form id="mock-payment-form">
          <div class="modal-two-columns">
            
            <!-- Left Column: Fields & Payment -->
            <div class="modal-form-side">
              <div class="form-group">
                <label for="cust-name" class="form-label">Naam</label>
                <input type="text" id="cust-name" placeholder="Jan de Vries" required class="form-control" />
              </div>
              <div class="form-group">
                <label for="cust-email" class="form-label">E-mailadres</label>
                <input type="email" id="cust-email" placeholder="jan@devries.nl" required class="form-control" />
              </div>
              <div class="form-group">
                <label for="cust-phone" class="form-label">Telefoonnummer</label>
                <input type="tel" id="cust-phone" placeholder="06 12345678" required class="form-control" />
              </div>

              <div class="form-group" style="margin-top: 0.5rem; margin-bottom: 0.5rem;">
                <label for="checkout-promo-code" class="form-label">Kortingscode of IWS Lidnummer</label>
                <div style="display: flex; gap: 0.5rem;">
                  <input type="text" id="checkout-promo-code" placeholder="Bijv. WS-10024 of GIFT-50" class="form-control" style="flex: 1;" />
                  <button type="button" class="btn btn-outline" id="apply-promo-btn" style="padding: 0 1rem; font-size: 0.85rem; border-color: rgba(140, 110, 70, 0.4); background: transparent; color: var(--text-charcoal);">Toepassen</button>
                </div>
                <span id="promo-status-msg" style="display: none; font-size: 0.75rem; margin-top: 4px; font-weight: 600;"></span>
              </div>

              <div class="payment-method-section" style="margin-top: 0.5rem; padding-top: 0.5rem;">
                <span class="payment-method-title">Betaalmethode</span>
                <div class="payment-grids">
                  <label class="pay-method-card active">
                    <input type="radio" name="payment-method" value="ideal" checked />
                    <span class="pay-logo">iDEAL</span>
                  </label>
                  <label class="pay-method-card">
                    <input type="radio" name="payment-method" value="bancontact" />
                    <span class="pay-logo">Bancontact</span>
                  </label>
                </div>

                <div class="form-group bank-select-group" id="bank-group" style="margin-bottom: 0;">
                  <label for="bank-select" class="form-label">Selecteer uw bank</label>
                  <select id="bank-select" class="form-control">
                    <option value="rabobank">Rabobank</option>
                    <option value="ing">ING</option>
                    <option value="abnamro">ABN AMRO</option>
                    <option value="sns">SNS Bank</option>
                    <option value="bunq">bunq</option>
                  </select>
                </div>
              </div>
            </div>

            <!-- Right Column: Summary & Submit -->
            <div class="modal-summary-side">
              <div>
                <div class="modal-summary-title">Besteloverzicht</div>
                <div id="modal-summary-items" class="modal-summary-items-list">
                  <!-- Dynamic items populated by JS -->
                </div>
                <div class="modal-summary-calc">
                  <div class="modal-summary-item-row" id="modal-summary-subtotal-row">
                    <span>Subtotaal:</span>
                    <span id="modal-summary-subtotal">€0,00</span>
                  </div>
                  <div class="modal-summary-item-row" id="modal-summary-fee-row">
                    <span>Administratiekosten:</span>
                    <span id="modal-summary-fee">€0,00</span>
                  </div>
                  <div class="modal-summary-item-row" id="modal-summary-shipping-row" style="display: none;">
                    <span>Verzendkosten:</span>
                    <span id="modal-summary-shipping">€0,00</span>
                  </div>
                  <div class="modal-summary-item-row" id="modal-summary-discount-row" style="display: none; color: #dc3545; font-weight: 600;">
                    <span>Korting:</span>
                    <span id="modal-summary-discount">€0,00</span>
                  </div>
                </div>
              </div>

              <div>
                <div class="modal-summary-total-row">
                  <span>Totaal:</span>
                  <span id="modal-total-amt">&euro;0,00</span>
                </div>

                <button type="submit" class="btn btn-gold btn-block" id="modal-submit-btn" style="margin-top: 0; width: 100%;">
                  Betaling Afronden
                </button>
              </div>
            </div>
            
          </div>
        </form>
      </div>

      <div id="modal-step-2" style="display: none; text-align: center; padding: 3rem 1rem;">
        <div class="spinner"></div>
        <h3 class="processing-title">Verbinding maken met bank...</h3>
        <p class="processing-desc">Een moment geduld alstublieft, we verifiëren uw betalingsgegevens.</p>
      </div>

      <div id="modal-step-3" style="display: none;">
        <div class="success-ticket-stub">
          <div class="success-header">
            <span class="success-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3.5" stroke-linecap="round" stroke-linejoin="round" style="display:block;">
                <polyline points="20 6 9 17 4 12" />
              </svg>
            </span>
            <h3 class="success-title">Reservering Geslaagd!</h3>
            <p class="success-subtitle">Bedankt voor uw bestelling. Uw tickets zijn gereed.</p>
          </div>
          <div class="ticket-perforation"></div>
          <div class="success-body">
            <div class="ticket-summary-box">
              <div class="summary-item">
                <span>Reserveringsnummer:</span>
                <strong>#WF-2026-98741</strong>
              </div>
              <div class="summary-item">
                <span>E-mailadres:</span>
                <strong id="receipt-sent-email">jan@devries.nl</strong>
              </div>
              <div class="summary-divider"></div>
              <div class="summary-items-header">Bestelde Kaarten & Producten</div>
              <div id="receipt-tickets-list" class="receipt-tickets-ul">
                <!-- Dynamically populated by JS as list items -->
              </div>
            </div>
            <p class="ticket-delivery-notice">
              De officiële E-tickets zijn naar uw e-mailadres verzonden. Neem uw geprinte ticket of mobiele barcode mee naar de ingang van de De Oude Vismijn.
            </p>
 
            <!-- Mijn Account Banner -->
            <a href="/account" class="account-banner" id="my-account-link">
              <div class="account-banner-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="var(--success-green)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="display:block;">
                  <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                  <circle cx="12" cy="7" r="4" />
                </svg>
              </div>
              <div class="account-banner-text">
                <strong>Bekijk in Mijn Account</strong>
                <span>Beheer uw tickets, download e-tickets en bekijk uw bestelgeschiedenis.</span>
              </div>
              <span class="account-banner-arrow">→</span>
            </a>
 
            <button type="button" class="btn btn-charcoal btn-block" id="modal-success-close">
              Sluiten & Terug naar Website
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>


  
  
      <!-- ==========================================
       1. MASTERCLASS INFO & LINE-UP MODAL (GENT THEME)
       ========================================== -->
  <div 
    class="modal-overlay" 
    id="masterclass-info-modal" 
    style="display: none; position: fixed; inset: 0; top: 0; left: 0; width: 100%; height: 100vh; background: rgba(18, 17, 15, 0.82); backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); z-index: 999999; align-items: center; justify-content: center; padding: 1.5rem; overflow-y: auto; box-sizing: border-box;"
  >
    <div class="vintage-modal-card" style="background-color: var(--bg-sand, #FAF7F2); background-image: url('data:image/svg+xml;utf8,%3Csvg viewBox=%220 0 200 200%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cfilter id=%22noiseFilter%22%3E%3CfeTurbulence type=%22fractalNoise%22 baseFrequency=%220.8%22 numOctaves=%223%22 stitchTiles=%22stitch%22/%3E%3C/filter%3E%3Crect width=%22100%25%22 height=%22100%25%22 filter=%22url(%23noiseFilter)%22 opacity=%220.08%22/%3E%3C/svg%3E'); border: 2.5px solid #1E3A8A; border-radius: 12px; width: 100%; max-width: 680px; box-shadow: 0 25px 60px -15px rgba(0, 0, 0, 0.5); position: relative; overflow: hidden; margin: auto; color: var(--text-charcoal, #171614);">
      
      <!-- Modal Header -->
      <div style="padding: 1.5rem 2rem 1.25rem; background-color: var(--bg-parchment, #F3ECE0); border-bottom: 2px dashed rgba(140, 110, 70, 0.35); display: flex; justify-content: space-between; align-items: flex-start;">
        <div>
          <span style="font-family: var(--font-sans); font-size: 0.7rem; font-weight: 800; letter-spacing: 0.2em; text-transform: uppercase; color: #1E3A8A; display: block; margin-bottom: 0.3rem;">SPECIAL MASTERCLASS</span>
          <h3 id="mc-modal-title" style="font-family: var(--font-sans, sans-serif); font-size: 1.45rem; font-weight: 800; color: var(--text-charcoal, #171614); margin: 0; line-height: 1.2; letter-spacing: -0.01em;">Masterclass Informatie</h3>
          <p id="mc-modal-subtitle" style="font-family: var(--font-sans); font-size: 0.88rem; color: var(--text-muted, #665C54); margin: 0.35rem 0 0; line-height: 1.4;">Exclusieve proeverij</p>
        </div>
        <button type="button" id="mc-modal-close" style="background: rgba(0, 0, 0, 0.05); border: 1px solid rgba(140, 110, 70, 0.4); color: var(--text-charcoal); cursor: pointer; padding: 0.4rem; border-radius: 6px; display: flex; align-items: center; justify-content: center; transition: all 0.2s;" aria-label="Sluiten">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
        </button>
      </div>

      <!-- Modal Body -->
      <div style="max-height: 60vh; overflow-y: auto; padding: 1.75rem 2rem; display: flex; flex-direction: column; gap: 1.5rem;">
        
        <!-- Ambassador Box -->
        <div style="background: #FFFFFF; border: 1px solid #A8DAB5; border-radius: 8px; padding: 1.15rem 1.35rem; display: flex; gap: 1.15rem; align-items: flex-start; box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);">
          <div style="width: 44px; height: 44px; border-radius: 50%; background: #E6F4EA; border: 1.5px solid #A8DAB5; color: #1E3A8A; display: flex; align-items: center; justify-content: center; flex-shrink: 0; margin-top: 2px;">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>
          </div>
          <div>
            <strong id="mc-modal-ambassador-name" style="display: block; font-size: 1.05rem; color: var(--text-charcoal, #171614); font-family: var(--font-sans, sans-serif); font-weight: 700;">Ambassadeur</strong>
            <span id="mc-modal-ambassador-title" style="font-family: var(--font-sans); font-size: 0.75rem; font-weight: 800; color: #1E3A8A; text-transform: uppercase; letter-spacing: 0.08em; display: block; margin-top: 0.15rem; margin-bottom: 0.4rem;">Expert &amp; Host</span>
            <p id="mc-modal-ambassador-bio" style="font-family: var(--font-sans); font-size: 0.88rem; color: var(--text-muted, #554D45); margin: 0; line-height: 1.55;">Bio...</p>
          </div>
        </div>

        <!-- Meta Grid -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
          <div style="background: #FFFFFF; border: 1px solid rgba(140, 110, 70, 0.3); border-radius: 6px; padding: 0.75rem 1rem; display: flex; flex-direction: column; gap: 0.2rem;">
            <span style="font-family: var(--font-sans); font-size: 0.65rem; letter-spacing: 0.1em; color: rgba(80, 60, 40, 0.5); text-transform: uppercase;">LOCATIE</span>
            <span id="mc-modal-location" style="font-family: var(--font-sans); font-size: 0.95rem; font-weight: 700; color: var(--text-charcoal); border-bottom: 1px dotted rgba(140, 110, 70, 0.4); padding-bottom: 2px;">-</span>
          </div>
          <div style="background: #FFFFFF; border: 1px solid rgba(140, 110, 70, 0.3); border-radius: 6px; padding: 0.75rem 1rem; display: flex; flex-direction: column; gap: 0.2rem;">
            <span style="font-family: var(--font-sans); font-size: 0.65rem; letter-spacing: 0.1em; color: rgba(80, 60, 40, 0.5); text-transform: uppercase;">SESSIEDUUR</span>
            <span id="mc-modal-duration" style="font-family: var(--font-sans); font-size: 0.95rem; font-weight: 700; color: var(--text-charcoal); border-bottom: 1px dotted rgba(140, 110, 70, 0.4); padding-bottom: 2px;">-</span>
          </div>
        </div>

        <!-- Description -->
        <div>
          <h4 id="mc-modal-about-heading" style="font-family: var(--font-sans, sans-serif); font-size: 1.05rem; font-weight: 800; margin: 0 0 0.45rem 0; color: var(--text-charcoal); letter-spacing: -0.01em;">Over deze Masterclass</h4>
          <p id="mc-modal-description" style="font-family: var(--font-sans); font-size: 0.92rem; color: var(--text-charcoal); line-height: 1.6; margin: 0;">Beschrijving...</p>
        </div>

        <!-- Lineup -->
        <div>
          <h4 id="mc-modal-lineup-heading" style="font-family: var(--font-sans, sans-serif); font-size: 1.05rem; font-weight: 800; margin: 0 0 0.65rem 0; color: var(--text-charcoal); letter-spacing: -0.01em;">Officiële Proeflijst (Inbegrepen Drams)</h4>
          <ul id="mc-modal-lineup" style="list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 0.5rem;">
          </ul>
        </div>

      </div>

      <!-- Modal Footer -->
      <div style="padding: 1.25rem 2rem; background-color: var(--bg-parchment, #F3ECE0); border-top: 2px dashed rgba(140, 110, 70, 0.35); display: flex; justify-content: space-between; align-items: center; gap: 1rem;">
        <div>
          <span style="font-family: var(--font-sans); font-size: 0.65rem; letter-spacing: 0.1em; color: rgba(80, 60, 40, 0.6); text-transform: uppercase; font-weight: 700; display: block; margin-bottom: 2px;">PRIJS P.P.</span>
          <div class="ticket-price-block" id="mc-modal-price-block" style="display: flex; align-items: baseline; color: #1E3A8A;">
            <span class="price-currency" style="font-size: 1.25rem; font-weight: 700; font-family: var(--font-sans); margin-right: 2px;">&euro;</span>
            <span class="price-val" id="mc-modal-price-val" style="font-size: 2.4rem; font-family: var(--font-sans, sans-serif); font-weight: 800; line-height: 1;">0</span>
            <span class="price-cents" id="mc-modal-price-cents" style="font-size: 1.25rem; font-weight: 700; font-family: var(--font-sans);">,00</span>
          </div>
        </div>
        <button type="button" id="mc-modal-add-btn" style="height: 48px; padding: 0 1.6rem; font-family: var(--font-sans, sans-serif); font-weight: 800; font-size: 0.95rem; text-transform: uppercase; letter-spacing: 0.04em; background: #1E3A8A; color: #FFFFFF; border: 2px solid #171614; border-radius: 4px; box-shadow: 4px 4px 0px rgba(23, 22, 20, 0.9); cursor: pointer; display: inline-flex; align-items: center; justify-content: center; transition: all 0.2s ease;">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="vertical-align: middle; margin-right: 8px;"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>
          Toevoegen aan Winkelmand
        </button>
      </div>
    </div>
  </div>

      <!-- ==========================================
       2. DYNAMIC ADD-ON UPSELL MODAL (GENT THEME)
       ========================================== -->
  <div 
    class="modal-overlay" 
    id="addon-upsell-modal" 
    style="display: none; position: fixed; inset: 0; top: 0; left: 0; width: 100%; height: 100vh; background: rgba(18, 17, 15, 0.82); backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px); z-index: 999999; align-items: center; justify-content: center; padding: 1.5rem; overflow-y: auto; box-sizing: border-box;"
  >
    <div class="vintage-modal-card" style="background-color: var(--bg-sand, #FAF7F2); border: 2.5px solid #1E3A8A; border-radius: 12px; width: 100%; max-width: 720px; box-shadow: 0 25px 60px -15px rgba(0, 0, 0, 0.5); position: relative; overflow: hidden; margin: auto; color: var(--text-charcoal, #171614);">
      
      <!-- Header -->
      <div style="padding: 1.5rem 2rem 1.25rem; background-color: var(--bg-parchment, #F3ECE0); border-bottom: 2px dashed rgba(140, 110, 70, 0.35); display: flex; justify-content: space-between; align-items: flex-start;">
        <div>
          <span style="font-family: var(--font-sans); font-size: 0.7rem; font-weight: 800; letter-spacing: 0.2em; text-transform: uppercase; color: #caac8e; display: block; margin-bottom: 0.3rem;">FESTIVAL ADD-ONS &amp; SPECIALS</span>
          <h3 style="font-family: var(--font-sans, sans-serif); font-size: 1.45rem; font-weight: 800; color: var(--text-charcoal, #171614); margin: 0; line-height: 1.2; letter-spacing: -0.01em;">Maak uw Festivalbeleving Compleet</h3>
          <p style="font-family: var(--font-sans); font-size: 0.88rem; color: var(--text-muted, #665C54); margin: 0.35rem 0 0; line-height: 1.4;">Voeg optioneel nog een exclusieve Masterclass, Whisky Bootjes of Dada Rondleiding toe:</p>
        </div>
        <button type="button" id="addon-upsell-close" style="background: rgba(0, 0, 0, 0.05); border: 1px solid rgba(140, 110, 70, 0.4); color: var(--text-charcoal); cursor: pointer; padding: 0.4rem; border-radius: 6px; display: flex; align-items: center; justify-content: center; transition: all 0.2s;" aria-label="Sluiten">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="18" x2="18" y2="18"></line></svg>
        </button>
      </div>

      <!-- Day filter notice banner -->
      <div id="upsell-day-notice" style="display: none; padding: 0.55rem 1.75rem; background: rgba(197, 139, 57, 0.14); border-bottom: 1px dashed rgba(140, 110, 70, 0.35); font-family: var(--font-sans); font-size: 0.82rem; color: #7A551E; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.5rem;">
        <span id="upsell-day-notice-text">📅 Getoond voor uw festivaldag: <strong>Vrijdag</strong></span>
        <button type="button" id="upsell-toggle-days-btn" style="background: none; border: none; color: #1E3A8A; font-family: var(--font-sans); font-weight: 800; cursor: pointer; text-decoration: underline; font-size: 0.8rem; padding: 0;">
          Toon alle festivaldagen
        </button>
      </div>

      <!-- Category Filter Pills inside Modal -->
      <div class="upsell-modal-filters" style="display: flex; gap: 0.4rem; padding: 0.75rem 1.75rem; background: var(--bg-parchment, #F3ECE0); border-bottom: 1px solid rgba(140, 110, 70, 0.2); overflow-x: auto; flex-wrap: wrap;">
        <button type="button" class="upsell-filter-btn active" data-upsell-cat="all" style="padding: 0.35rem 0.85rem; font-family: var(--font-sans); font-size: 0.76rem; font-weight: 700; border-radius: 20px; border: 1px solid #1E3A8A; background: #1E3A8A; color: #FFFFFF; cursor: pointer; transition: all 0.2s;">Alles</button>
        <button type="button" class="upsell-filter-btn" data-upsell-cat="warehouse" style="padding: 0.35rem 0.85rem; font-family: var(--font-sans); font-size: 0.76rem; font-weight: 700; border-radius: 20px; border: 1px solid rgba(140, 110, 70, 0.3); background: transparent; color: var(--text-charcoal); cursor: pointer; transition: all 0.2s;">Dada Rondleiding</button>
        <button type="button" class="upsell-filter-btn" data-upsell-cat="masterclass" style="padding: 0.35rem 0.85rem; font-family: var(--font-sans); font-size: 0.76rem; font-weight: 700; border-radius: 20px; border: 1px solid rgba(140, 110, 70, 0.3); background: transparent; color: var(--text-charcoal); cursor: pointer; transition: all 0.2s;">Masterclasses</button>
        <button type="button" class="upsell-filter-btn" data-upsell-cat="tram" style="padding: 0.35rem 0.85rem; font-family: var(--font-sans); font-size: 0.76rem; font-weight: 700; border-radius: 20px; border: 1px solid rgba(140, 110, 70, 0.3); background: transparent; color: var(--text-charcoal); cursor: pointer; transition: all 0.2s;">Whisky Bootjes</button>
        <button type="button" class="upsell-filter-btn" data-upsell-cat="botteling" style="padding: 0.35rem 0.85rem; font-family: var(--font-sans); font-size: 0.76rem; font-weight: 700; border-radius: 20px; border: 1px solid rgba(140, 110, 70, 0.3); background: transparent; color: var(--text-charcoal); cursor: pointer; transition: all 0.2s;">Botteling</button>
      </div>

      <!-- Addons List Container -->
      <div style="max-height: 52vh; overflow-y: auto; padding: 1.25rem 1.75rem; display: flex; flex-direction: column; gap: 0.75rem;" id="addon-upsell-list">
        <!-- Populated dynamically with ALL filtered add-ons -->
      </div>

      <!-- Footer -->
      <div style="padding: 1.25rem 2rem; background-color: var(--bg-parchment, #F3ECE0); border-top: 2px dashed rgba(140, 110, 70, 0.35); display: flex; justify-content: space-between; align-items: center; gap: 1rem;">
        <button type="button" id="addon-upsell-cancel" style="background: none; border: none; color: #776B5F; font-size: 0.9rem; font-weight: 600; cursor: pointer; text-decoration: underline;">
          Nee bedankt, direct doorgaan
        </button>
        <button type="button" id="addon-upsell-proceed-btn" style="height: 48px; padding: 0 1.6rem; font-family: var(--font-sans, sans-serif); font-weight: 800; font-size: 0.95rem; text-transform: uppercase; letter-spacing: 0.04em; background: #1E3A8A; color: #FFFFFF; border: 2px solid #171614; border-radius: 4px; box-shadow: 4px 4px 0px rgba(23, 22, 20, 0.8); cursor: pointer; display: inline-flex; align-items: center; justify-content: center; transition: all 0.2s ease;">
          Naar Afrekenen &rarr;
        </button>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* --- 2.5-SECOND COIN-FLIP PRELOADER --- */
  .hp-preloader-overlay {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background-color: var(--bg-parchment);
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    transition: opacity 0.4s cubic-bezier(0.16, 1, 0.3, 1), transform 0.4s ease, filter 0.4s ease;
  }

  .hp-preloader-overlay.fade-out {
    opacity: 0;
    filter: blur(12px);
    transform: scale(1.04);
    pointer-events: none;
  }

  .preloader-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2.25rem;
    perspective: 1200px;
  }

  /* Butter-smooth 3D Coin Toss */
  .coin-3d-stage {
    position: relative;
    width: 140px;
    height: 170px;
    display: flex;
    justify-content: center;
    align-items: center;
    perspective: 1000px;
  }

  .coin-flipper-core {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    filter: drop-shadow(0 14px 28px rgba(45, 42, 38, 0.25));
    will-change: transform;
    animation: coinTossSmooth 2.2s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  .coin-flipper-core svg {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
  }

  @keyframes coinTossSmooth {
    0% {
      transform: translateY(0) rotateY(0deg) scale(0.85);
    }
    40% {
      transform: translateY(-40px) rotateY(360deg) scale(1.15);
    }
    80% {
      transform: translateY(0) rotateY(720deg) scale(0.98);
    }
    92% {
      transform: translateY(-6px) rotateY(720deg) scale(1.02);
    }
    100% {
      transform: translateY(0) rotateY(720deg) scale(1);
    }
  }

  /* Pulsing Floor Shadow */
  .coin-glow-shadow {
    position: absolute;
    bottom: -18px;
    left: 50%;
    transform: translateX(-50%);
    width: 110px;
    height: 18px;
    border-radius: 50%;
    background: radial-gradient(ellipse at center, rgba(197, 139, 57, 0.55) 0%, transparent 75%);
    animation: coinShadowPulse 2.2s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  @keyframes coinShadowPulse {
    0% { transform: translateX(-50%) scale(0.5); opacity: 0.3; }
    40% { transform: translateX(-50%) scale(1.35); opacity: 0.85; }
    80% { transform: translateX(-50%) scale(0.95); opacity: 0.5; }
    100% { transform: translateX(-50%) scale(1); opacity: 0.65; }
  }

  /* Preloader Text & Progress Bar */
  .preloader-text-group {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.65rem;
  }

  .preloader-title {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.85rem;
    letter-spacing: 0.22em;
    color: var(--text-charcoal);
    text-transform: uppercase;
  }

  .preloader-progress-track {
    width: 220px;
    height: 4px;
    background-color: rgba(45, 42, 38, 0.12);
    border-radius: 4px;
    overflow: hidden;
    position: relative;
  }

  .preloader-progress-bar {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, var(--whisky-gold-dark), var(--whisky-gold), var(--whisky-gold-light));
    border-radius: 4px;
    animation: fillLoader 2.2s cubic-bezier(0.4, 0, 0.2, 1) forwards;
  }

  @keyframes fillLoader {
    0% { width: 0%; }
    40% { width: 45%; }
    75% { width: 85%; }
    100% { width: 100%; }
  }

  .preloader-subtitle {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: var(--whisky-gold-dark);
  }

  /* ----------------------------------------------------
     SECTION LAYOUTS (TICKET TAILOR STYLE)
     ---------------------------------------------------- */
  .section-container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 2rem;
  }
  .text-center { text-align: center; }

  .tt-hero-section {
    position: relative;
    padding: 15.5rem 0 3.5rem; /* Generous top clearance ensuring logo badge sits cleanly above title */
    background: linear-gradient(135deg, #091326 0%, #1E3A8A 35%, #172554 65%, #0B1528 100%) !important;
    color: #FFFFFF !important;
    overflow: hidden;
  }

  .tt-hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }
  .hero-tag {
    display: inline-block;
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: #E4C765 !important;
    margin-bottom: 1rem;
    border-bottom: 2px solid #22C55E;
    padding-bottom: 0.5rem;
  }
  .tt-hero-title {
    font-family: var(--font-display);
    font-size: clamp(2.4rem, 4.8vw, 3.85rem);
    color: #FFFFFF !important;
    text-shadow: 0 4px 25px rgba(0, 0, 0, 0.5);
    line-height: 1.05;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }
  .hero-date-line {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-sans);
    font-size: 0.92rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--whisky-gold-light, #E4C765);
    margin-bottom: 1.5rem;
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.5);
  }
  .hero-date-line .date-sep {
    color: rgba(228, 199, 101, 0.55);
    font-size: 0.6rem;
  }
  .highlight-text {
    color: var(--whisky-gold-light, #E2B670) !important;
    background: none !important;
    border: none !important;
    border-radius: 0 !important;
    padding: 0 !important;
    box-shadow: none !important;
    text-shadow: 0 2px 12px rgba(0, 0, 0, 0.6) !important;
  }
  .tt-hero-subtitle {
    font-size: 1.05rem; /* Refined smaller subtitle font */
    color: rgba(255, 255, 255, 0.92) !important;
    line-height: 1.6;
    margin-bottom: 2rem;
    max-width: 620px;
  }
  .tt-hero-text {
    padding-bottom: 4rem;
    position: relative;
    z-index: 3;
    transition: all 0.3s;
  }
  .hero-barrel-art {
    position: absolute;
    left: -50px;
    bottom: -10px;
    width: 380px;
    height: auto;
    opacity: 0.07;
    pointer-events: none;
    z-index: 0;
  }
  .hero-barrel-art img {
    width: 100%;
    height: auto;
    filter: invert(1) brightness(2);
  }
  .tt-hero-text > *:not(.hero-barrel-art) {
    position: relative;
    z-index: 1;
  }
  .hero-actions {
    display: flex;
    gap: 1.25rem;
    align-items: center;
  }
  .btn-beige {
    background-color: var(--whisky-gold-light, #E9DFB7) !important;
    color: var(--text-charcoal, #1D1C1A) !important;
    border: 2px solid var(--text-charcoal, #1D1C1A) !important;
    box-shadow: 4px 4px 0px rgba(29, 28, 26, 0.9) !important;
    font-family: var(--font-sans) !important;
    font-weight: 800 !important;
    text-transform: uppercase !important;
    letter-spacing: 0.06em !important;
    padding: 0.95rem 1.85rem !important;
    border-radius: 4px !important;
    transition: all 0.25s cubic-bezier(0.16, 1, 0.3, 1) !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    text-decoration: none !important;
  }
  .btn-beige:hover {
    background-color: var(--whisky-gold, #D5C28A) !important;
    color: var(--text-charcoal, #1D1C1A) !important;
    transform: translate(-2px, -2px) !important;
    box-shadow: 6px 6px 0px rgba(29, 28, 26, 0.9) !important;
  }
  .btn-beige:active {
    transform: translate(2px, 2px) !important;
    box-shadow: 0px 0px 0px rgba(29, 28, 26, 0.9) !important;
  }
  .tt-text-link {
    color: #FFFFFF !important;
    border-bottom: 1.5px solid rgba(255, 255, 255, 0.75) !important;
    text-decoration: none;
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 1.02rem;
    padding-bottom: 2px;
    transition: all 0.2s ease;
    white-space: nowrap;
  }
  .tt-text-link:hover {
    color: #FFFFFF !important;
    border-bottom-color: #FFFFFF !important;
    opacity: 0.8;
  }
  .reviews-title {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    color: #FFFFFF !important;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
    margin-bottom: 1.25rem;
  }
  .reviews-grid {
    display: flex;
    gap: 3rem;
  }
  .review-item {
    display: flex;
    flex-direction: column;
  }
  .review-item .stars {
    color: #E4C765 !important;
    font-size: 1.25rem;
    letter-spacing: 2px;
    margin-bottom: 0.35rem;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  }
  .review-item .review-source {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.85) !important;
  }

  /* --- FULL-BLEED SEAMLESS HERO VIDEO BANNER --- */
  .hero-video-full-right {
    position: absolute;
    inset: 0; /* Spans 100% width and height of hero section */
    z-index: 1;
    overflow: hidden;
    /* Alpha Mask: Completely transparent on left 35%, smooth transition to solid on right 72% */
    mask-image: linear-gradient(to right, transparent 0%, transparent 35%, black 72%, black 100%);
    -webkit-mask-image: linear-gradient(to right, transparent 0%, transparent 35%, black 72%, black 100%);
  }

  .hero-video-element {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 75% center;
    display: block;
  }

  /* Multi-stop Haags Green Blend & Vignette Overlay */
  .hero-video-blend-gradient {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: 
      linear-gradient(
        to right, 
        #091326 0%, 
        #091326 30%, 
        rgba(9, 19, 38, 0.85) 45%, 
        rgba(9, 19, 38, 0.3) 65%, 
        transparent 85%
      ),
      linear-gradient(
        to bottom, 
        #091326 0%, 
        rgba(9, 19, 38, 0.6) 12%, 
        transparent 28%, 
        transparent 78%, 
        #091326 100%
      ) !important;
  }

  /* Subtle Deep Emerald & Gold Tint Overlay */
  .hero-video-tint-overlay {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(
      circle at 70% 50%,
      rgba(22, 86, 54, 0.15) 0%,
      rgba(9, 19, 38, 0.45) 100%
    ) !important;
  }

  /* Center Right Whisky Glass Artwork (Middel 15.svg) */
  .tt-hero-visual-right {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    z-index: 5;
    pointer-events: none;
    height: 100%;
  }

  .hero-glass-art-card {
    width: 100%;
    max-width: 320px;
    display: flex;
    justify-content: center;
    align-items: center;
    filter: drop-shadow(0 15px 35px rgba(0, 0, 0, 0.45));
    opacity: 0.95;
    margin-bottom: 5.5rem; /* Lift logo higher independently */
    /* No animation - clean static artwork */
  }

  .hero-glass-art-card svg {
    width: 100%;
    height: auto;
    max-height: 420px;
    display: block;
  }

  /* floatingGlass removed */

  .hero-video-badge {
    position: absolute;
    bottom: 6.5rem;
    right: 2rem;
    display: flex;
    align-items: center;
    gap: 8px;
    background-color: rgba(29, 28, 26, 0.85);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid var(--whisky-gold);
    color: var(--whisky-gold-light);
    padding: 0.45rem 1rem;
    border-radius: 20px;
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.35);
    z-index: 5;
  }

  .hero-video-badge .badge-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: var(--whisky-gold);
    box-shadow: 0 0 8px var(--whisky-gold);
    animation: pulseGlow 1.5s infinite;
  }

  /* --- HORIZONTAL 2-ROW INFINITE MARQUEE SECTION --- */
  .horizontal-photo-gallery-section {
    padding: 4rem 0 5rem;
    background-color: transparent;
    border-top: none;
    border-bottom: none;
    overflow: hidden;
    position: relative;
  }

  .horizontal-marquee-container {
    width: 100%;
    overflow: hidden;
    padding: 3rem 0;
    mask-image: linear-gradient(to right, transparent 0%, black 5%, black 95%, transparent 100%);
    -webkit-mask-image: linear-gradient(to right, transparent 0%, black 5%, black 95%, transparent 100%);
  }

  .marquee-row-wrapper {
    width: 100%;
    overflow: visible;
    display: flex;
    padding: 1rem 0;
  }

  .marquee-row {
    display: flex;
    gap: 2.5rem;
    width: max-content;
    will-change: transform;
  }

  .marquee-row-left {
    animation: scrollMarqueeLeft 50s linear infinite;
  }

  .marquee-row-right {
    animation: scrollMarqueeRight 55s linear infinite;
  }

  .horizontal-marquee-container:hover .marquee-row {
    animation-play-state: paused;
  }

  @keyframes scrollMarqueeLeft {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  @keyframes scrollMarqueeRight {
    0% { transform: translateX(-50%); }
    100% { transform: translateX(0); }
  }

  .marquee-photo-card {
    flex-shrink: 0;
    width: 400px;
    height: 280px;
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
    cursor: pointer;
    padding: 10px;
  }

  .marquee-photo-card.rotate-neg-1 { transform: rotate(-1.8deg); }
  .marquee-photo-card.rotate-pos-2 { transform: rotate(2.2deg); }
  .marquee-photo-card.rotate-neg-2 { transform: rotate(-2.2deg); }
  .marquee-photo-card.rotate-pos-1 { transform: rotate(1.8deg); }

  .marquee-photo-card:hover {
    transform: scale(1.08) rotate(0deg) !important;
    z-index: 10;
  }

  .photo-mask-frame {
    width: 100%;
    height: 100%;
    -webkit-mask-image: url('/torn-mask.svg');
    mask-image: url('/torn-mask.svg');
    -webkit-mask-size: 100% 100%;
    mask-size: 100% 100%;
    -webkit-mask-repeat: no-repeat;
    mask-repeat: no-repeat;
    filter: drop-shadow(0 10px 20px rgba(45, 42, 38, 0.2));
  }

  .photo-mask-frame img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.5s ease;
  }

  .marquee-photo-card:hover .photo-mask-frame img {
    transform: scale(1.08);
  }

  /* Hero Text Highlights & Reviews */
  .highlight-text {
    display: inline;
    line-height: 1.1;
  }

  .tt-hero-reviews {
    margin-top: 4rem;
    position: relative;
    max-width: 500px;
  }
  .reviews-title {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    color: #FFFFFF !important;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
    margin-bottom: 1.25rem;
  }
  .reviews-grid {
    display: flex;
    gap: 3rem;
  }
  .review-item {
    display: flex;
    flex-direction: column;
  }
  .review-item .stars {
    color: #E4C765 !important;
    font-size: 1.25rem;
    letter-spacing: 2px;
    margin-bottom: 0.35rem;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  }
  .review-item .review-source {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.85) !important;
  }
  .trophy-icon {
    position: absolute;
    right: -20px;
    bottom: -10px;
    font-size: 4rem;
    transform: rotate(15deg);
    opacity: 0.9;
  }

  /* Trust Logos text mock */
  .trust-text-logo {
    font-family: var(--font-display);
    font-size: 1.5rem;
    color: var(--text-charcoal);
    opacity: 0.4;
    transition: opacity 0.3s;
    letter-spacing: 0.1em;
  }
  .trust-text-logo:hover {
    opacity: 1;
  }

  /* ====== TEAR SECTION ====== */
  .tear-section {
    position: relative;
    background-color: var(--bg-parchment);
  }

  .tear-header-area {
    padding-top: 6rem;
    padding-bottom: 3rem;
    background-color: var(--bg-parchment);
  }

  /* Torn edge images */
  .torn-edge-wrap {
    position: relative;
    z-index: 20;
    line-height: 0;
    filter: drop-shadow(0px 18px 18px rgba(0,0,0,0.55));
  }

  .torn-edge-img {
    display: block;
    width: 100%;
    height: 60px;
  }

  /* Top edge: shadow only visible BELOW (into dark gap), clipped above */
  .torn-edge-wrap:not(.torn-edge-bottom) {
    margin-bottom: -30px;
    clip-path: inset(0 -50px -50px -50px);
  }

  /* Bottom edge: shadow only visible ABOVE (into dark gap), clipped below */
  .torn-edge-bottom {
    margin-top: -30px;
    margin-bottom: 0;
    filter: drop-shadow(0px -18px 18px rgba(0,0,0,0.55));
    clip-path: inset(-50px -50px 0px -50px);
  }

  /* Dark ticket area — sits BEHIND the torn edges */
  .dark-gap {
    padding: 3rem 0;
    padding-top: 3.5rem; /* Reduced to pull content closer to torn edge overlap */
    background-color: var(--success-green); /* Rich brand dark green (#1E3A8A) */
    position: relative;
    z-index: 1;
  }

  :global(.ticket-stub.hidden-by-filter),
  :global(.ticket-stub.hidden-by-limit) {
    display: none !important;
  }

  .mobile-filter-dropdowns {
    display: none;
  }

  .shop-filters-container {
    background: transparent;
    border: none;
    padding: 0;
    margin-bottom: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    box-shadow: none;
  }
  .filter-row {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .filter-label {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(250, 247, 242, 0.6);
    width: 60px;
    flex-shrink: 0;
  }
  .filter-buttons {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .filter-tab-btn {
    background-color: rgba(250, 247, 242, 0.05);
    border: 1px solid rgba(250, 247, 242, 0.18);
    color: #FAF7F2;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 500;
    padding: 0.4rem 1rem;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .filter-tab-btn:hover {
    border-color: #d5c28a;
    background-color: rgba(213, 194, 138, 0.1);
  }
  .filter-tab-btn.active {
    background-color: #d5c28a;
    border-color: #d5c28a;
    color: var(--text-charcoal);
    font-weight: 700;
  }

  /* Fade-in animation for tickets on scroll */
  .fade-in-group :global(.ticket-stub) {
    opacity: 1;
    transform: translateY(0);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }
  .fade-in-group.is-visible :global(.ticket-stub) {
    opacity: 1;
    transform: translateY(0);
  }
  .fade-in-group.is-visible :global(.ticket-stub):nth-child(2) { transition-delay: 0.1s; }
  .fade-in-group.is-visible :global(.ticket-stub):nth-child(3) { transition-delay: 0.2s; }
  .fade-in-group.is-visible :global(.ticket-stub):nth-child(4) { transition-delay: 0.3s; }

  /* Drams banner — light theme matching hero */
  .banner-drams {
    background-color: var(--bg-parchment); /* Match parchment background */
    color: var(--text-charcoal);
    padding: 5rem 2rem;
    text-align: center;
  }

  .tt-section-header {
    max-width: 650px;
    margin: 0 auto 3rem;
  }
  .tt-section-title {
    font-family: var(--font-display);
    font-size: clamp(1.85rem, 3.2vw, 2.45rem);
    line-height: 1.15;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    margin-bottom: 0.75rem;
  }
  .tt-section-subtitle {
    font-family: var(--font-sans);
    font-size: 1.05rem;
    line-height: 1.6;
    color: var(--text-muted);
  }
  .shop-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr;
    gap: 5rem;
    /* align-items: stretch by default allows the sidebar to fill the height, enabling position: sticky to work */
  }
  .tickets-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* TT Split Layout specifics */
  .split-features-section {
    padding: 8rem 0 2rem;
    background-color: var(--bg-parchment);
  }
  .tt-tag {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--whisky-gold-dark);
    margin-bottom: 1rem;
  }
  .tt-split-title {
    font-family: var(--font-sans);
    font-size: 1.35rem;
    font-weight: 800;
    line-height: 1.25;
    color: var(--text-charcoal);
    letter-spacing: -0.01em;
    margin-bottom: 0.85rem;
  }
  .tt-split-desc {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    line-height: 1.65;
    color: var(--text-muted);
    margin-bottom: 1.25rem;
  }
  .tt-feature-list {
    list-style: none;
    font-family: var(--font-sans);
    font-size: 0.92rem;
    color: var(--text-charcoal);
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
    margin-bottom: 1.25rem;
  }
  .tt-text-link {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--whisky-gold-dark);
    text-decoration: none;
    border-bottom: 1px solid var(--whisky-gold);
    padding-bottom: 2px;
    transition: all 0.2s;
  }
  .tt-text-link:hover {
    color: var(--text-charcoal);
    border-color: var(--text-charcoal);
  }

  /* Tab Accordion Section */
  .tab-section {
    padding: 5rem 0 8rem;
    background-color: var(--bg-parchment);
  }

  /* Heritage Stats Bar */
  .heritage-bar {
    background-color: var(--bg-paper);
    border-top: 1px solid var(--border-color);
    border-bottom: 1px solid var(--border-color);
    padding: 4rem 0;
  }
  .heritage-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    text-align: center;
    gap: 2rem;
  }
  .heritage-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
  }
  .stat-icon-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 68px;
    height: 68px;
    border-radius: 50%;
    background-color: rgba(140, 110, 70, 0.07);
    border: 1px solid rgba(140, 110, 70, 0.25);
    margin-bottom: 0.25rem;
    transition: transform 0.3s ease, border-color 0.3s ease;
  }
  .heritage-stat:hover .stat-icon-wrapper {
    transform: translateY(-3px);
    border-color: var(--whisky-gold-dark);
  }
  .stat-val {
    font-family: var(--font-display);
    font-size: 2.5rem;
    color: var(--text-charcoal);
    line-height: 1;
  }
  .stat-label {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--whisky-gold-dark);
  }

  /* Testimonials Section */
  .testimonials-section {
    padding: 8rem 0;
    background-color: var(--bg-parchment);
  }
  .play-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background-color: var(--whisky-gold);
    color: #FFF;
    font-size: 2rem;
    cursor: pointer;
    box-shadow: 0 10px 20px rgba(197, 139, 57, 0.4);
    transition: transform 0.2s;
  }
  .play-button:hover {
    transform: scale(1.1);
  }
  .testimonial-quote {
    font-size: 2.25rem;
    line-height: 1.4;
    text-transform: none;
    letter-spacing: 0;
    margin-bottom: 1.5rem;
    color: var(--text-charcoal);
  }
  .testimonial-author {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 1rem;
    color: var(--text-muted);
    margin-bottom: 1rem;
  }
  .review-stars {
    color: var(--whisky-gold);
    font-size: 1.5rem;
  }

  /* Responsive fixes */
  @media (max-width: 1024px) {
    .tt-hero-grid, .shop-grid, .split-layout, .tab-container {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    .split-layout.reverse > * {
      direction: ltr; /* reset */
    }
    .tt-hero-visual {
      height: 400px;
    }
    .float-1 {
      position: static;
      width: 100%;
      margin-bottom: 2rem;
    }
    .float-2 {
      position: static;
    }
    .checkout-sidebar-container {
      position: static;
    }
    .heritage-grid {
      grid-template-columns: repeat(3, 1fr) !important;
      gap: 0.5rem !important;
    }
    .split-text {
      max-width: 100%;
    }
  }

  /* Comprehensive Mobile Polish for Homepage (< 990px) */
  @media (max-width: 990px) {
    /* 1. HERO SECTION */
    .tt-hero-section {
      padding: 4.5rem 1.25rem 3.5rem !important;
      min-height: 72vh !important;
      display: flex !important;
      flex-direction: column !important;
      justify-content: center !important;
      align-items: center !important;
      box-sizing: border-box !important;
    }
    .tt-hero-grid {
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
      justify-content: center !important;
      width: 100% !important;
    }
    .tt-hero-text {
      margin: 0 auto !important;
      padding: 0 !important;
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
      text-align: center !important;
      width: 100% !important;
      max-width: 440px !important;
    }
    .tt-hero-title {
      font-size: 2.35rem !important;
      line-height: 1.08 !important;
      text-align: center !important;
      margin: 0 0 1.15rem !important;
      letter-spacing: 0.02em !important;
    }
    .hero-date-line {
      display: flex !important;
      flex-direction: row !important;
      flex-wrap: wrap !important;
      justify-content: center !important;
      align-items: center !important;
      gap: 0.45rem 0.75rem !important;
      font-size: 0.98rem !important;
      font-weight: 800 !important;
      margin-bottom: 1.85rem !important;
      text-align: center !important;
    }
    .hero-date-line .date-text,
    .hero-date-line .loc-text {
      font-size: 0.96rem !important;
      font-weight: 800 !important;
    }
    .date-sep {
      display: inline !important;
      font-size: 0.65rem !important;
      opacity: 0.7 !important;
    }
    .tt-hero-subtitle {
      display: none !important;
    }
    .hero-actions {
      display: flex !important;
      flex-direction: column !important;
      width: 100% !important;
      gap: 0.65rem !important;
      align-items: stretch !important;
      max-width: 360px !important;
      margin: 0 auto !important;
    }
    .hero-actions .btn-beige {
      width: 100% !important;
      padding: 0.85rem 1.5rem !important;
      font-size: 0.95rem !important;
      font-weight: 800 !important;
      letter-spacing: 0.04em !important;
      text-transform: uppercase !important;
      text-align: center !important;
      justify-content: center !important;
      border-radius: 4px !important;
      border: 2px solid var(--text-charcoal, #0F172A) !important;
      box-shadow: 3px 3px 0px var(--text-charcoal, #0F172A) !important;
    }
    .hero-actions .tt-text-link {
      width: 100% !important;
      padding: 0.75rem 1.5rem !important;
      border: 1.5px solid rgba(255, 255, 255, 0.45) !important;
      background: rgba(255, 255, 255, 0.08) !important;
      border-radius: 4px !important;
      color: #FFFFFF !important;
      font-size: 0.88rem !important;
      font-weight: 700 !important;
      text-align: center !important;
      margin-left: 0 !important;
      text-decoration: none !important;
      display: flex !important;
      align-items: center !important;
      justify-content: center !important;
    }
    .tt-hero-reviews {
      margin-top: 1.5rem !important;
      text-align: center !important;
      max-width: 100% !important;
    }
    .reviews-title {
      font-size: 0.85rem !important;
      margin-bottom: 0.35rem !important;
    }
    .reviews-grid {
      display: flex !important;
      justify-content: center !important;
    }
    .tt-hero-visual-right {
      display: none !important;
    }
    .hero-barrel-art {
      display: none !important;
    }

    /* 2. TICKETS & SHOP SECTION */
    .tear-section {
      overflow-x: hidden !important;
      width: 100% !important;
    }
    .dark-gap {
      overflow-x: hidden !important;
      padding: 5.5rem 0 4rem !important;
    }
    .tear-header-area {
      padding-top: 6.5rem !important;
      padding-bottom: 3.5rem !important;
      padding-left: 1.25rem !important;
      padding-right: 1.25rem !important;
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
      justify-content: center !important;
      text-align: center !important;
    }
    .tt-section-header {
      margin: 0 auto !important;
      max-width: 580px !important;
      text-align: center !important;
    }
    .tt-section-title {
      font-size: 2.15rem !important;
      line-height: 1.15 !important;
      text-align: center !important;
      letter-spacing: 0.02em !important;
      margin-top: 1rem !important;
      margin-bottom: 0.75rem !important;
    }
    .tt-section-subtitle {
      font-size: 0.95rem !important;
      line-height: 1.5 !important;
      max-width: 90% !important;
      margin: 0 auto !important;
      text-align: center !important;
      color: rgba(23, 22, 20, 0.82) !important;
    }
    .shop-filters-container {
      display: none !important;
    }
    .mobile-filter-dropdowns {
      display: flex !important;
      flex-direction: column !important;
      gap: 0.75rem !important;
      margin-top: 0.5rem !important;
      margin-bottom: 2rem !important;
      padding: 1.15rem !important;
      background: rgba(15, 23, 42, 0.75) !important;
      border: 1.5px solid rgba(202, 172, 142, 0.4) !important;
      border-radius: 8px !important;
      box-sizing: border-box !important;
      width: 100% !important;
      position: relative !important;
      z-index: 50 !important;
    }
    .custom-dropdown {
      position: relative !important;
      width: 100% !important;
    }
    .custom-dropdown.is-open {
      z-index: 9999 !important;
    }
    .custom-dd-btn {
      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      width: 100% !important;
      height: 48px !important;
      padding: 0 1rem !important;
      background-color: #FAF7F2 !important;
      border: 2px solid #1D1C1A !important;
      border-radius: 6px !important;
      box-shadow: 2px 2px 0px #1D1C1A !important;
      cursor: pointer !important;
      text-align: left !important;
      box-sizing: border-box !important;
      transition: all 0.2s ease !important;
    }
    .custom-dd-label-group {
      display: flex !important;
      align-items: center !important;
      gap: 0.5rem !important;
      font-family: var(--font-sans) !important;
      font-size: 0.88rem !important;
      min-width: 0 !important;
    }
    .custom-dd-prefix {
      font-weight: 800 !important;
      color: #8C7853 !important;
      font-size: 0.8rem !important;
      text-transform: uppercase !important;
      flex-shrink: 0 !important;
    }
    .custom-dd-selected-text {
      font-weight: 700 !important;
      color: #171614 !important;
      white-space: nowrap !important;
      overflow: hidden !important;
      text-overflow: ellipsis !important;
    }
    .custom-dd-chevron {
      color: #171614 !important;
      transition: transform 0.25s cubic-bezier(0.16, 1, 0.3, 1) !important;
      flex-shrink: 0 !important;
    }
    .custom-dropdown.is-open .custom-dd-chevron {
      transform: rotate(180deg) !important;
    }
    .custom-dropdown.is-open .custom-dd-btn {
      border-color: #1E3A8A !important;
      box-shadow: 3px 3px 0px #1E3A8A !important;
    }
    .custom-dd-menu {
      display: none !important;
      position: absolute !important;
      top: calc(100% + 4px) !important;
      left: 0 !important;
      right: 0 !important;
      width: 100% !important;
      max-height: 280px !important;
      overflow-y: auto !important;
      background: #FAF7F2 !important;
      border: 2px solid #1D1C1A !important;
      border-radius: 6px !important;
      box-shadow: 0 12px 28px rgba(0, 0, 0, 0.4) !important;
      z-index: 999999 !important;
      box-sizing: border-box !important;
      padding: 0.25rem 0 !important;
    }
    .custom-dropdown.is-open .custom-dd-menu {
      display: block !important;
    }
    .custom-dd-item {
      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      padding: 0.75rem 1rem !important;
      font-family: var(--font-sans) !important;
      font-size: 0.88rem !important;
      font-weight: 700 !important;
      color: #171614 !important;
      cursor: pointer !important;
      transition: background 0.15s ease !important;
      border-bottom: 1px solid rgba(0, 0, 0, 0.06) !important;
    }
    .custom-dd-item:last-child {
      border-bottom: none !important;
    }
    .custom-dd-item:hover {
      background-color: #F0E8DC !important;
    }
    .custom-dd-item.active {
      background-color: #E2D5B8 !important;
      color: #171614 !important;
      font-weight: 800 !important;
    }
    .custom-dd-item .check-icon {
      display: none !important;
    }
    .custom-dd-item.active .check-icon {
      display: block !important;
      stroke: #1E3A8A !important;
    }

    /* Vertical Ticket Stack on Mobile */
    .shop-grid {
      display: flex !important;
      flex-direction: column !important;
      width: 100% !important;
    }
    .tickets-list {
      display: flex !important;
      flex-direction: column !important;
      gap: 1.25rem !important;
      width: 100% !important;
      overflow: visible !important;
      overflow-x: visible !important;
      overflow-y: visible !important;
      padding: 0 !important;
      margin: 0 !important;
      scroll-snap-type: none !important;
    }
    .tickets-list::-webkit-scrollbar {
      display: none !important;
    }
    .tickets-list > .ticket-stub {
      flex: none !important;
      width: 100% !important;
      max-width: 100% !important;
      margin: 0 0 1.25rem 0 !important;
    }
    .checkout-sidebar-container {
      display: none !important;
    }

    /* 3. PHOTO MARQUEE COMPACT FILMSTRIP */
    .marquee-section {
      padding: 3rem 0 !important;
    }
    .horizontal-marquee-container {
      gap: 0.5rem !important;
    }
    .marquee-photo-card {
      width: 190px !important;
      height: 130px !important;
      padding: 3px !important;
    }
    .photo-mask-frame {
      border-radius: 4px !important;
    }

    /* 4. TIMELINE SECTION POLISH */
    .timeline-scroll-section {
      padding: 3.5rem 0 !important;
    }
    .timeline-title {
      font-size: 2rem !important;
      line-height: 1.15 !important;
    }
    .timeline-intro-text {
      font-size: 0.9rem !important;
      line-height: 1.5 !important;
      margin-bottom: 1.5rem !important;
    }
    .timeline-track-viewport {
      padding: 0 1rem 1.5rem !important;
    }
    .timeline-track {
      gap: 1rem !important;
    }
    .timeline-card {
      width: 82vw !important;
      max-width: 310px !important;
      height: auto !important;
      min-height: 360px !important;
      padding: 1.25rem 1rem !important;
      border-radius: 8px !important;
      border: 2px solid var(--text-charcoal) !important;
      box-shadow: 4px 4px 0px var(--text-charcoal) !important;
    }
    .card-year {
      font-size: 2.25rem !important;
    }
    .card-title {
      font-size: 1.2rem !important;
      margin: 0.35rem 0 0.5rem !important;
    }
    .card-desc {
      font-size: 0.85rem !important;
      line-height: 1.45 !important;
    }
    .card-media-frame {
      height: 130px !important;
      margin-top: 0.75rem !important;
    }
    .timeline-axis-wrap {
      display: none !important;
    }

    /* 5. SPLIT FEATURES & PILLARS */
    .split-features-section {
      padding: 3rem 0 !important;
    }
    .split-layout {
      gap: 1.75rem !important;
      margin-bottom: 3.5rem !important;
    }
    .visual-placeholder img {
      height: 220px !important;
    }
    .tt-split-title {
      font-size: 1.5rem !important;
    }

    /* 6. HERITAGE STATS */
    .heritage-bar {
      padding: 2.25rem 0 !important;
    }
    .heritage-grid {
      display: grid !important;
      grid-template-columns: repeat(3, 1fr) !important;
      gap: 0.5rem !important;
      align-items: start !important;
      text-align: center !important;
    }
    .heritage-stat {
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
      gap: 0.25rem !important;
    }
    .stat-icon-wrapper {
      width: 46px !important;
      height: 46px !important;
      margin-bottom: 0.15rem !important;
    }
    .stat-icon-wrapper svg {
      width: 22px !important;
      height: 22px !important;
    }
    .stat-val {
      font-size: 1.35rem !important;
      line-height: 1.1 !important;
    }
    .stat-label {
      font-size: 0.62rem !important;
      letter-spacing: 0.04em !important;
      line-height: 1.25 !important;
    }

    /* 7. CHECKOUT BOTTOM SHEET */
    .checkout-modal-overlay {
      align-items: flex-end !important;
      padding: 0 !important;
    }
    .checkout-modal-card {
      max-width: 100% !important;
      width: 100% !important;
      border-radius: 16px 16px 0 0 !important;
      max-height: 90vh !important;
      overflow-y: auto !important;
      padding: 1.5rem 1.25rem !important;
      margin: 0 !important;
    }
  }



  /* Modal Overlay CSS duplicated from before */
  .checkout-modal-overlay { 
    position: fixed; 
    top: 0; 
    left: 0; 
    width: 100%; 
    height: 100%; 
    background-color: rgba(29, 28, 26, 0.75); 
    backdrop-filter: blur(5px); 
    z-index: 1000; 
    display: flex; 
    align-items: flex-start; 
    justify-content: center; 
    padding: 2rem 1rem; 
    overflow-y: auto; 
    animation: fadeIn 0.3s ease-out; 
  }
  .checkout-modal-card { 
    background-color: var(--bg-paper-card); 
    border: 2px solid var(--text-charcoal); 
    border-radius: 12px; 
    width: 100%; 
    max-width: 860px; 
    padding: 2rem; 
    position: relative; 
    box-shadow: 0 30px 70px rgba(0,0,0,0.5); 
    transition: max-width 0.3s; 
    margin: auto;
  }
  
  /* 2-Column Modal Layout */
  .modal-two-columns {
    display: flex;
    gap: 1.5rem;
    margin-top: 1.25rem;
  }
  .modal-form-side {
    flex: 1.2;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    min-width: 0;
  }
  .modal-summary-side {
    flex: 1;
    background-color: rgba(30, 58, 138, 0.03);
    border: 1px solid rgba(140, 110, 70, 0.25);
    border-radius: 8px;
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-width: 0;
  }
  .modal-summary-title {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--success-green);
    margin-bottom: 0.5rem;
    border-bottom: 1px dashed rgba(140, 110, 70, 0.25);
    padding-bottom: 0.25rem;
  }
  .modal-summary-items-list {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    color: var(--text-charcoal);
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    max-height: 120px;
    overflow-y: auto;
    margin-bottom: 0.75rem;
    scrollbar-width: thin;
    scrollbar-color: rgba(30, 58, 138, 0.2) transparent;
  }
  .modal-summary-items-list::-webkit-scrollbar {
    width: 4px;
  }
  .modal-summary-items-list::-webkit-scrollbar-track {
    background: transparent;
  }
  .modal-summary-items-list::-webkit-scrollbar-thumb {
    background-color: rgba(30, 58, 138, 0.2);
    border-radius: 2px;
  }
  .modal-summary-item-row {
    display: flex;
    justify-content: space-between;
    gap: 0.5rem;
  }
  .modal-summary-calc {
    border-top: 1px solid rgba(140, 110, 70, 0.2);
    padding-top: 0.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    font-family: var(--font-sans);
    font-size: 0.8rem;
  }
  .modal-summary-total-row {
    display: flex;
    justify-content: space-between;
    font-weight: 800;
    font-size: 1.1rem;
    color: var(--text-charcoal);
    border-top: 1px dashed rgba(140, 110, 70, 0.25);
    padding-top: 0.5rem;
    margin-top: 0.25rem;
    margin-bottom: 1rem;
  }
  .modal-summary-total-row span {
    color: var(--success-green);
  }

  @media (max-width: 800px) {
    .checkout-modal-card {
      max-width: 520px;
      padding: 1.25rem;
    }
    .modal-two-columns {
      flex-direction: column;
      gap: 1.25rem;
    }
  }

  .modal-close-btn { position: absolute; top: 1rem; right: 1rem; font-size: 1.5rem; background: transparent; border: none; cursor: pointer; color: var(--text-muted); transition: color 0.2s; }
  .modal-close-btn:hover { color: var(--text-charcoal); }
  .modal-title { font-size: 1.5rem; color: var(--text-charcoal); text-align: center; margin-bottom: 0.25rem; }
  .modal-subtitle { font-family: var(--font-sans); font-size: 0.8rem; color: var(--text-muted); text-align: center; margin-bottom: 1rem; }
  .form-group { margin-bottom: 0.85rem; display: flex; flex-direction: column; gap: 0.3rem; }
  .form-label { font-family: var(--font-sans); font-weight: 700; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--text-charcoal); }
  .form-control { background-color: var(--bg-parchment); border: 1px solid var(--border-color-dark); padding: 0.6rem 0.85rem; border-radius: 6px; font-family: var(--font-sans); font-size: 0.9rem; color: var(--text-charcoal); transition: all 0.3s; }
  .form-control:focus { outline: none; border-color: var(--whisky-gold-dark); box-shadow: 0 0 8px rgba(197, 139, 57, 0.15); }
  .payment-method-section { margin-top: 1.25rem; padding-top: 1rem; border-top: 1px dashed var(--border-color); }
  .payment-method-title { display: block; font-family: var(--font-sans); font-weight: 700; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; color: var(--text-charcoal); margin-bottom: 0.5rem; }
  .payment-grids { display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem; margin-bottom: 1rem; }
  .pay-method-card { border: 1px solid var(--border-color-dark); padding: 0.6rem; border-radius: 6px; display: flex; align-items: center; justify-content: center; cursor: pointer; background-color: var(--bg-parchment); transition: all 0.2s; }
  .pay-method-card input[type="radio"] { display: none; }
  .pay-logo { font-family: var(--font-sans); font-weight: 800; font-size: 0.95rem; color: var(--text-muted); }
  .pay-method-card.active { border-color: var(--whisky-gold-dark); background-color: var(--whisky-gold-trans); }
  .pay-method-card.active .pay-logo { color: var(--whisky-gold-dark); }
  .modal-amount-row { display: flex; justify-content: space-between; align-items: center; font-family: var(--font-sans); font-size: 1.05rem; margin-top: 2rem; padding: 1rem 0; border-top: 1px solid var(--border-color-dark); border-bottom: 1px solid var(--border-color-dark); margin-bottom: 1.5rem; }
  .modal-amount-row strong { font-size: 1.45rem; color: var(--whisky-gold-dark); }
  .btn-block { width: 100%; padding: 1rem; }
  .spinner { width: 50px; height: 50px; border: 4px solid var(--bg-sand); border-top: 4px solid var(--whisky-gold); border-radius: 50%; margin: 0 auto 1.5rem; animation: spin 1s linear infinite; }
  @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
  .processing-title { font-size: 1.35rem; color: var(--text-charcoal); margin-bottom: 0.5rem; }
  .processing-desc { font-size: 0.95rem; color: var(--text-muted); }
  .success-ticket-stub { display: flex; flex-direction: column; }
  .success-header { text-align: center; padding: 0.5rem 0 0.5rem; }
  .success-icon { display: inline-flex; width: 48px; height: 48px; border-radius: 50%; background-color: rgba(30, 58, 138, 0.08); color: var(--success-green); align-items: center; justify-content: center; margin-bottom: 0.75rem; border: 2px solid var(--success-green); }
  .success-title { font-size: 1.5rem; color: var(--text-charcoal); margin-bottom: 0.1rem; }
  .success-subtitle { font-size: 0.85rem; color: var(--text-muted); }
  .success-body { padding-top: 0.75rem; }
  .ticket-summary-box { background-color: #FCFAF7; border: 1.5px solid #d5c28a; border-radius: 8px; padding: 1rem; margin-bottom: 0.75rem; display: flex; flex-direction: column; gap: 0.5rem; box-shadow: inset 0 2px 4px rgba(0,0,0,0.01); }
  .summary-item { display: flex; justify-content: space-between; font-family: var(--font-sans); font-size: 0.85rem; }
  .summary-item span { color: var(--text-muted); font-weight: 600; }
  .summary-item strong { color: var(--text-charcoal); font-weight: 700; }
  .summary-divider { border-top: 1px dashed rgba(140, 110, 70, 0.25); margin: 0.4rem 0; }
  .summary-items-header { font-family: var(--font-sans); font-size: 0.75rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em; color: var(--success-green); margin-bottom: 0.25rem; text-align: left; }
  .receipt-tickets-ul { font-family: var(--font-sans); font-size: 0.82rem; color: var(--text-charcoal); display: flex; flex-direction: column; gap: 0.35rem; text-align: left; }
  .receipt-ticket-li { display: flex; justify-content: space-between; padding-left: 0.75rem; position: relative; }
  .receipt-ticket-li::before { content: "•"; color: var(--whisky-gold-dark); position: absolute; left: 0; font-weight: bold; }
  .ticket-delivery-notice { font-size: 0.8rem; color: var(--text-muted); line-height: 1.5; text-align: center; margin-bottom: 1rem; }

  /* ====================================================
     VINTAGE HORIZONTAL TIMELINE STYLING
     ==================================================== */
  .timeline-scroll-section {
    position: relative;
    background-color: var(--bg-parchment);
    /* Calibrated height for smooth horizontal card scroll without dead scroll space */
    height: 185vh;
  }

  .timeline-sticky-viewport {
    position: sticky;
    top: 0;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    overflow: hidden;
    padding: 2rem 0;
  }

  .timeline-container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 4rem;
    display: grid;
    grid-template-columns: 380px 1fr;
    gap: 4rem;
    align-items: center;
    height: calc(100vh - 180px);
    width: 100%;
  }

  .timeline-left-header {
    display: flex;
    flex-direction: column;
    justify-content: center;
    z-index: 10;
  }

  .timeline-title {
    font-size: 3rem;
    color: var(--text-charcoal);
    line-height: 1.1;
    margin-bottom: 0.5rem;
  }

  .timeline-intro-text {
    font-size: 1.1rem;
    color: var(--text-muted);
    line-height: 1.6;
    margin-bottom: 2rem;
  }

  /* Left Era List styling */
  .timeline-era-list {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    margin-top: 1rem;
    border-left: 2px solid var(--border-color);
    padding-left: 1.5rem;
  }

  .era-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.9rem;
    color: var(--text-muted);
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    left: -25px; /* overlay the dot on the border line */
  }

  .era-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: var(--border-color-dark);
    border: 2px solid var(--bg-parchment);
    transition: all 0.3s ease;
    box-sizing: content-box;
  }

  .era-text {
    transition: transform 0.3s ease;
  }

  .era-item.active {
    color: var(--whisky-gold-dark);
  }

  .era-item.active .era-dot {
    background-color: var(--whisky-gold);
    transform: scale(1.5);
    box-shadow: 0 0 0 4px var(--whisky-gold-trans);
  }

  .era-item.active .era-text {
    transform: translateX(4px);
  }

  /* Right Side Scrolling Track */
  .timeline-track-viewport {
    overflow: hidden;
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
  }

  .timeline-track {
    cursor: grab;
    user-select: none;
    display: flex;
    gap: 3rem;
    padding: 2rem 0;
    will-change: transform;
    /* On desktop, translate is controlled by JS. We add a smooth transition for tick-button clicks */
    /* CSS transition disabled during scroll to prevent stutter */
  }

  .timeline-card {
    width: 400px;
    min-height: 550px;
    height: 100%;
    flex-shrink: 0;
    background-color: var(--bg-paper-card);
    border: 1px solid var(--border-color);
    border-radius: 12px;
    padding: 2rem 2.25rem;
    box-shadow: var(--shadow-paper);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    display: flex;
    flex-direction: column;
    gap: 1rem;
    box-sizing: border-box;
  }

  .timeline-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-hover);
    border-color: var(--border-color-dark);
  }

  .timeline-card.active {
    border-color: var(--whisky-gold);
    box-shadow: 0 15px 40px rgba(197, 139, 57, 0.1), var(--shadow-paper);
  }

  /* Special Highlighted Card for Jubilee */
  .timeline-card.highlighted-jubilee {
    background: linear-gradient(145deg, #1d1c1a 0%, #2f2a24 100%);
    border: 2px solid var(--whisky-gold);
    color: #fff;
  }

  .timeline-card.highlighted-jubilee:hover {
    box-shadow: 0 25px 55px rgba(197, 139, 57, 0.25), 0 10px 30px rgba(0,0,0,0.5);
  }

  .timeline-card.highlighted-jubilee .card-title {
    color: var(--whisky-gold-light);
  }

  .text-white-muted {
    color: rgba(250, 247, 242, 0.6) !important;
  }

  .card-year-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 0.75rem;
    flex-shrink: 0;
  }

  .timeline-card.highlighted-jubilee .card-year-row {
    border-color: rgba(222, 214, 201, 0.2);
  }

  .card-year {
    font-family: var(--font-display);
    font-size: 2.25rem;
    color: var(--whisky-gold-dark);
    line-height: 1;
  }

  .card-num {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 0.9rem;
    color: var(--text-muted);
    letter-spacing: 0.1em;
  }

  .card-title {
    font-family: var(--font-serif);
    font-weight: 700;
    font-size: 1.35rem;
    text-transform: none;
    letter-spacing: 0;
    color: var(--text-charcoal);
    min-height: 3.2rem;
    display: flex;
    align-items: flex-end;
    margin: 0;
    flex-shrink: 0;
  }

  .card-desc {
    font-size: 0.92rem;
    line-height: 1.55;
    color: var(--text-muted);
    margin: 0;
    flex: 1;
    overflow: visible;
  }

  .card-img-frame {
    width: 100%;
    height: 175px;
    flex-shrink: 0;
    margin-top: auto;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid var(--border-color);
    position: relative;
    /* Sepia vintage mask / look */
    filter: sepia(0.35) contrast(1.05) brightness(0.95);
    transition: filter 0.3s ease;
  }

  .timeline-card:hover .card-img-frame {
    filter: sepia(0) contrast(1) brightness(1);
  }

  .card-img-frame img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* Bottom Axis Wrap */
  .timeline-axis-wrap {
    width: 100%;
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 4rem;
    margin-top: 1.5rem;
    position: relative;
    z-index: 10;
  }

  .timeline-axis-line {
    width: 100%;
    height: 2px;
    background-color: var(--border-color);
    position: relative;
  }

  .timeline-axis-progress {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 0%;
    background-color: var(--whisky-gold);
    transition: width 0.3s ease-out;
  }

  .timeline-axis-ticks {
    display: flex;
    justify-content: space-between;
    margin-top: 0.75rem;
  }

  .axis-tick {
    background: transparent;
    border: none;
    cursor: pointer;
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.85rem;
    color: var(--text-muted);
    position: relative;
    padding: 0.25rem 0.5rem;
    transition: all 0.3s ease;
  }

  .axis-tick::before {
    content: '';
    position: absolute;
    top: -11px;
    left: 50%;
    transform: translateX(-50%);
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background-color: var(--border-color-dark);
    transition: all 0.3s ease;
  }

  .axis-tick:hover {
    color: var(--text-charcoal);
  }

  .axis-tick.active {
    color: var(--whisky-gold-dark);
    font-weight: 800;
  }

  .axis-tick.active::before {
    background-color: var(--whisky-gold);
    transform: translateX(-50%) scale(1.5);
    box-shadow: 0 0 0 3px var(--whisky-gold-trans);
  }

  /* ====================================================
     RESPONSIVE TIMELINE (MOBILE DEGRADATION)
     ==================================================== */
  @media (max-width: 1024px) {
    .timeline-scroll-section {
      height: auto !important; /* Disables the sticky scroll track depth */
      padding: 6rem 0;
    }

    .timeline-sticky-viewport {
      position: relative !important;
      height: auto !important;
      top: auto !important;
      padding: 0 !important;
    }

    .timeline-container {
      grid-template-columns: 1fr;
      gap: 3rem;
      height: auto !important;
      padding: 0 2rem;
    }

    .timeline-left-header {
      text-align: center;
      align-items: center;
    }

    .timeline-intro-text {
      max-width: 600px;
    }

    .timeline-era-list {
      display: none; /* Hide era list on mobile to save vertical space */
    }

    .timeline-track-viewport {
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      padding-bottom: 1.5rem;
      scrollbar-width: none; /* Hide standard scrollbar on Firefox */
    }

    .timeline-track-viewport::-webkit-scrollbar {
      display: none; /* Hide standard scrollbar on Chrome/Safari */
    }

    .timeline-track {
      padding: 1rem 0;
      gap: 1.5rem;
      /* On mobile we want natural touch swipe, no JS transform override */
      transform: none !important; 
      transition: none !important;
    }

    .timeline-card {
      width: 290px;
      height: 480px;
      scroll-snap-align: center;
      padding: 1.5rem;
    }

    .card-desc {
      min-height: auto;
    }

    .timeline-axis-wrap {
      padding: 0 2rem;
      margin-top: 1rem;
    }

    .axis-tick span {
      font-size: 0.75rem;
    }
  }

  /* Interactive Timetable Styles */
  .timetable-section {
    background: var(--bg-parchment);
    padding: 5rem 0;
    border-top: 1px solid rgba(140, 110, 70, 0.15);
  }
  .timetable-tabs {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 3rem;
  }
  .timetable-tab-btn {
    background-color: transparent;
    border: 2px solid rgba(140, 110, 70, 0.3);
    color: var(--text-charcoal);
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 700;
    padding: 0.6rem 1.5rem;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .timetable-tab-btn:hover {
    border-color: var(--success-green);
    background-color: rgba(30, 58, 138, 0.05);
  }
  .timetable-tab-btn.active {
    background-color: var(--success-green);
    border-color: var(--success-green);
    color: #FCFAF7;
  }
  .timetable-time-filter {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    justify-content: center;
    background: rgba(140, 110, 70, 0.08);
    padding: 0.4rem 1rem;
    border-radius: 30px;
    border: 1px solid rgba(140, 110, 70, 0.15);
  }
  .time-filter-label {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    margin-right: 0.35rem;
  }
  .tt-time-btn {
    padding: 0.4rem 0.9rem;
    font-family: var(--font-sans);
    font-size: 0.825rem;
    font-weight: 700;
    border-radius: 20px;
    border: 1px solid transparent;
    background: transparent;
    color: var(--text-charcoal);
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .tt-time-btn:hover {
    color: var(--success-green);
    background-color: rgba(30, 58, 138, 0.08);
  }
  .tt-time-btn.active {
    background-color: var(--success-green);
    border-color: var(--success-green);
    color: #FCFAF7;
    box-shadow: 0 2px 8px rgba(30, 58, 138, 0.25);
  }
  .timetable-pane {
    display: none;
    animation: fadeIn 0.4s ease-out;
  }
  .timetable-pane.active {
    display: block;
  }
  .timetable-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.5rem;
  }
  .tt-card {
    background: var(--bg-paper-card);
    border: 1px solid rgba(140, 110, 70, 0.2);
    border-radius: 8px;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0,0,0,0.02);
    position: relative;
    overflow: hidden;
  }
  .tt-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.08);
    border-color: var(--success-green);
  }
  .tt-card-time {
    font-family: var(--font-sans);
    font-weight: 800;
    font-size: 1.1rem;
    color: var(--success-green);
    margin-bottom: 0.25rem;
  }
  .tt-card-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }
  .tt-loc {
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
  }
  .tt-price {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 0.95rem;
    color: var(--success-green);
  }
  .tt-card-title {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--text-charcoal);
    margin-bottom: 0.5rem;
    line-height: 1.2;
  }
  .tt-card-desc {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    color: var(--text-muted);
    line-height: 1.45;
    margin-bottom: 1.5rem;
    flex-grow: 1;
  }
  .tt-book-btn {
    align-self: flex-start;
    padding: 0.5rem 1rem !important;
    font-size: 0.85rem !important;
  }
</style>
<script id="tickets-data-json" type="application/json" set:html={JSON.stringify(tickets)}></script>
<script>
  // FAQ Exclusive Accordion: Close other items when opening an item
  const faqDetailsList = document.querySelectorAll<HTMLDetailsElement>('.faq-item');
  faqDetailsList.forEach((detail) => {
    detail.addEventListener('toggle', () => {
      if (detail.open) {
        faqDetailsList.forEach((other) => {
          if (other !== detail && other.open) {
            other.removeAttribute('open');
          }
        });
      }
    });
  });

  // 1. Enforce 100% reliable background video autoplay
  const heroBgVideo = document.getElementById("hero-bg-video") as HTMLVideoElement;
  if (heroBgVideo) {
    heroBgVideo.muted = true;
    heroBgVideo.defaultMuted = true;
    heroBgVideo.playsInline = true;
    const tryPlay = () => {
      heroBgVideo.play().catch(() => {
        document.addEventListener('click', () => heroBgVideo.play(), { once: true });
        document.addEventListener('touchstart', () => heroBgVideo.play(), { once: true });
        document.addEventListener('scroll', () => heroBgVideo.play(), { once: true });
      });
    };
    tryPlay();
    window.addEventListener('load', tryPlay);
  }

  // 2. Safe Preloader Dismiss
  (function() {
    const pl = document.getElementById('hp-preloader');
    if (pl) {
      setTimeout(() => {
        pl.classList.add('fade-out');
        document.body.style.overflow = '';
      }, 2200);
      setTimeout(() => {
        pl.style.display = 'none';
      }, 2650);
    }
  })();

  // 3. Tab Triggers (Alles wat u moet weten)
  const tabTriggers = document.querySelectorAll('.tab-trigger-card');
  tabTriggers.forEach(tab => {
    tab.addEventListener('click', () => {
      tabTriggers.forEach(t => t.classList.remove('active'));
      tab.classList.add('active');
    });
  });

  // 4. Cart & Shop State
  const cart: Record<string, { title: string; price: number; qty: number; category: string; timeslot?: string; delivery?: string; guide?: string }> = {};
  const serviceFee = 4.50;
  const cartItemsContainer = document.getElementById("cart-items");
  const subtotalEl = document.getElementById("receipt-subtotal");
  const feeEl = document.getElementById("receipt-fee");
  const shippingRow = document.getElementById("shipping-row");
  const shippingEl = document.getElementById("receipt-shipping");
  const totalEl = document.getElementById("receipt-total");
  const checkoutBtn = document.getElementById("checkout-trigger-btn") as HTMLButtonElement;

  function updateShop(syncSave: boolean = true) {
    let subtotal = 0;
    let totalItems = 0;
    let shippingActive = false;

    Object.keys(cart).forEach(id => {
      const item = cart[id];
      if (item && item.qty > 0) {
        subtotal += item.price * item.qty;
        totalItems += item.qty;
        if (item.category === "botteling" && item.delivery === "shipping") {
          shippingActive = true;
        }
      }
    });

    const activeShipping = shippingActive ? 12.50 : 0;
    const activeFee = totalItems > 0 ? serviceFee : 0;
    const finalTotal = subtotal + activeFee + activeShipping;

    if (cartItemsContainer) {
      if (totalItems === 0) {
        cartItemsContainer.innerHTML = '<p class="empty-cart-msg">Selecteer hiernaast uw ticket(s) om de bestelling te starten.</p>';
      } else {
        let listHtml = "";
        const entries = Object.keys(cart).filter(id => cart[id] && cart[id].qty > 0);
        
        entries.forEach((id, index) => {
          const item = cart[id];
          let displayTitle = item.title;
          
          if (item.category === "tram" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "masterclass" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "trail" && item.timeslot) {
            const guideSuffix = item.guide ? ` - Gids: ${item.guide}` : '';
            displayTitle = `${item.title} (${item.timeslot}${guideSuffix})`;
          } else if (item.category === "vatenmaken" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "botteling" && item.delivery) {
            const methodText = item.delivery === "shipping" ? "Verzenden" : "Ophalen";
            displayTitle = `${item.title} [${methodText}]`;
          }

          const isLast = index === entries.length - 1;
          listHtml += `
            <div class="cart-item-row" data-id="${id}" ${isLast ? 'style="border-bottom:none;"' : ''}>
              <div class="cart-item-info">
                <span class="cart-item-name">${displayTitle}</span>
                <div class="cart-item-controls">
                  <div class="cart-stepper">
                    <button type="button" class="sidebar-qty-btn sidebar-qty-minus" data-id="${id}" title="${item.qty === 1 ? 'Verwijderen uit winkelwagen' : 'Kaartje verminderen'}" aria-label="Verminderen">&minus;</button>
                    <span class="sidebar-qty-num">${item.qty}</span>
                    <button type="button" class="sidebar-qty-btn sidebar-qty-plus" data-id="${id}" title="Kaartje toevoegen" aria-label="Toevoegen">+</button>
                  </div>
                  <span class="cart-item-unit-price">&times; &euro;${item.price.toFixed(2)}</span>
                </div>
              </div>
              <span class="cart-item-price">&euro;${(item.price * item.qty).toFixed(2)}</span>
            </div>
          `;
        });
        
        cartItemsContainer.innerHTML = listHtml;
      }
    }
    
    if (subtotalEl) subtotalEl.innerHTML = `&euro;${subtotal.toFixed(2)}`;
    if (feeEl) feeEl.innerHTML = `&euro;${activeFee.toFixed(2)}`;
    if (shippingRow && shippingEl) {
      if (shippingActive) {
        shippingRow.style.display = "flex";
        shippingEl.innerHTML = `&euro;12.50`;
      } else {
        shippingRow.style.display = "none";
      }
    }
    if (totalEl) totalEl.innerHTML = `&euro;${finalTotal.toFixed(2)}`;
    if (checkoutBtn) checkoutBtn.disabled = totalItems === 0;

    if (syncSave) {
      saveCartToLocalStorage(subtotal, totalItems);
    }
  }

  // Delegated click listener for direct quantity adjustments (+ and -) in sidebar cart
  if (cartItemsContainer) {
    cartItemsContainer.addEventListener('click', (e) => {
      const target = e.target as HTMLElement;
      const btn = target.closest('.sidebar-qty-btn') as HTMLButtonElement;
      if (!btn) return;

      e.preventDefault();
      e.stopPropagation();

      const id = btn.getAttribute('data-id');
      if (!id || !cart[id]) return;

      const isPlus = btn.classList.contains('sidebar-qty-plus');
      if (isPlus) {
        cart[id].qty = Math.min(10, (cart[id].qty || 0) + 1);
      } else {
        if (cart[id].qty > 1) {
          cart[id].qty -= 1;
        } else {
          // If 1 and - is clicked: remove completely!
          cart[id].qty = 0;
        }
      }

      // Synchronize corresponding ticket-stub on the page
      const card = document.querySelector(`.ticket-stub[data-ticket-id="${id}"]`);
      if (card) {
        const qtyVal = card.querySelector('.qty-val');
        if (qtyVal) qtyVal.textContent = String(cart[id].qty);
        if (cart[id].qty > 0) card.classList.add('selected');
        else card.classList.remove('selected');
      }

      updateShop();
    });
  }

  function saveCartToLocalStorage(subtotalVal: number, totalItemsVal: number) {
    try {
      const activeItems = Object.keys(cart)
        .filter(id => cart[id] && cart[id].qty > 0)
        .map(id => {
          const item = cart[id];
          let displayTitle = item.title;
          if (item.category === "tram" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "masterclass" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "trail" && item.timeslot) {
            const guideSuffix = item.guide ? ` - Gids: ${item.guide}` : '';
            displayTitle = `${item.title} (${item.timeslot}${guideSuffix})`;
          } else if (item.category === "vatenmaken" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "botteling" && item.delivery) {
            const methodText = item.delivery === "shipping" ? "Verzenden" : "Ophalen";
            displayTitle = `${item.title} [${methodText}]`;
          }
          return {
            id,
            title: displayTitle,
            price: item.price,
            qty: item.qty,
            category: item.category || 'entree',
            date: item.date || '',
            time: item.time || ''
          };
        });

      localStorage.setItem("whiskyfest_cart", JSON.stringify({
        subtotal: subtotalVal,
        totalItems: totalItemsVal,
        items: activeItems,
        cartRaw: cart
      }));

      // Notify Header Cart and other components immediately
      window.dispatchEvent(new CustomEvent('cart-updated', { detail: { source: 'index' } }));
    } catch(e) {}
  }

  function restoreCartFromLocalStorage() {
    try {
      const raw = localStorage.getItem("whiskyfest_cart");
      if (raw) {
        const parsed = JSON.parse(raw);
        if (parsed.cartRaw) {
          for (const k in cart) delete cart[k];
          Object.assign(cart, parsed.cartRaw);
          document.querySelectorAll('.ticket-stub').forEach(card => {
            const id = card.getAttribute('data-ticket-id') || '';
            const qtyVal = card.querySelector('.qty-val');
            const itemQty = (cart[id] && cart[id].qty) ? cart[id].qty : 0;
            if (qtyVal) qtyVal.textContent = String(itemQty);
            if (itemQty > 0) card.classList.add('selected');
            else card.classList.remove('selected');
          });
          updateShop(false);
        } else if (parsed.items) {
          for (const k in cart) delete cart[k];
          parsed.items.forEach((it: any) => {
            cart[it.id] = { ...it };
          });
          document.querySelectorAll('.ticket-stub').forEach(card => {
            const id = card.getAttribute('data-ticket-id') || '';
            const qtyVal = card.querySelector('.qty-val');
            const itemQty = (cart[id] && cart[id].qty) ? cart[id].qty : 0;
            if (qtyVal) qtyVal.textContent = String(itemQty);
            if (itemQty > 0) card.classList.add('selected');
            else card.classList.remove('selected');
          });
          updateShop(false);
        }
      }
    } catch(e) {}
  }

  // Listen for cart changes from Header or other tabs
  window.addEventListener('cart-updated', (e: any) => {
    if (e.detail && e.detail.source === 'index') return;
    restoreCartFromLocalStorage();
  });
  window.addEventListener('storage', restoreCartFromLocalStorage);

  // 5. Ticket Filtering Logic
  const dayButtons = document.querySelectorAll('#day-filter-buttons .filter-tab-btn');
  const catButtons = document.querySelectorAll('#cat-filter-buttons .filter-tab-btn');
  const timeButtons = document.querySelectorAll('#time-filter-buttons .filter-tab-btn');
  const timeFilterRow = document.getElementById('time-filter-row');
  
  let activeDay = 'all';
  let activeCat = 'all';
  let activeTime = 'all';
  let visibleCount = 10;

  const showMoreContainer = document.getElementById('show-more-container');
  const showMoreBtn = document.getElementById('show-more-btn');

  function applyFilters() {
    if (timeFilterRow) {
      if (activeCat === 'botteling') {
        timeFilterRow.style.display = 'none';
        activeTime = 'all';
        timeButtons.forEach(b => {
          if (b.getAttribute('data-filter-time') === 'all') b.classList.add('active');
          else b.classList.remove('active');
        });
      } else {
        timeFilterRow.style.display = 'flex';
      }
    }

    const cards = document.querySelectorAll(".ticket-stub");
    const matchingCards: HTMLElement[] = [];

    cards.forEach(card => {
      const cardDay = card.getAttribute('data-day') || 'all';
      const cardCat = card.getAttribute('data-category') || 'entree';
      const cardTime = card.getAttribute('data-daypart') || 'all';

      const matchDay = (activeDay === 'all') || (cardDay === 'all') || (cardDay === activeDay);
      const matchCat = (activeCat === 'all') || (cardCat === activeCat);
      const matchTime = (activeTime === 'all') || (cardTime === 'all') || (cardTime === activeTime);

      if (matchDay && matchCat && matchTime) {
        card.classList.remove('hidden-by-filter');
        matchingCards.push(card as HTMLElement);
      } else {
        card.classList.add('hidden-by-filter');
        card.classList.remove('hidden-by-limit');
      }
    });

    const totalMatching = matchingCards.length;
    matchingCards.forEach((card, idx) => {
      if (idx < visibleCount) {
        card.classList.remove('hidden-by-limit');
      } else {
        card.classList.add('hidden-by-limit');
      }
    });

    if (totalMatching > visibleCount) {
      if (showMoreContainer) showMoreContainer.style.display = 'block';
      const remaining = totalMatching - visibleCount;
      const nextBatch = Math.min(10, remaining);
      if (showMoreBtn) showMoreBtn.textContent = `Laad 10 meer (${remaining} over) ↓`;
    } else {
      if (showMoreContainer) showMoreContainer.style.display = 'none';
    }
  }

  if (showMoreBtn) {
    showMoreBtn.addEventListener('click', () => {
      visibleCount += 10;
      applyFilters();
    });
  }

  dayButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      dayButtons.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      activeDay = btn.getAttribute('data-filter-day') || 'all';
      visibleCount = 10;
      applyFilters();
    });
  });

  catButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      catButtons.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      activeCat = btn.getAttribute('data-filter-cat') || 'all';
      visibleCount = 10;
      applyFilters();
    });
  });

  timeButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      timeButtons.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      activeTime = btn.getAttribute('data-filter-time') || 'all';
      visibleCount = 10;
      applyFilters();
    });
  });

  // Custom Styled Mobile Dropdowns Logic
  function setupCustomDropdown(dropdownId: string, onSelect: (val: string, label: string) => void) {
    const dd = document.getElementById(dropdownId);
    if (!dd) return;
    const btn = dd.querySelector('.custom-dd-btn') as HTMLButtonElement | null;
    const items = dd.querySelectorAll('.custom-dd-item');

    if (btn) {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const isOpen = dd.classList.contains('is-open');
        document.querySelectorAll('.custom-dropdown').forEach(d => d.classList.remove('is-open'));
        if (!isOpen) {
          dd.classList.add('is-open');
          btn.setAttribute('aria-expanded', 'true');
        } else {
          btn.setAttribute('aria-expanded', 'false');
        }
      });
    }

    items.forEach(item => {
      item.addEventListener('click', (e) => {
        e.stopPropagation();
        const val = item.getAttribute('data-value') || 'all';
        const span = item.querySelector('span');
        const label = span ? span.textContent || '' : '';
        
        items.forEach(it => it.classList.remove('active'));
        item.classList.add('active');
        dd.classList.remove('is-open');
        if (btn) btn.setAttribute('aria-expanded', 'false');

        onSelect(val, label);
      });
    });
  }

  // Close custom dropdowns on click outside
  document.addEventListener('click', () => {
    document.querySelectorAll('.custom-dropdown').forEach(d => {
      d.classList.remove('is-open');
      const b = d.querySelector('.custom-dd-btn');
      if (b) b.setAttribute('aria-expanded', 'false');
    });
  });

  const ddDayText = document.getElementById('dd-day-text');
  const ddCatText = document.getElementById('dd-cat-text');
  const ddTimeText = document.getElementById('dd-time-text');

  setupCustomDropdown('dd-filter-day', (val, label) => {
    activeDay = val;
    if (ddDayText) ddDayText.textContent = val === 'all' ? 'Alle Dagen' : label.split(' 2026')[0];
    dayButtons.forEach(b => {
      b.classList.toggle('active', (b.getAttribute('data-filter-day') || 'all') === activeDay);
    });
    visibleCount = 10;
    applyFilters();
  });

  setupCustomDropdown('dd-filter-cat', (val, label) => {
    activeCat = val;
    if (ddCatText) ddCatText.textContent = val === 'all' ? 'Alle Tickets & Masterclasses' : label;
    catButtons.forEach(b => {
      b.classList.toggle('active', (b.getAttribute('data-filter-cat') || 'all') === activeCat);
    });
    visibleCount = 10;
    applyFilters();
  });

  setupCustomDropdown('dd-filter-time', (val, label) => {
    activeTime = val;
    if (ddTimeText) ddTimeText.textContent = val === 'all' ? 'Alle Tijdsloten' : label.split(' (')[0];
    timeButtons.forEach(b => {
      b.classList.toggle('active', (b.getAttribute('data-filter-time') || 'all') === activeTime);
    });
    visibleCount = 10;
    applyFilters();
  });

  function updateCustomDropdownLabels() {
    const dayItem = document.querySelector(`#dd-filter-day .custom-dd-item[data-value="${activeDay}"]`);
    if (dayItem && ddDayText) {
      document.querySelectorAll('#dd-filter-day .custom-dd-item').forEach(i => i.classList.remove('active'));
      dayItem.classList.add('active');
      const s = dayItem.querySelector('span');
      ddDayText.textContent = activeDay === 'all' ? 'Alle Dagen' : (s ? s.textContent?.split(' 2026')[0] || '' : '');
    }

    const catItem = document.querySelector(`#dd-filter-cat .custom-dd-item[data-value="${activeCat}"]`);
    if (catItem && ddCatText) {
      document.querySelectorAll('#dd-filter-cat .custom-dd-item').forEach(i => i.classList.remove('active'));
      catItem.classList.add('active');
      const s = catItem.querySelector('span');
      ddCatText.textContent = activeCat === 'all' ? 'Alle Tickets & Masterclasses' : (s ? s.textContent || '' : '');
    }

    const timeItem = document.querySelector(`#dd-filter-time .custom-dd-item[data-value="${activeTime}"]`);
    if (timeItem && ddTimeText) {
      document.querySelectorAll('#dd-filter-time .custom-dd-item').forEach(i => i.classList.remove('active'));
      timeItem.classList.add('active');
      const s = timeItem.querySelector('span');
      ddTimeText.textContent = activeTime === 'all' ? 'Alle Tijdsloten' : (s ? s.textContent?.split(' (')[0] || '' : '');
    }
  }

  // Handle URL Query Params for deep linked categories & days (e.g. /?cat=masterclass#sessies)
  function handleUrlFilters() {
    const urlParams = new URLSearchParams(window.location.search);
    const catParam = urlParams.get('cat') || urlParams.get('type');
    const dayParam = urlParams.get('day');

    if (catParam) {
      const targetCatBtn = document.querySelector(`button[data-filter-cat="${catParam}"]`) as HTMLButtonElement;
      if (targetCatBtn) {
        catButtons.forEach(b => b.classList.remove('active'));
        targetCatBtn.classList.add('active');
        activeCat = catParam;
      }
    }

    if (dayParam) {
      const targetDayBtn = document.querySelector(`button[data-filter-day="${dayParam}"]`) as HTMLButtonElement;
      if (targetDayBtn) {
        dayButtons.forEach(b => b.classList.remove('active'));
        targetDayBtn.classList.add('active');
        activeDay = dayParam;
      }
    }

    applyFilters();
    updateCustomDropdownLabels();

    if (window.location.hash === '#sessies' || catParam || dayParam) {
      setTimeout(() => {
        const sessiesEl = document.getElementById('sessies');
        if (sessiesEl) {
          sessiesEl.scrollIntoView({ behavior: 'smooth' });
        }
      }, 150);
    }
  }

  handleUrlFilters();

  // Also listen to hashchange / popstate if navigating within page
  window.addEventListener('popstate', handleUrlFilters);

  // 6. Global Event-Delegated Quantity Click Listeners
  document.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    const btn = target.closest('.qty-btn') as HTMLButtonElement;
    if (!btn) return;

    e.preventDefault();
    const action = btn.getAttribute('data-action');
    const card = btn.closest('.ticket-stub') as HTMLElement;
    if (!card) return;

    const id = card.getAttribute('data-ticket-id') || '';
    const price = parseFloat(card.getAttribute('data-price') || '0');
    const title = card.getAttribute('data-title') || '';
    const category = card.getAttribute('data-category') || 'entree';
    const qtyVal = card.querySelector('.qty-val') as HTMLElement;

    if (!cart[id]) {
      cart[id] = { title, price, qty: 0, category };
    }

    const tramSelect = card.querySelector('.tram-select') as HTMLSelectElement;
    const introSelect = card.querySelector('.intro-select') as HTMLSelectElement;
    const bottlingSelect = card.querySelector('.bottling-select') as HTMLSelectElement;
    const trailTimeSelect = card.querySelector('.trail-time-select') as HTMLSelectElement;
    const trailGuideSelect = card.querySelector('.trail-guide-select') as HTMLSelectElement;
    const vatenmakenTimeSelect = card.querySelector('.vatenmaken-time-select') as HTMLSelectElement;

    if (tramSelect) cart[id].timeslot = tramSelect.value;
    if (introSelect) cart[id].timeslot = introSelect.value;
    if (bottlingSelect) cart[id].delivery = bottlingSelect.value;
    if (trailTimeSelect) cart[id].timeslot = trailTimeSelect.value;
    if (trailGuideSelect) cart[id].guide = trailGuideSelect.value;
    if (vatenmakenTimeSelect) cart[id].timeslot = vatenmakenTimeSelect.value;

    if (action === 'plus') {
      cart[id].qty = Math.min(10, (cart[id].qty || 0) + 1);
      if (qtyVal) qtyVal.textContent = String(cart[id].qty);
      card.classList.add('selected');
      updateShop();
    } else if (action === 'minus') {
      if (cart[id] && cart[id].qty > 0) {
        cart[id].qty -= 1;
        if (qtyVal) qtyVal.textContent = String(cart[id].qty);
        if (cart[id].qty === 0) card.classList.remove('selected');
        updateShop();
      }
    }
  });

  // 7. Delegated Dropdown Change Listeners
  document.addEventListener('change', (e) => {
    const target = e.target as HTMLElement;
    const select = target.closest('select') as HTMLSelectElement;
    if (!select) return;

    const card = select.closest('.ticket-stub');
    if (!card) return;

    const id = card.getAttribute('data-ticket-id') || '';
    if (!cart[id]) {
      const price = parseFloat(card.getAttribute('data-price') || '0');
      const title = card.getAttribute('data-title') || '';
      const category = card.getAttribute('data-category') || 'entree';
      cart[id] = { title, price, qty: 0, category };
    }

    if (select.classList.contains('tram-select') || select.classList.contains('intro-select') || select.classList.contains('trail-time-select') || select.classList.contains('vatenmaken-time-select')) {
      cart[id].timeslot = select.value;
      updateShop();
    } else if (select.classList.contains('trail-guide-select')) {
      cart[id].guide = select.value;
      updateShop();
    } else if (select.classList.contains('bottling-select')) {
      cart[id].delivery = select.value;
      updateShop();
    }
  });

  // Initial Filter & Cart Pass
  applyFilters();
  restoreCartFromLocalStorage();
  updateShop();
  document.addEventListener("DOMContentLoaded", () => {
    applyFilters();
    restoreCartFromLocalStorage();
    updateShop();
  });
  window.addEventListener("load", () => {
    applyFilters();
    restoreCartFromLocalStorage();
    updateShop();
  });

  // 8. Checkout Modal Flow
  const modalOverlay = document.getElementById("checkout-modal");
  const modalCloseX = document.getElementById("modal-close-x");
  const modalSuccessClose = document.getElementById("modal-success-close");
  const mockPaymentForm = document.getElementById("mock-payment-form");
  const modalStep1 = document.getElementById("modal-step-1");
  const modalStep2 = document.getElementById("modal-step-2");
  const modalStep3 = document.getElementById("modal-step-3");
  const modalTotalAmt = document.getElementById("modal-total-amt");
  const receiptSentEmail = document.getElementById("receipt-sent-email");
  const receiptTicketsList = document.getElementById("receipt-tickets-list");

  let promoDiscount = 0;
  let activePromoCode = "";

  function recalcModalTotal() {
    let subtotal = 0;
    let totalItems = 0;
    let shippingActive = false;

    Object.keys(cart).forEach(id => {
      const item = cart[id];
      if (item && item.qty > 0) {
        subtotal += item.price * item.qty;
        totalItems += item.qty;
        if (item.category === "botteling" && item.delivery === "shipping") {
          shippingActive = true;
        }
      }
    });

    const activeShipping = shippingActive ? 12.50 : 0;
    const activeFee = totalItems > 0 ? serviceFee : 0;
    const totalBeforeDiscount = subtotal + activeFee + activeShipping;
    const finalTotal = Math.max(0, totalBeforeDiscount - promoDiscount);

    if (modalTotalAmt) {
      modalTotalAmt.innerHTML = `&euro;${finalTotal.toFixed(2)}`;
    }

    const modalSummaryItems = document.getElementById("modal-summary-items");
    if (modalSummaryItems) {
      modalSummaryItems.innerHTML = "";
      Object.keys(cart).forEach(id => {
        const item = cart[id];
        if (item && item.qty > 0) {
          let displayTitle = item.title;
          if (item.category === "tram" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "masterclass" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "trail" && item.timeslot) {
            const guideSuffix = item.guide ? ` - Gids: ${item.guide}` : '';
            displayTitle = `${item.title} (${item.timeslot}${guideSuffix})`;
          } else if (item.category === "vatenmaken" && item.timeslot) {
            displayTitle = `${item.title} (${item.timeslot})`;
          } else if (item.category === "botteling" && item.delivery) {
            const methodText = item.delivery === "shipping" ? "Verzenden" : "Ophalen";
            displayTitle = `${item.title} [${methodText}]`;
          }

          const row = document.createElement("div");
          row.className = "modal-summary-item-row";
          row.innerHTML = `
            <span>${item.qty}x ${displayTitle}</span>
            <span>&euro;${(item.price * item.qty).toFixed(2)}</span>
          `;
          modalSummaryItems.appendChild(row);
        }
      });
    }

    const modalSummarySub = document.getElementById("modal-summary-subtotal");
    if (modalSummarySub) modalSummarySub.innerHTML = `&euro;${subtotal.toFixed(2)}`;

    const modalSummaryFee = document.getElementById("modal-summary-fee");
    if (modalSummaryFee) modalSummaryFee.innerHTML = `&euro;${activeFee.toFixed(2)}`;

    const modalSummaryShipping = document.getElementById("modal-summary-shipping");
    const modalSummaryShippingRow = document.getElementById("modal-summary-shipping-row");
    if (modalSummaryShippingRow && modalSummaryShipping) {
      if (shippingActive) {
        modalSummaryShippingRow.style.display = "flex";
        modalSummaryShipping.innerHTML = `&euro;12.50`;
      } else {
        modalSummaryShippingRow.style.display = "none";
      }
    }

    const modalSummaryDiscount = document.getElementById("modal-summary-discount");
    const modalSummaryDiscountRow = document.getElementById("modal-summary-discount-row");
    if (modalSummaryDiscountRow && modalSummaryDiscount) {
      if (promoDiscount > 0) {
        modalSummaryDiscountRow.style.display = "flex";
        modalSummaryDiscount.innerHTML = `- &euro;${promoDiscount.toFixed(2)}`;
      } else {
        modalSummaryDiscountRow.style.display = "none";
      }
    }

    return finalTotal;
  }

  const promoInput = document.getElementById("checkout-promo-code") as HTMLInputElement;
  const promoMsg = document.getElementById("promo-status-msg");
  const applyPromoBtn = document.getElementById("apply-promo-btn");

  if (applyPromoBtn && promoInput && promoMsg) {
    applyPromoBtn.addEventListener("click", () => {
      const code = promoInput.value.trim().toUpperCase();
      if (!code) {
        promoDiscount = 0;
        activePromoCode = "";
        promoMsg.style.display = "none";
        recalcModalTotal();
        return;
      }

      if (code.startsWith("WS-") || code === "IWS") {
        promoDiscount = 55.00;
        activePromoCode = code;
        promoMsg.style.display = "block";
        promoMsg.style.color = "var(--success-green)";
        promoMsg.textContent = "✓ Lidmaatschap gevalideerd: €55,00 korting toegepast.";
      } else if (code === "GIFT-50" || code === "CADEAU-50") {
        promoDiscount = 50.00;
        activePromoCode = code;
        promoMsg.style.display = "block";
        promoMsg.style.color = "var(--success-green)";
        promoMsg.textContent = "✓ Cadeaubon toegepast: €50,00 korting verwerkt.";
      } else {
        promoDiscount = 0;
        activePromoCode = "";
        promoMsg.style.display = "block";
        promoMsg.style.color = "#d9534f";
        promoMsg.textContent = "✗ Ongeldige code. Probeer het opnieuw.";
      }
      recalcModalTotal();
    });
  }

  if (checkoutBtn) {
    checkoutBtn.addEventListener("click", (e) => {
      e.preventDefault();
      handleCheckoutClick(e);
    });
  }

  if (modalCloseX) {
    modalCloseX.addEventListener("click", () => {
      if (modalOverlay) modalOverlay.style.display = "none";
      document.body.style.overflow = "";
    });
  }

  if (modalSuccessClose) {
    modalSuccessClose.addEventListener("click", () => {
      if (modalOverlay) modalOverlay.style.display = "none";
      document.body.style.overflow = "";
      if (modalStep3) modalStep3.style.display = "none";
      if (modalStep1) modalStep1.style.display = "block";
      Object.keys(cart).forEach(id => {
        cart[id].qty = 0;
        const qEl = document.getElementById(`qty-${id}`);
        if (qEl) qEl.textContent = "0";
      });
      updateShop();
    });
  }

  if (modalOverlay) {
    modalOverlay.addEventListener("click", (e) => {
      if (e.target === modalOverlay) {
        modalOverlay.style.display = "none";
        document.body.style.overflow = "";
      }
    });
  }

  const payCards = document.querySelectorAll(".pay-method-card");
  const bankGroup = document.getElementById("bank-group");
  payCards.forEach(card => {
    const radio = card.querySelector('input[type="radio"]') as HTMLInputElement;
    card.addEventListener("click", () => {
      payCards.forEach(c => c.classList.remove("active"));
      card.classList.add("active");
      if (radio) {
        radio.checked = true;
        if (bankGroup) {
          bankGroup.style.display = radio.value === "ideal" ? "flex" : "none";
        }
      }
    });
  });

  if (mockPaymentForm) {
    mockPaymentForm.addEventListener("submit", (e) => {
      e.preventDefault();
      const emailInput = document.getElementById("cust-email") as HTMLInputElement;
      const email = emailInput ? emailInput.value : "deondraijer@gmail.com";
      const nameInput = document.getElementById("cust-name") as HTMLInputElement;
      const name = nameInput ? nameInput.value : "Deon Draijer";
      const phoneInput = document.getElementById("cust-phone") as HTMLInputElement;
      const phone = phoneInput ? phoneInput.value : "+31 6 12345678";
      
      if (modalStep1) modalStep1.style.display = "none";
      if (modalStep2) modalStep2.style.display = "block";
      
      setTimeout(() => {
        try {
          if (modalStep2) modalStep2.style.display = "none";
          
          let orderList: string[] = [];
          let itemsOrdered: any[] = [];
          let resPrefix = "WF";

          if (activePromoCode.startsWith("WS-") || activePromoCode === "IWS") {
            resPrefix = "WS";
          }
          
          Object.keys(cart).forEach(id => {
            const item = cart[id];
            if (item && item.qty > 0) {
              let itemDisplayTitle = item.title;
              if (item.category === "tram" && item.timeslot) {
                itemDisplayTitle = `${item.title} (${item.timeslot.split(' - ')[1] || item.timeslot})`;
              } else if (item.category === "botteling" && item.delivery) {
                const method = item.delivery === "shipping" ? "Verzenden" : "Ophalen";
                itemDisplayTitle = `${item.title} [${method}]`;
              }
              
              orderList.push(`${itemDisplayTitle} (${item.qty}x)`);
              itemsOrdered.push({
                id: id,
                title: item.title,
                qty: item.qty,
                price: item.price,
                category: item.category,
                timeslot: item.timeslot || "",
                delivery: item.delivery || ""
              });
            }
          });
          
          const finalResNumber = `#${resPrefix}-2026-${Math.floor(10000 + Math.random() * 90000)}`;

          const orderData = {
            name: name,
            email: email,
            phone: phone,
            items: itemsOrdered,
            isMember: resPrefix === "WS",
            resNumber: finalResNumber,
            discountApplied: promoDiscount,
            totalPrice: recalcModalTotal()
          };
          localStorage.setItem('wf_last_order', JSON.stringify(orderData));

          const successResNumEl = document.querySelector("#modal-step-3 .ticket-summary-box strong");
          if (successResNumEl) successResNumEl.textContent = finalResNumber;
          
          if (receiptSentEmail) receiptSentEmail.textContent = email;
          if (receiptTicketsList) {
            receiptTicketsList.innerHTML = "";
            orderList.forEach(txt => {
              const div = document.createElement("div");
              div.className = "receipt-ticket-li";
              div.innerHTML = `<span>${txt}</span>`;
              receiptTicketsList.appendChild(div);
            });
          }
        } catch (err) {
          console.error("Order processing error:", err);
        } finally {
          if (modalStep1) modalStep1.style.display = "none";
          if (modalStep2) modalStep2.style.display = "none";
          if (modalStep3) modalStep3.style.display = "block";
        }
      }, 1500);
    });
  }

    // 9. Enhanced Interactive Timetable Logic (Day, Timepart & Category Type)
  let activeTtTab = 'vrijdag';
  let activeTtTime = 'all';
  let activeTtType = 'all';

  function determineCardCategory(card: HTMLElement) {
    const infoBtn = card.querySelector('[data-mc-id]');
    const bookBtn = card.querySelector('[data-target-stub]');
    const stubId = (infoBtn && infoBtn.getAttribute('data-mc-id')) || (bookBtn && bookBtn.getAttribute('data-target-stub')) || '';
    const titleText = (card.querySelector('.tt-card-title') && card.querySelector('.tt-card-title').textContent.toLowerCase()) || '';

    if (stubId.includes('session') || stubId.includes('vip') || titleText.includes('sessie') || titleText.includes('vip')) return 'entree';
    if (stubId.includes('tram') || titleText.includes('tram')) return 'tram';
    if (stubId.includes('trail') || titleText.includes('trail')) return 'trail';
    if (stubId.includes('vatenmaken') || stubId.includes('workshop') || titleText.includes('vatenmaken') || titleText.includes('workshop')) return 'vatenmaken';
    if (stubId.includes('botteling') || titleText.includes('botteling')) return 'botteling';
    return 'masterclass';
  }

  function filterTimetableCards() {
    const activePane = document.getElementById(`tt-pane-${activeTtTab}`);
    if (!activePane) return;
    const cards = activePane.querySelectorAll('.tt-card');

    cards.forEach(card => {
      const cardEl = card as HTMLElement;
      const cardTimepart = cardEl.getAttribute('data-timepart') || 'all';
      const category = cardEl.getAttribute('data-category') || determineCardCategory(cardEl);
      cardEl.setAttribute('data-category', category);

      const matchesTime = (activeTtTime === 'all' || cardTimepart === activeTtTime);
      const matchesType = (activeTtType === 'all' || category === activeTtType);

      if (matchesTime && matchesType) {
        cardEl.style.display = 'flex';
      } else {
        cardEl.style.display = 'none';
      }
    });
  }

  const ttTabs = document.querySelectorAll('.timetable-tab-btn');
  const ttPanes = document.querySelectorAll('.timetable-pane');
  const ttTimeBtns = document.querySelectorAll('.tt-time-btn');
  const ttTypeBtns = document.querySelectorAll('.tt-type-btn');

  ttTabs.forEach(tab => {
    tab.addEventListener('click', () => {
      ttTabs.forEach(t => t.classList.remove('active'));
      tab.classList.add('active');
      activeTtTab = tab.getAttribute('data-tt-tab') || 'vrijdag';
      ttPanes.forEach(pane => {
        if (pane.id === `tt-pane-${activeTtTab}`) {
          pane.classList.add('active');
        } else {
          pane.classList.remove('active');
        }
      });
      filterTimetableCards();
    });
  });

  ttTimeBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      ttTimeBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      activeTtTime = btn.getAttribute('data-tt-time') || 'all';
      filterTimetableCards();
    });
  });

  ttTypeBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      ttTypeBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      activeTtType = btn.getAttribute('data-tt-type') || 'all';
      filterTimetableCards();
    });
  });

  filterTimetableCards();

  const ttBookBtns = document.querySelectorAll('.tt-book-btn');
  ttBookBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const targetStubId = btn.getAttribute('data-target-stub') || "";
      const targetSelectVal = btn.getAttribute('data-target-select') || "";
      
      const ticketStub = document.querySelector(`.ticket-stub[data-ticket-id="${targetStubId}"]`);
      if (ticketStub) {
        if (targetSelectVal) {
          const select = ticketStub.querySelector('select') as HTMLSelectElement;
          if (select) {
            select.value = targetSelectVal;
            select.dispatchEvent(new Event('change', { bubbles: true }));
          }
        }
        const plusBtn = ticketStub.querySelector('.plus-btn') as HTMLButtonElement;
        if (plusBtn) {
          plusBtn.click();
          alert(`Toegevoegd aan winkelwagen: ${ticketStub.getAttribute('data-title')}!`);
          document.getElementById('ticket-gap')?.scrollIntoView({ behavior: 'smooth' });
        }
      }
    });
  });

  // 10. Scroll Fade-In
  const fadeGroups = document.querySelectorAll('.fade-in-group');
  if (fadeGroups.length > 0) {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('is-visible');
        }
      });
    }, { threshold: 0.01, rootMargin: '100px 0px 100px 0px' });
    
    fadeGroups.forEach(g => {
      const rect = g.getBoundingClientRect();
      if (rect.top < window.innerHeight + 200 && rect.bottom > -200) {
        g.classList.add('is-visible');
      }
      observer.observe(g);
    });
  }

  // 11. Interactive Horizontal Timeline
  const timelineSection = document.querySelector('.timeline-scroll-section') as HTMLElement;
  const timelineTrack = document.getElementById('timeline-track') as HTMLElement;
  const timelineViewport = document.querySelector('.timeline-track-viewport') as HTMLElement;
  const timelineCards = document.querySelectorAll('.timeline-card');
  const axisProgress = document.getElementById('axis-progress') as HTMLElement;
  const axisTicks = document.querySelectorAll('.axis-tick');
  const eraItems = document.querySelectorAll('.era-item');

  let activeIndex = 0;
  let isDesktop = window.innerWidth > 1024;
  let currentScrollTranslate = 0;
  let isDragging = false;
  let startX = 0;
  let dragStartTranslate = 0;

  window.addEventListener('resize', () => {
    isDesktop = window.innerWidth > 1024;
    if (!isDesktop && timelineTrack) {
      timelineTrack.style.transform = 'none';
    }
  });

  function getTimelineScrollData() {
    if (!timelineTrack) return { maxTranslate: 0, trackWidth: 0, viewportWidth: 0 };
    const trackWidth = timelineTrack.scrollWidth;
    const viewportWidth = timelineTrack.parentElement?.clientWidth || window.innerWidth;
    const maxTranslate = Math.max(0, trackWidth - viewportWidth + 60);
    return { maxTranslate, trackWidth, viewportWidth };
  }

  function setTimelinePosition(fraction: number, smooth: boolean = false) {
    if (!timelineTrack) return;
    const { maxTranslate } = getTimelineScrollData();
    const clampedFraction = Math.max(0, Math.min(1, fraction));
    currentScrollTranslate = clampedFraction * maxTranslate;

    if (smooth) {
      timelineTrack.style.transition = 'transform 0.4s cubic-bezier(0.16, 1, 0.3, 1)';
      setTimeout(() => {
        if (timelineTrack) timelineTrack.style.transition = 'none';
      }, 450);
    } else {
      timelineTrack.style.transition = 'none';
    }

    timelineTrack.style.transform = `translateX(-${currentScrollTranslate}px)`;

    if (axisProgress) {
      axisProgress.style.width = `${clampedFraction * 100}%`;
    }

    const cardCount = timelineCards.length;
    const activeIdx = Math.min(cardCount - 1, Math.round(clampedFraction * (cardCount - 1)));
    updateActiveStates(activeIdx);
  }

  function updateTimelineScroll() {
    if (!timelineSection || !timelineTrack || !isDesktop || isDragging) return;

    const rect = timelineSection.getBoundingClientRect();
    const sectionHeight = timelineSection.scrollHeight;
    const viewHeight = window.innerHeight;
    const scrollDistance = sectionHeight - viewHeight;
    const scrolledAmount = -rect.top;
    const fraction = scrolledAmount / scrollDistance;

    setTimelinePosition(fraction, false);
  }

  function updateActiveStates(index: number) {
    if (index === activeIndex) return;
    activeIndex = index;

    timelineCards.forEach((card, idx) => {
      if (idx === index) card.classList.add('active');
      else card.classList.remove('active');
    });

    axisTicks.forEach((tick, idx) => {
      if (idx === index) tick.classList.add('active');
      else tick.classList.remove('active');
    });

    const targets = Array.from(eraItems).map(e => parseInt(e.getAttribute('data-target') || '0', 10)).sort((a, b) => a - b);
    let activeEra = 0;
    for (const t of targets) {
      if (index >= t) activeEra = t;
    }

    eraItems.forEach((era) => {
      const targetIdx = parseInt(era.getAttribute('data-target') || '0', 10);
      if (targetIdx === activeEra) era.classList.add('active');
      else era.classList.remove('active');
    });
  }

  window.addEventListener('scroll', updateTimelineScroll, { passive: true });
  window.addEventListener('load', updateTimelineScroll);

  axisTicks.forEach((tick, tickIdx) => {
    tick.addEventListener('click', () => {
      const targetIndex = parseInt(tick.getAttribute('data-index') || String(tickIdx), 10);
      const cardCount = timelineCards.length;
      const targetFraction = targetIndex / (cardCount - 1);

      if (isDesktop && timelineSection) {
        const sectionTop = timelineSection.offsetTop;
        const sectionHeight = timelineSection.scrollHeight;
        const viewHeight = window.innerHeight;
        const scrollDistance = sectionHeight - viewHeight;
        const scrollTarget = sectionTop + (targetFraction * scrollDistance);

        window.scrollTo({ top: scrollTarget, behavior: 'smooth' });
        setTimelinePosition(targetFraction, true);
      } else {
        const card = timelineCards[targetIndex] as HTMLElement;
        if (timelineViewport && card) {
          timelineViewport.scrollTo({
            left: card.offsetLeft - 20,
            behavior: 'smooth'
          });
          updateActiveStates(targetIndex);
        }
      }
    });
  });

  eraItems.forEach(era => {
    era.addEventListener('click', () => {
      const targetIndex = parseInt(era.getAttribute('data-target') || '0', 10);
      const tickBtn = document.querySelector(`.axis-tick[data-index="${targetIndex}"]`) as HTMLButtonElement;
      if (tickBtn) tickBtn.click();
    });
  });

  if (timelineViewport && timelineTrack) {
    timelineViewport.addEventListener('mousedown', (e) => {
      if (!isDesktop) return;
      isDragging = true;
      startX = e.pageX;
      dragStartTranslate = currentScrollTranslate;
      timelineTrack.style.cursor = 'grabbing';
    });

    window.addEventListener('mousemove', (e) => {
      if (!isDragging) return;
      e.preventDefault();
      const dx = e.pageX - startX;
      const { maxTranslate } = getTimelineScrollData();
      const newTranslate = Math.max(0, Math.min(maxTranslate, dragStartTranslate - dx));
      const fraction = maxTranslate > 0 ? newTranslate / maxTranslate : 0;
      setTimelinePosition(fraction, false);
    });

    window.addEventListener('mouseup', () => {
      if (isDragging) {
        isDragging = false;
        if (timelineTrack) timelineTrack.style.cursor = 'grab';
      }
    });

    timelineViewport.addEventListener('wheel', (e) => {
      if (!isDesktop) return;
      const delta = Math.abs(e.deltaX) > Math.abs(e.deltaY) ? e.deltaX : e.deltaY;
      if (Math.abs(delta) > 5) {
        const { maxTranslate } = getTimelineScrollData();
        const newTranslate = Math.max(0, Math.min(maxTranslate, currentScrollTranslate + delta * 1.5));
        const fraction = maxTranslate > 0 ? newTranslate / maxTranslate : 0;
        setTimelinePosition(fraction, false);
      }
    }, { passive: true });
  }

  // 12. Aftermovie Video Player Logic
  const aftermoviePlayBtn = document.getElementById('aftermovie-play-btn');
  const videoOverlay = document.getElementById('video-overlay');
  const aftermovieVideo = document.getElementById('aftermovie-video') as HTMLVideoElement;

  if (aftermoviePlayBtn && videoOverlay && aftermovieVideo) {
    const startAftermovie = () => {
      videoOverlay.style.opacity = '0';
      videoOverlay.style.pointerEvents = 'none';
      aftermovieVideo.controls = true;
      aftermovieVideo.play();
    };
    aftermoviePlayBtn.addEventListener('click', startAftermovie);
    videoOverlay.addEventListener('click', startAftermovie);
  }

  
  // ==========================================
  // MASTERCLASSES INFO & UPSELL POPUP CONTROLLER
  // ==========================================
  const masterclassesList = [
  {
    "id": "intro-tickets",
    "title": "Whisky Introductie",
    "subtitle": "Basis Nosing & Tasting voor Beginners",
    "price": "€ 7,50",
    "duration": "40 minuten",
    "location": "De Oude Vismijn - Spaarkamer",
    "ambassador": {
      "name": "Valentijn Klop",
      "title": "Certified Whisky Educator & Sommelier",
      "bio": "Valentijn neemt beginnende liefhebbers mee op een toegankelijke reis langs smaakprofielen, distillatietechnieken en vatenrijping."
    },
    "tastingLineup": [
      "Lowland Light & Floral Single Malt (10 Y.O.)",
      "Speyside Rich Orchard Fruit Single Malt (12 Y.O.)"
    ],
    "description": "De perfecte start van uw festivaldag! Leer de basisprincipes van het proeven, het herkennen van aroma's en hoe u het maximale uit uw festivalbezoek haalt."
  },
  {
    "id": "macallan-exclusive",
    "title": "Macallan Exclusive Experience",
    "subtitle": "Exclusieve Sherry Cask Single Malts",
    "price": "€ 77,50",
    "duration": "60 minuten",
    "location": "De Oude Vismijn Gent (Gentse Toren)",
    "ambassador": {
      "name": "Gerard Spaans",
      "title": "Master of Malt & Senior Brand Ambassador",
      "bio": "Al meer dan 20 jaar dé autoriteit op het gebied van Schotse single malts en zeldzame Macallan bottelingen."
    },
    "tastingLineup": [
      "The Macallan 12 Y.O. Double Cask",
      "The Macallan 15 Y.O. Double Cask",
      "The Macallan 18 Y.O. Sherry Oak Cask (Iconic)",
      "The Macallan Rare Cask Release 2025/2026",
      "Secret Distillery Cask Sample (Exclusive)"
    ],
    "description": "Een ongeëvenaarde proeverij van 's werelds meest begeerde distilleerderij. Inclusief beklimming van de Gentse Toren."
  },
  {
    "id": "session-mc-highland-park",
    "title": "Highland Park: Orkney Viking Soul",
    "subtitle": "Heidehoning & Subtiele Turf",
    "price": "€ 22,50",
    "duration": "45 minuten",
    "location": "De Oude Vismijn - Telkamer",
    "ambassador": {
      "name": "Martin Markvardsen",
      "title": "Global Brand Ambassador Highland Park",
      "bio": "Rechtstreeks overgevlogen uit Kirkwall, Orkney. Martin deelt de eeuwenoude tradities van de noordelijkste Schotse distilleerderij."
    },
    "tastingLineup": [
      "Highland Park 12 Y.O. Viking Honour",
      "Highland Park 15 Y.O. Viking Heart (Keramiek)",
      "Highland Park 18 Y.O. Viking Pride",
      "Highland Park Cask Strength Release No. 5"
    ],
    "description": "Proef het unieke samenspel van aromatische heideturf, Oloroso sherryvaten en het zilte noordelijke zeeklimaat van de Orkney eilanden."
  },
  {
    "id": "session-mc-glenfiddich",
    "title": "Glenfiddich & The Balvenie Showcase",
    "subtitle": "Speyside Icons & Ambachtelijk Kuiperijwerk",
    "price": "€ 25,00",
    "duration": "45 minuten",
    "location": "De Oude Vismijn - Foyer",
    "ambassador": {
      "name": "Tony van Rooijen",
      "title": "William Grant & Sons Ambassador",
      "bio": "Gepassioneerd verhalenverteller over traditioneel vakmanschap, vloermouterijen en ambachtelijk kuiperijwerk in Dufftown."
    },
    "tastingLineup": [
      "Glenfiddich 15 Y.O. Solera Reserve",
      "Glenfiddich 18 Y.O. Small Batch Reserve",
      "The Balvenie 12 Y.O. DoubleWood",
      "The Balvenie 14 Y.O. Caribbean Cask",
      "The Balvenie 16 Y.O. French Oak"
    ],
    "description": "Vergelijk twee legendarische zuster-distilleerderijen uit Dufftown en ontdek hoe houtkeuze en Solera-rijping het smaakprofiel transformeren."
  }
]
;
  const denHaagTicketsData: any[] = JSON.parse(document.getElementById("tickets-data-json")?.textContent || "[]");
  const allAddonsList: any[] = denHaagTicketsData.filter(t => t.category !== 'entree');
  let activeMcId: string | null = null;

  const mcModal = document.getElementById('masterclass-info-modal');
  const mcModalClose = document.getElementById('mc-modal-close');
  const mcModalAddBtn = document.getElementById('mc-modal-add-btn');

  const upsellModal = document.getElementById('addon-upsell-modal');
  const upsellModalClose = document.getElementById('addon-upsell-close');
  const upsellModalCancel = document.getElementById('addon-upsell-cancel');
  const upsellModalProceed = document.getElementById('addon-upsell-proceed-btn');

  function openCheckoutModal() {
    promoDiscount = 0;
    activePromoCode = "";
    if (promoInput) promoInput.value = "";
    if (promoMsg) promoMsg.style.display = "none";
    
    recalcModalTotal();
    if (modalStep1) modalStep1.style.display = "block";
    if (modalStep2) modalStep2.style.display = "none";
    if (modalStep3) modalStep3.style.display = "none";
    if (modalOverlay) {
      modalOverlay.style.display = "flex";
      document.body.style.overflow = "hidden";
    }
  }

  let activeUpsellCategory = 'all';
  let ignoreUpsellDayFilter = false;

  function renderAddonUpsellList() {
    const listContainer = document.getElementById('addon-upsell-list');
    const dayNotice = document.getElementById('upsell-day-notice');
    const dayNoticeText = document.getElementById('upsell-day-notice-text');
    const toggleDaysBtn = document.getElementById('upsell-toggle-days-btn');
    if (!listContainer) return;

    // Detect if user selected a specific day for an entree ticket in cart
    const cartDays = new Set();
    Object.keys(cart).forEach(id => {
      if (cart[id] && cart[id].qty > 0) {
        const matchingTicket = denHaagTicketsData.find(t => t.id === id);
        if (matchingTicket && matchingTicket.day && matchingTicket.day !== 'all') {
          cartDays.add(matchingTicket.day);
        }
      }
    });

    if (dayNotice && dayNoticeText && toggleDaysBtn) {
      if (cartDays.size > 0) {
        dayNotice.style.display = 'flex';
        const formattedDays = [...cartDays].map((d: any) => d.charAt(0).toUpperCase() + d.slice(1)).join(', ');
        if (ignoreUpsellDayFilter) {
          dayNoticeText.innerHTML = `📅 U bekijkt momenteel <strong>alle festivaldagen</strong>`;
          toggleDaysBtn.textContent = `Filter alleen op uw festivaldag (${formattedDays})`;
        } else {
          dayNoticeText.innerHTML = `📅 Getoond voor uw festivaldag: <strong>${formattedDays}</strong>`;
          toggleDaysBtn.textContent = `Toon alle festivaldagen`;
        }
      } else {
        dayNotice.style.display = 'none';
      }
    }

    const filtered = allAddonsList.filter(item => {
      const matchCat = activeUpsellCategory === 'all' || item.category === activeUpsellCategory;
      const matchDay = ignoreUpsellDayFilter || cartDays.size === 0 || item.day === 'all' || cartDays.has(item.day);
      return matchCat && matchDay;
    });

    // Update total on proceed button
    let subtotal = 0;
    let totalItems = 0;
    let shippingActive = false;
    Object.keys(cart).forEach(id => {
      const it = cart[id];
      if (it && it.qty > 0) {
        subtotal += it.price * it.qty;
        totalItems += it.qty;
        if (it.category === "botteling" && it.delivery === "shipping") {
          shippingActive = true;
        }
      }
    });
    const activeShipping = shippingActive ? 12.50 : 0;
    const activeFee = totalItems > 0 ? serviceFee : 0;
    const finalTotal = subtotal + activeFee + activeShipping;
    if (upsellModalProceed) {
      upsellModalProceed.innerHTML = `Naar Afrekenen (€ ${finalTotal.toFixed(2).replace('.', ',')}) &rarr;`;
    }

    if (filtered.length === 0) {
      listContainer.innerHTML = '<p style="color:#777; font-size:0.9rem; text-align:center; padding: 2rem 0;">Geen add-ons gevonden voor deze categorie/dagselectie.</p>';
      return;
    }

    listContainer.innerHTML = filtered.map(item => {
      const currentQty = (cart[item.id] && cart[item.id].qty) || 0;
      const isAdded = currentQty > 0;
      const isSoldOut = item.isSoldOut;
      const catLabel = item.category === 'warehouse' ? 'Dada Rondleiding' : item.category === 'tram' ? 'Whisky Bootjes' : item.category === 'botteling' ? 'Botteling' : 'Masterclass';

      return `
        <div style="background: #FFFFFF; border: 1px solid #E6DDD0; border-radius: 8px; padding: 0.9rem 1.15rem; display: flex; justify-content: space-between; align-items: center; gap: 1rem; box-shadow: 0 2px 6px rgba(0, 0, 0, 0.03); ${isSoldOut ? 'opacity: 0.65;' : ''}">
          <div style="flex: 1; min-width: 0;">
            <div style="display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.2rem; flex-wrap: wrap;">
              <span style="font-size: 0.65rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.08em; padding: 0.15rem 0.45rem; border-radius: 3px; background: rgba(140, 110, 70, 0.12); color: #8A6831;">${catLabel}</span>
              <strong style="font-size: 0.95rem; color: #171614; line-height: 1.25;">${item.title}</strong>
            </div>
            <span style="font-size: 0.8rem; color: #665C54; display: block;">
              ${item.date ? item.date + ' • ' : ''}${item.time ? item.time + ' • ' : ''}${item.location || 'De Oude Vismijn Gent'}
            </span>
          </div>
          <div style="display: flex; align-items: center; gap: 0.85rem; flex-shrink: 0;">
            <strong style="color: #1E3A8A; font-size: 1.05rem; font-family: var(--font-sans, sans-serif); font-weight: 800; white-space: nowrap;">
              € ${item.price.toFixed(2).replace('.', ',')}
            </strong>
            ${isSoldOut ? `
              <span style="font-size: 0.75rem; font-weight: 800; color: #b91c1c; padding: 0.45rem 0.75rem; background: rgba(185, 28, 28, 0.1); border-radius: 4px; border: 1px dashed #b91c1c;">UITVERKOCHT</span>
            ` : isAdded ? `
              <div class="upsell-stepper" style="display: inline-flex; align-items: center; background: rgba(30, 58, 138, 0.08); border: 1.5px solid #1E3A8A; border-radius: 6px; padding: 2px; gap: 2px; user-select: none;">
                <button 
                  type="button" 
                  class="btn-upsell-qty-action btn-upsell-minus" 
                  data-upsell-mc-id="${item.id}" 
                  data-upsell-action="minus"
                  title="${currentQty === 1 ? 'Verwijderen uit winkelwagen' : 'Kaartje verminderen'}"
                  aria-label="Verminderen"
                  style="width: 26px; height: 26px; display: inline-flex; align-items: center; justify-content: center; background: transparent; border: none; border-radius: 4px; color: #1E3A8A; font-size: 15px; font-weight: 800; cursor: pointer; transition: all 0.15s;"
                >&minus;</button>
                <span style="font-family: var(--font-sans); font-weight: 800; font-size: 0.85rem; color: #1E3A8A; padding: 0 6px; min-width: 18px; text-align: center;">${currentQty}</span>
                <button 
                  type="button" 
                  class="btn-upsell-qty-action btn-upsell-plus" 
                  data-upsell-mc-id="${item.id}" 
                  data-upsell-action="plus"
                  title="Kaartje toevoegen"
                  aria-label="Toevoegen"
                  style="width: 26px; height: 26px; display: inline-flex; align-items: center; justify-content: center; background: transparent; border: none; border-radius: 4px; color: #1E3A8A; font-size: 15px; font-weight: 800; cursor: pointer; transition: all 0.15s;"
                >+</button>
              </div>
            ` : `
              <button 
                type="button" 
                class="btn-upsell-qty-action" 
                data-upsell-mc-id="${item.id}"
                data-upsell-action="plus"
                style="display: inline-flex; align-items: center; gap: 0.35rem; padding: 0.45rem 0.9rem; font-size: 0.8rem; font-weight: 800; border: 1.5px solid #1E3A8A; color: #1E3A8A; background: #FAF7F2; cursor: pointer; border-radius: 6px; transition: all 0.2s;"
              >
                + In Winkelmand
              </button>
            `}
          </div>
        </div>
      `;
    }).join('');
  }

  // Handle Day Filter toggle inside Upsell Modal
  document.addEventListener('click', function(e) {
    const toggleBtn = (e.target as HTMLElement).closest('#upsell-toggle-days-btn');
    if (!toggleBtn) return;
    e.preventDefault();
    ignoreUpsellDayFilter = !ignoreUpsellDayFilter;
    renderAddonUpsellList();
  });

  // Handle Category Filter clicks inside Upsell Modal
  document.addEventListener('click', function(e) {
    const filterBtn = (e.target as HTMLElement).closest('.upsell-filter-btn');
    if (!filterBtn) return;
    
    document.querySelectorAll('.upsell-filter-btn').forEach(b => {
      b.classList.remove('active');
      (b as HTMLElement).style.background = 'transparent';
      (b as HTMLElement).style.color = 'var(--text-charcoal)';
      (b as HTMLElement).style.borderColor = 'rgba(140, 110, 70, 0.3)';
    });

    filterBtn.classList.add('active');
    (filterBtn as HTMLElement).style.background = '#1E3A8A';
    (filterBtn as HTMLElement).style.color = '#FFFFFF';
    (filterBtn as HTMLElement).style.borderColor = '#1E3A8A';

    activeUpsellCategory = filterBtn.getAttribute('data-upsell-cat') || 'all';
    renderAddonUpsellList();
  });

  // Handle Upsell Stepper & Add-to-cart clicks (+ and -)
  document.addEventListener('click', function(e) {
    const btn = (e.target as HTMLElement).closest('.btn-upsell-qty-action');
    if (!btn) return;
    e.preventDefault();
    e.stopPropagation();

    const mcId = btn.getAttribute('data-upsell-mc-id');
    const action = btn.getAttribute('data-upsell-action') || 'plus';
    if (!mcId) return;

    const item = allAddonsList.find(m => m.id === mcId);
    if (!item) return;

    if (!cart[mcId]) {
      let defaultDelivery: string | undefined = undefined;
      if (item.category === 'botteling') defaultDelivery = 'pickup';

      cart[mcId] = {
        title: item.title,
        price: item.price,
        qty: 0,
        category: item.category,
        timeslot: item.time || undefined,
        delivery: defaultDelivery,
        date: item.date || '',
        time: item.time || ''
      };
    }

    if (action === 'plus') {
      cart[mcId].qty = Math.min(10, (cart[mcId].qty || 0) + 1);
    } else if (action === 'minus') {
      if (cart[mcId].qty > 1) {
        cart[mcId].qty -= 1;
      } else {
        // If 1 and minus clicked: remove completely!
        cart[mcId].qty = 0;
      }
    }

    // Synchronize ticket-stub card on the page if it exists
    const card = document.querySelector(`.ticket-stub[data-ticket-id="${mcId}"]`);
    if (card) {
      const qtyVal = card.querySelector('.qty-val');
      if (qtyVal) qtyVal.textContent = String(cart[mcId].qty);
      if (cart[mcId].qty > 0) card.classList.add('selected');
      else card.classList.remove('selected');
    }

    updateShop();
    renderAddonUpsellList();
  });

  function handleCheckoutClick(e: Event) {
    if (e && e.preventDefault) e.preventDefault();

    let totalItems = 0;
    Object.keys(cart).forEach(id => {
      if (cart[id] && cart[id].qty > 0) {
        totalItems += cart[id].qty;
      }
    });

    if (totalItems === 0) {
      alert('Uw winkelwagen is leeg. Selecteer eerst een ticket.');
      return;
    }

    // Always show upsell popup first when clicking Afrekenen
    if (upsellModal) {
      activeUpsellCategory = 'all';
      // Reset filter button UI
      document.querySelectorAll('.upsell-filter-btn').forEach((b, idx) => {
        if (idx === 0) {
          b.classList.add('active');
          (b as HTMLElement).style.background = '#1E3A8A';
          (b as HTMLElement).style.color = '#FFFFFF';
          (b as HTMLElement).style.borderColor = '#1E3A8A';
        } else {
          b.classList.remove('active');
          (b as HTMLElement).style.background = 'transparent';
          (b as HTMLElement).style.color = 'var(--text-charcoal)';
          (b as HTMLElement).style.borderColor = 'rgba(140, 110, 70, 0.3)';
        }
      });
      renderAddonUpsellList();
      upsellModal.style.display = 'flex';
      document.body.style.overflow = 'hidden';
    } else {
      openCheckoutModal();
    }
  }

  // Global delegated listener for ANY checkout button across the entire page
  document.addEventListener('click', function(e) {
    const target = (e.target as HTMLElement).closest('#checkout-btn, #checkout-trigger-btn, .btn-checkout, #header-dropdown-checkout-btn, #header-cart-checkout, [data-action="checkout"]');
    if (target) {
      e.preventDefault();
      handleCheckoutClick(e);
    }
  });

  if (upsellModalProceed) {
    upsellModalProceed.onclick = function() {
      if (upsellModal) upsellModal.style.display = 'none';
      openCheckoutModal();
    };
  }

  if (upsellModalCancel) {
    upsellModalCancel.onclick = function() {
      if (upsellModal) upsellModal.style.display = 'none';
      openCheckoutModal();
    };
  }

  if (upsellModalClose) {
    upsellModalClose.onclick = function() {
      if (upsellModal) upsellModal.style.display = 'none';
      document.body.style.overflow = 'auto';
    };
  }

  function openMasterclassModal(mcId: string) {
    activeMcId = mcId;

    const item = (denHaagTicketsData || []).find((t: any) => t.id === mcId);
    const ticketStubEl = document.querySelector(`.ticket-stub[data-ticket-id="${mcId}"]`);
    const domCategory = (item && item.category) || (ticketStubEl && ticketStubEl.getAttribute('data-category')) || '';
    const domTitle = (item && item.title) || (ticketStubEl && ticketStubEl.getAttribute('data-title')) || 'Festival Onderdeel';
    let numericPrice = item ? item.price : ((ticketStubEl && parseFloat(ticketStubEl.getAttribute('data-price') || '0')) || 0);

    let tag = 'FESTIVAL ONDERDEEL';
    let title = domTitle;
    let subtitle = item ? `${item.date} • ${item.time}` : 'Exclusieve festivalbeleving';
    let location = (item && item.location) || 'De Oude Vismijn Gent';
    let duration = '45 minuten';
    let description = (item && item.extra) || 'Ervaar een uniek festivalonderdeel tijdens het International Whisky Festival Gent.';
    let ambName = 'International Whisky Festival';
    let ambTitle = 'Expert & Begeleider';
    let ambBio = 'Ervaren team met passie voor whiskycultuur, vakmanschap en historische tradities.';
    let descHeading = 'Over deze Masterclass';
    let lineupHeading = 'Officiële Proeflijst (Inbegrepen Drams):';
    let lineup = [
      'Exclusieve Single Malt Proefdram #1',
      'Zeldzame Cask Strength Proefdram #2'
    ];

    if (mcId.includes('vip') || (item && item.title && item.title.toLowerCase().includes('vip'))) {
      tag = 'VIP FESTIVAL SESSIE';
      title = item?.title || 'VIP Sessie - Vrijdagmiddag';
      subtitle = `${item?.date || 'Vrijdag 2 Okt 2026'} • ${item?.time || '13:00 – 17:00 uur'}`;
      location = 'De Oude Vismijn Gent';
      duration = '4 uur exclusieve festivaltoegang';
      descHeading = 'Over het VIP Arrangement';
      description = 'De ultieme festivalbeleving voor de echte whiskyliefhebber. Geniet van 4 uur lang ongestoord proeven, vroege toegang vóór de reguliere opening, toegang tot exclusieve VIP masterclasses en verwennerij met luxe hapjes.';
      ambName = 'Festival Organisatie & VIP Concierge';
      ambTitle = 'VIP Host & Begeleiding';
      ambBio = 'Persoonlijke ontvangst en exclusieve service tijdens de officiële opening van het festivalweekend in Gent.';
      lineupHeading = 'Wat is inbegrepen bij uw VIP ticket:';
      lineup = [
        '12 gratis festival proefdrams (t.w.v. € 12,00)',
        'Officieel Glencairn festival proefglas',
        'Toegang tot alle exclusieve VIP Masterclasses in de kerk',
        'Luxe hapjes-vouchers (worsten- & bitterballenplateau)',
        'Officieel festivalboekje met beursplattegrond en alle standhouders'
      ];
    } else if (domCategory === 'entree' || mcId.startsWith('session-')) {
      tag = 'REGULIERE FESTIVAL SESSIE';
      title = item?.title || 'Reguliere Festival Entree';
      subtitle = `${item?.date || 'Festivaldag 2026'} • ${item?.time || '4 uur toegang'}`;
      location = 'De Oude Vismijn Gent';
      duration = '4 uur festivaltoegang';
      descHeading = 'Over deze Festivalsessie';
      description = 'Beleef de magie van het meest toonaangevende whiskyfestival van Nederland in de historische De Oude Vismijn van Gent. Ontdek meer dan 40 standhouders en honderden verschillende Schotse single malts, wereldwhisky’s en Nederlandse distilleerderijen.';
      ambName = 'International Whisky Festival Team';
      ambTitle = 'Festival Host';
      ambBio = 'Het officiële festivalteam staat klaar om u te verwelkomen in de historische ambiance van de De Oude Vismijn.';
      lineupHeading = 'Wat is inbegrepen bij uw entree:';
      lineup = [
        'Officieel Glencairn festival proefglas',
        'Talloze gratis te proeven drams op de beursvloer',
        'Compleet festivalboekje met alle standhouders en plattegrond',
        '4 uur onbeperkt toegang tot alle beurseilanden en stands'
      ];
    } else if (domCategory === 'warehouse' || domCategory === 'dada') {
      tag = 'DADA CHAPEL RONDLEIDING';
      subtitle = `${item?.date || ''} • Dada Chapel Distilleerderij`;
      location = 'Dada Chapel Distilleerderij (Gent)';
      duration = '45 minuten';
      descHeading = 'Over deze Dada Chapel Rondleiding';
      description = `Exclusieve rondleiding door de Dada Chapel Distilleerderij te Gent inclusief proeverij van hun bijzondere distillaten. ${item?.title || ''}`;
      ambName = 'Dada Chapel Distilleerder & Gids';
      ambTitle = 'Master Distiller';
      ambBio = 'Rondleiding en toelichting door de ambachtelijke distilleerders van Dada Chapel in het historische hart van Gent.';
      lineupHeading = 'Inbegrepen bij de rondleiding:';
      lineup = [
        'Rondleiding door de distilleerderij',
        'Uitleg over het unieke distillatieproces',
        'Exclusieve proeverij van Dada Chapel creaties'
      ];
    } else if (domCategory === 'boot' || domCategory === 'tram') {
      tag = 'GENTSE WHISKY BOOTJES';
      subtitle = `${item?.date || ''} • Vertrek: ${item?.time || ''}`;
      location = 'Aanlegsteiger direct aan De Oude Vismijn';
      duration = '50 minuten varende proeverij';
      descHeading = 'Over de Gentse Whisky Bootjes';
      description = `Geniet van een sfeervolle varende whiskyproeverij door de historische Gentse binnenwateren, vertrekkend vanaf De Oude Vismijn onder begeleiding van deskundige uitleg en topdrams.`;
      ambName = 'Whisky Bootjes Schipper & Gids';
      ambTitle = 'Gastheer & Whisky Sommelier';
      ambBio = 'Varen over de Gentse wateren met vakkundige proefnotities en prachtige stadsverhalen.';
      lineupHeading = 'Inbegrepen tijdens de vaartocht:';
      lineup = [
        '50 minuten rondvaart door historisch Gent',
        'Geselecteerde premium whisky proefdrams',
        'Begeleiding door ervaren whiskygids'
      ];
    } else if (domCategory === 'botteling') {
      tag = 'FESTIVAL BOTTELING';
      subtitle = 'Gelimiteerde Cask Strength Single Cask';
      location = 'Uitsluitend af te halen tijdens het festival bij De Oude Vismijn';
      duration = 'Festival Cask Exclusive';
      descHeading = 'Over deze Festival Botteling';
      description = `Officiële exclusieve botteling voor het Gents Whisky Festival 2026. ${item?.title || ''}`;
      ambName = 'Festival Bottling Panel';
      ambTitle = 'Cask Selection Committee';
      ambBio = 'Persoonlijk geselecteerd uit de beste vaten op vatsterkte.';
      lineupHeading = 'Details van de botteling:';
      lineup = [
        '70cl Volledige Fles (Cask Strength)',
        'Officieel genummerd herkomstcertificaat'
      ];
    } else if (domCategory === 'masterclass') {
      tag = 'SPECIAL MASTERCLASS';
      subtitle = `${item?.date || ''} • ${item?.time || ''}`;
      location = item?.location || 'De Oude Vismijn (Spaarkamer)';
      duration = '45 minuten';
      descHeading = 'Over deze Masterclass';
      description = `Exclusieve masterclass: ${item?.title || ''}. ${item?.extra ? 'Extra toelichting: ' + item.extra : ''}`;
      ambName = (item?.title && item.title.includes('Macallan')) ? 'Gerard Spaans' : 'Brand Ambassador';
      ambTitle = (item?.title && item.title.includes('Macallan')) ? 'Senior Brand Ambassador & Master of Malt' : 'Senior Brand Ambassador';
      ambBio = 'Internationaal expert met diepgaande kennis over distillatietechnieken, vatselecties en de unieke smaakprofielen van deze distilleerderij.';
      lineupHeading = 'Officiële Proeflijst (Inbegrepen Drams):';
      lineup = [
        'Proefdram #1: Distillery Signature Expressie (12 Y.O.)',
        'Proefdram #2: Cask Strength Single Cask Sample',
        'Proefdram #3: Exclusieve Vintage Editie'
      ];
    }

    // Dynamic overrides from GHL or dataset if provided
    if (item?.description && item.description.trim() !== '') {
      description = item.description;
    }
    if (item?.price !== undefined && item.price !== null && item.price > 0) {
      numericPrice = typeof item.price === 'number' ? item.price : (parseFloat(item.price) || numericPrice);
    }
    if (item?.ambassadorName && item.ambassadorName.trim() !== '') ambName = item.ambassadorName;
    if (item?.ambassadorTitle && item.ambassadorTitle.trim() !== '') ambTitle = item.ambassadorTitle;
    if (item?.ambassadorBio && item.ambassadorBio.trim() !== '') ambBio = item.ambassadorBio;
    if (item?.tastingLineup && Array.isArray(item.tastingLineup) && item.tastingLineup.length > 0) {
      lineup = item.tastingLineup;
    }

    // Populate modal fields
    const tagEl = document.querySelector('#masterclass-info-modal span[style*="font-family: var(--font-sans)"]');
    const titleEl = document.getElementById('mc-modal-title');
    const subtitleEl = document.getElementById('mc-modal-subtitle');
    const locationEl = document.getElementById('mc-modal-location');
    const durationEl = document.getElementById('mc-modal-duration');
    const aboutHeadingEl = document.getElementById('mc-modal-about-heading');
    const descEl = document.getElementById('mc-modal-description');
    const lineupList = document.getElementById('mc-modal-lineup');
    const lineupHeadingEl = document.getElementById('mc-modal-lineup-heading');
    const ambNameEl = document.getElementById('mc-modal-ambassador-name');
    const ambTitleEl = document.getElementById('mc-modal-ambassador-title');
    const ambBioEl = document.getElementById('mc-modal-ambassador-bio');

    if (tagEl) tagEl.textContent = tag;
    if (titleEl) titleEl.textContent = title;
    if (subtitleEl) subtitleEl.textContent = subtitle;
    if (locationEl) locationEl.textContent = location;
    if (durationEl) durationEl.textContent = duration;
    if (aboutHeadingEl) aboutHeadingEl.textContent = descHeading;
    if (descEl) descEl.textContent = description;

    if (lineupHeadingEl) lineupHeadingEl.textContent = lineupHeading;

    if (ambNameEl) ambNameEl.textContent = ambName;
    if (ambTitleEl) ambTitleEl.textContent = ambTitle;
    if (ambBioEl) ambBioEl.textContent = ambBio;

    if (lineupList) {
      lineupList.innerHTML = lineup.map(dram => `
        <li style="display: flex; align-items: center; gap: 0.75rem; padding: 0.6rem 0.9rem; background: #FFFFFF; border: 1px solid #E6DDD0; border-radius: 6px; font-size: 0.9rem; color: var(--text-charcoal, #171614);">
          <span style="color: #1E3A8A; font-size: 0.75rem; line-height: 1;">◆</span>
          <span style="font-weight: 600;">${dram}</span>
        </li>
      `).join('');
    }

    // Format Price correctly
    const priceSplit = numericPrice.toFixed(2).split('.');
    const priceValEl = document.getElementById('mc-modal-price-val');
    const priceCentsEl = document.getElementById('mc-modal-price-cents');
    if (priceValEl) priceValEl.textContent = priceSplit[0];
    if (priceCentsEl) priceCentsEl.textContent = ',' + priceSplit[1];

    if (mcModal) {
      mcModal.style.display = 'flex';
      document.body.style.overflow = 'hidden';
    }
  }

  // Global delegation for opening info modal (Clicking info link or ticket title)
  document.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    if (target.closest('.qty-selector, .qty-btn, .minus-btn, .plus-btn')) return;

    const infoBtn = target.closest('.btn-mc-info-trigger');
    if (infoBtn) {
      e.preventDefault();
      const mcId = infoBtn.getAttribute('data-mc-id');
      if (mcId) openMasterclassModal(mcId);
      return;
    }

    const titleClick = target.closest('.ticket-title');
    if (titleClick) {
      const stubEl = titleClick.closest('.ticket-stub');
      const mcId = stubEl?.getAttribute('data-ticket-id');
      if (mcId) openMasterclassModal(mcId);
    }
  });

  // Close MC Modal
  if (mcModalClose) mcModalClose.addEventListener('click', () => {
    if (mcModal) mcModal.style.display = 'none';
    document.body.style.overflow = '';
  });
  if (mcModal) mcModal.addEventListener('click', (e) => {
    if (e.target === mcModal) {
      mcModal.style.display = 'none';
      document.body.style.overflow = '';
    }
  });

  // Add MC to Cart from Modal
  if (mcModalAddBtn) {
    mcModalAddBtn.addEventListener('click', () => {
      if (activeMcId) {
        let stub = document.querySelector(`.ticket-stub[data-ticket-id="${activeMcId}"]`);
        if (!stub) {
          // fallback search
          const allStubs = Array.from(document.querySelectorAll('.ticket-stub[data-category="masterclass"]'));
          if (allStubs.length > 0) stub = allStubs[0];
        }
        if (stub) {
          const plusBtn = stub.querySelector('.plus-btn') as HTMLButtonElement;
          if (plusBtn) plusBtn.click();
        }
      }
      if (mcModal) mcModal.style.display = 'none';
      document.body.style.overflow = '';
    });
  }

  </script>
